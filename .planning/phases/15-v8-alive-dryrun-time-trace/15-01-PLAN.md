---
phase: 15-v8-alive-dryrun-time-trace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/TcpServer/VisionRequestPacket.cs
  - WPF_Example/TcpServer/VisionResponsePacket.cs
  - WPF_Example/Custom/TcpServer/ResourceMap.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements:
  - TERM-03
  - TERM-04
  - TERM-05

must_haves:
  truths:
    - "$DRYRUN:1,1@ 수신 시 $DRYRUN:1,OK@ 응답하고 내부 _dryRunMode=true 설정"
    - "$DRYRUN:1,0@ 수신 시 $DRYRUN:1,OK@ 응답하고 내부 _dryRunMode=false 설정"
    - "DryRun ON 상태에서 $TEST 수신 시 Sequences.Start() 호출 없이 즉시 $RESULT:Site,Type,P@ 응답"
    - "DryRun OFF 상태에서 $TEST 수신 시 정상 검사 시퀀스 실행"
    - "$TIME:1,2026,4,10,14,0,0@ 수신 시 내부 DateTime 저장 + $TIME:1,OK@ 응답"
    - "$TRACE:1,P001,MAT001@ 수신 시 내부 string 저장 + $TRACE:1,OK@ 응답 (다음 $TRACE까지 값 유지)"
  artifacts:
    - path: "WPF_Example/TcpServer/VisionRequestPacket.cs"
      provides: "DryRunPacket, TimePacket, TracePacket 서브클래스 + 파싱 case"
      contains: "CMD_RECV_DRYRUN"
    - path: "WPF_Example/TcpServer/VisionResponsePacket.cs"
      provides: "DryRunResultPacket, TimeResultPacket, TraceResultPacket 서브클래스 + 직렬화 case"
      contains: "CMD_SEND_DRYRUN"
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessDryRun, ProcessTime, ProcessTrace 메서드 + _dryRunMode 플래그"
      contains: "_dryRunMode"
  key_links:
    - from: "VisionRequestPacket.Convert(string)"
      to: "SystemHandler.MainRun() switch"
      via: "VisionRequestType enum dispatch"
      pattern: "case VisionRequestType\\.DryRun"
    - from: "SystemHandler.ProcessTest()"
      to: "_dryRunMode flag"
      via: "if (_dryRunMode) early return"
      pattern: "if \\(_dryRunMode\\)"
---

<objective>
DRYRUN/TIME/TRACE 3개 신규 TCP 명령의 패킷 파싱/직렬화/처리 로직을 추가하고, DryRun 모드에서 $TEST 시 즉시 OK 응답하는 인터셉트 로직을 구현한다. $LIGHT는 현 응답 포맷을 정상 스펙으로 확정하여 본 Phase에서 코드 수정 없음 (CONTEXT.md D-01).

Purpose: 설비 연동 시 DryRun(통신 테스트), 시간 동기화, Pallet/Material 추적 기능 제공
Output: 3개 신규 명령 처리 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-v8-alive-dryrun-time-trace/15-RESEARCH.md
@.planning/Request_v8_TerminalMode.md

<interfaces>
<!-- VisionRequestPacket.cs: 기존 패킷 파싱 패턴 -->
<!-- enum VisionRequestType { RecipeChange, RecipeGet, SiteStatus, Light, Test, Unknown=999 } -->
<!-- Convert(string) 내부 switch(msgList[0]) 에서 CMD_RECV_XXX 상수로 분기 -->
<!-- 각 서브클래스는 VisionRequestPacket(VisionRequestType.XXX) 호출하는 생성자 패턴 -->

<!-- VisionResponsePacket.cs: 기존 응답 직렬화 패턴 -->
<!-- enum EVisionResponseType { RecipeChange, RecipeGet, SiteStatus, Light, Test, GrabStatus, Unknown=999 } -->
<!-- Convert(VisionResponsePacket) 내부 switch(packet.ResponseType) 에서 msg += CMD_SEND_XXX + CMD_SEPERATOR + fields -->

<!-- SystemHandler.cs (Custom): MainRun() 내부 switch(packet.RequestType) 에서 ProcessXxx() 메서드 호출 후 responsePacket 반환 -->
<!-- responsePacket != null 이면 Server.SendPacket(i, responsePacket) 로 즉시 응답 -->

<!-- ResourceMap.cs: SetIdentifier() 내부 switch(packet.RequestType) 에서 Find(EResource, ESite) 매핑 -->

<!-- VisionServer 상수: MSG_STX='$', MSG_ETX='@', MSG_CONTENTS_SEPERATOR=',', MSG_CMD_SEPERATOR=':' -->

<!-- SequenceBase.ResponseQueue: ConcurrentQueue<TestResultPacket>, public -->
<!-- SequenceHandler[string name] 인덱서: Sequences.ElementAt(i).Value.Name 매칭 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DryRun/Time/Trace 패킷 타입 추가 (Request + Response)</name>
  <files>
    WPF_Example/TcpServer/VisionRequestPacket.cs
    WPF_Example/TcpServer/VisionResponsePacket.cs
    WPF_Example/Custom/TcpServer/ResourceMap.cs
  </files>
  <read_first>
    WPF_Example/TcpServer/VisionRequestPacket.cs
    WPF_Example/TcpServer/VisionResponsePacket.cs
    WPF_Example/Custom/TcpServer/ResourceMap.cs
    WPF_Example/TcpServer/VisionServer.cs
  </read_first>
  <action>
**VisionRequestPacket.cs:**

1. `VisionRequestType` enum에 3개 추가 (Test 뒤, Unknown 앞):
```csharp
DryRun,   //260413 hbk
Time,     //260413 hbk
Trace,    //260413 hbk
```

2. CMD_RECV 상수 3개 추가 (CMD_RECV_TEST 아래):
```csharp
public const string CMD_RECV_DRYRUN = "DRYRUN";  //260413 hbk
public const string CMD_RECV_TIME = "TIME";       //260413 hbk
public const string CMD_RECV_TRACE = "TRACE";     //260413 hbk
```

3. `Convert(string msg)` 메서드의 switch(msgList[0]) 에 3개 case 추가:

```csharp
case CMD_RECV_DRYRUN:  //260413 hbk
    packet = new DryRunPacket();
    DryRunPacket dryRunPacket = packet.AsDryRun();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    if (dataList.Length < 2) return null;
    if (Int32.TryParse(dataList[0], out siteNum) == false) return null;
    dryRunPacket.Site = siteNum;
    if (Int32.TryParse(dataList[1], out int dryRunState) == false) return null;
    dryRunPacket.Enable = (dryRunState == 1);
    break;

case CMD_RECV_TIME:  //260413 hbk
    packet = new TimePacket();
    TimePacket timePacket = packet.AsTime();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    if (dataList.Length < 7) return null;
    if (Int32.TryParse(dataList[0], out siteNum) == false) return null;
    timePacket.Site = siteNum;
    if (Int32.TryParse(dataList[1], out int year) == false) return null;
    if (Int32.TryParse(dataList[2], out int month) == false) return null;
    if (Int32.TryParse(dataList[3], out int day) == false) return null;
    if (Int32.TryParse(dataList[4], out int hour) == false) return null;
    if (Int32.TryParse(dataList[5], out int minute) == false) return null;
    if (Int32.TryParse(dataList[6], out int second) == false) return null;
    try {
        timePacket.SyncedTime = new DateTime(year, month, day, hour, minute, second);
    } catch (ArgumentOutOfRangeException) {
        return null;  //260413 hbk — 유효하지 않은 날짜/시간 값
    }
    break;

case CMD_RECV_TRACE:  //260413 hbk
    packet = new TracePacket();
    TracePacket tracePacket = packet.AsTrace();
    dataList = msgList[1].Split(VisionServer.MSG_CONTENTS_SEPERATOR);
    if (dataList.Length < 3) return null;
    if (Int32.TryParse(dataList[0], out siteNum) == false) return null;
    tracePacket.Site = siteNum;
    tracePacket.PalletId = dataList[1];
    tracePacket.MaterialId = dataList[2];
    break;
```

4. AsXxx() 캐스팅 메서드 3개 추가 (AsRecipeGet() 아래):
```csharp
public DryRunPacket AsDryRun() {  //260413 hbk
    if (RequestType != VisionRequestType.DryRun) return null;
    return this as DryRunPacket;
}
public TimePacket AsTime() {  //260413 hbk
    if (RequestType != VisionRequestType.Time) return null;
    return this as TimePacket;
}
public TracePacket AsTrace() {  //260413 hbk
    if (RequestType != VisionRequestType.Trace) return null;
    return this as TracePacket;
}
```

5. 서브클래스 3개 추가 (파일 맨 끝, RecipeGetPacket 클래스 아래, namespace 닫기 괄호 앞):
```csharp
public class DryRunPacket : VisionRequestPacket {  //260413 hbk
    public bool Enable { get; set; }
    public DryRunPacket() : base(VisionRequestType.DryRun) { }
}

public class TimePacket : VisionRequestPacket {  //260413 hbk
    public DateTime SyncedTime { get; set; }
    public TimePacket() : base(VisionRequestType.Time) { }
}

public class TracePacket : VisionRequestPacket {  //260413 hbk
    public string PalletId { get; set; }
    public string MaterialId { get; set; }
    public TracePacket() : base(VisionRequestType.Trace) { }
}
```

**VisionResponsePacket.cs:**

1. `EVisionResponseType` enum에 3개 추가 (GrabStatus 뒤, Unknown 앞):
```csharp
DryRun,   //260413 hbk
Time,     //260413 hbk
Trace,    //260413 hbk
```

2. CMD_SEND 상수 3개 추가 (CMD_SEND_GRAB_STATUS 아래):
```csharp
public const string CMD_SEND_DRYRUN = "DRYRUN";  //260413 hbk
public const string CMD_SEND_TIME = "TIME";       //260413 hbk
public const string CMD_SEND_TRACE = "TRACE";     //260413 hbk
```

3. `Convert(VisionResponsePacket packet)` 메서드의 switch에 3개 case 추가 (Test case 뒤):
```csharp
case EVisionResponseType.DryRun:  //260413 hbk
    DryRunResultPacket dryRunPacket = packet.AsDryRunResult();
    msg += CMD_SEND_DRYRUN;
    msg += VisionServer.MSG_CMD_SEPERATOR;
    msg += dryRunPacket.Site.ToString();
    msg += VisionServer.MSG_CONTENTS_SEPERATOR;
    msg += RESULT_OK;
    break;

case EVisionResponseType.Time:  //260413 hbk
    TimeResultPacket timePacket2 = packet.AsTimeResult();
    msg += CMD_SEND_TIME;
    msg += VisionServer.MSG_CMD_SEPERATOR;
    msg += timePacket2.Site.ToString();
    msg += VisionServer.MSG_CONTENTS_SEPERATOR;
    msg += RESULT_OK;
    break;

case EVisionResponseType.Trace:  //260413 hbk
    TraceResultPacket tracePacket = packet.AsTraceResult();
    msg += CMD_SEND_TRACE;
    msg += VisionServer.MSG_CMD_SEPERATOR;
    msg += tracePacket.Site.ToString();
    msg += VisionServer.MSG_CONTENTS_SEPERATOR;
    msg += RESULT_OK;
    break;
```

4. **$LIGHT case는 건드리지 않는다.** 현재 코드(`$LIGHT:Site,OP` 응답)가 정상 스펙으로 확정됨 (CONTEXT.md D-01). 조명은 설비당 1개라 Type 필드 불필요.

5. AsXxxResult() 캐스팅 메서드 3개 추가 (AsGrabStatusResult() 아래):
```csharp
public DryRunResultPacket AsDryRunResult() {  //260413 hbk
    if (ResponseType != EVisionResponseType.DryRun) return null;
    return this as DryRunResultPacket;
}
public TimeResultPacket AsTimeResult() {  //260413 hbk
    if (ResponseType != EVisionResponseType.Time) return null;
    return this as TimeResultPacket;
}
public TraceResultPacket AsTraceResult() {  //260413 hbk
    if (ResponseType != EVisionResponseType.Trace) return null;
    return this as TraceResultPacket;
}
```

6. 응답 서브클래스 3개 추가 (GrabStatusResultPacket 클래스 아래, namespace 닫기 괄호 앞):
```csharp
public class DryRunResultPacket : VisionResponsePacket {  //260413 hbk
    public DryRunResultPacket() : base(EVisionResponseType.DryRun) { }
}

public class TimeResultPacket : VisionResponsePacket {  //260413 hbk
    public TimeResultPacket() : base(EVisionResponseType.Time) { }
}

public class TraceResultPacket : VisionResponsePacket {  //260413 hbk
    public TraceResultPacket() : base(EVisionResponseType.Trace) { }
}
```

**ResourceMap.cs:**

`SetIdentifier()` 메서드의 switch에 3개 case 추가 (Unknown case 앞):
```csharp
case VisionRequestType.DryRun:   //260413 hbk — 리소스 매핑 불필요
case VisionRequestType.Time:     //260413 hbk
case VisionRequestType.Trace:    //260413 hbk
    break;
```
  </action>
  <verify>
    <automated>cd "D:/Project/FinalVision" && grep -c "CMD_RECV_DRYRUN\|CMD_RECV_TIME\|CMD_RECV_TRACE" WPF_Example/TcpServer/VisionRequestPacket.cs && grep -c "CMD_SEND_DRYRUN\|CMD_SEND_TIME\|CMD_SEND_TRACE" WPF_Example/TcpServer/VisionResponsePacket.cs && grep -c "VisionRequestType.DryRun\|VisionRequestType.Time\|VisionRequestType.Trace" WPF_Example/Custom/TcpServer/ResourceMap.cs</automated>
  </verify>
  <acceptance_criteria>
    - VisionRequestPacket.cs contains `public const string CMD_RECV_DRYRUN = "DRYRUN"`
    - VisionRequestPacket.cs contains `public const string CMD_RECV_TIME = "TIME"`
    - VisionRequestPacket.cs contains `public const string CMD_RECV_TRACE = "TRACE"`
    - VisionRequestPacket.cs contains `class DryRunPacket : VisionRequestPacket`
    - VisionRequestPacket.cs contains `class TimePacket : VisionRequestPacket`
    - VisionRequestPacket.cs contains `class TracePacket : VisionRequestPacket`
    - VisionRequestPacket.cs contains `case CMD_RECV_DRYRUN:`
    - VisionRequestPacket.cs contains `case CMD_RECV_TIME:`
    - VisionRequestPacket.cs contains `case CMD_RECV_TRACE:`
    - VisionRequestPacket.cs contains `new DateTime(year, month, day, hour, minute, second)`
    - VisionRequestPacket.cs contains `catch (ArgumentOutOfRangeException)`
    - VisionRequestPacket.cs contains `if (dataList.Length < 7) return null` (TIME guard)
    - VisionResponsePacket.cs contains `public const string CMD_SEND_DRYRUN = "DRYRUN"`
    - VisionResponsePacket.cs contains `CMD_SEND_TIME`
    - VisionResponsePacket.cs contains `CMD_SEND_TRACE`
    - VisionResponsePacket.cs contains `class DryRunResultPacket : VisionResponsePacket`
    - VisionResponsePacket.cs contains `class TimeResultPacket : VisionResponsePacket`
    - VisionResponsePacket.cs contains `class TraceResultPacket : VisionResponsePacket`
    - VisionResponsePacket.cs Light case is UNCHANGED (still contains `GetOnString()` — not modified in Phase 15)
    - ResourceMap.cs contains `case VisionRequestType.DryRun:`
  </acceptance_criteria>
  <done>DryRun/Time/Trace 패킷 파싱+직렬화+ResourceMap 등록 완료. $LIGHT는 수정 없음 (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: SystemHandler DRYRUN/TIME/TRACE 처리 메서드 + DryRun ProcessTest 인터셉트</name>
  <files>
    WPF_Example/Custom/SystemHandler.cs
  </files>
  <read_first>
    WPF_Example/Custom/SystemHandler.cs
    WPF_Example/SystemHandler.cs
    WPF_Example/Sequence/Sequence/SequenceBase.cs
    WPF_Example/TcpServer/VisionRequestPacket.cs
  </read_first>
  <action>
**WPF_Example/Custom/SystemHandler.cs:**

1. 필드 3개 추가 (클래스 시작 부분, MainRun() 메서드 앞):
```csharp
//260413 hbk — v8 터미널 모드 상태 필드
private volatile bool _dryRunMode = false;         //260413 hbk — DryRun ON/OFF
private DateTime _syncedTime = DateTime.MinValue;  //260413 hbk — TIME 동기화 값 (Windows 시계 미변경)
private string _palletId = "";                     //260413 hbk — TRACE Pallet ID (다음 $TRACE까지 유지)
private string _materialId = "";                   //260413 hbk — TRACE Material ID (다음 $TRACE까지 유지)
```

2. MainRun() switch에 3개 case 추가 (Test case 뒤, Unknown case 앞):
```csharp
case VisionRequestType.DryRun:  //260413 hbk
    responsePacket = ProcessDryRun(packet.AsDryRun());
    break;
case VisionRequestType.Time:  //260413 hbk
    responsePacket = ProcessTime(packet.AsTime());
    break;
case VisionRequestType.Trace:  //260413 hbk
    responsePacket = ProcessTrace(packet.AsTrace());
    break;
```

3. ProcessTest() 메서드를 DryRun 인터셉트 추가로 수정:
```csharp
private bool ProcessTest(TestPacket packet) {
    if (_dryRunMode) {  //260413 hbk — DryRun ON: Sequence 실행 없이 즉시 OK 응답
        TestResultPacket dryResult = new TestResultPacket();
        dryResult.Target = packet.Sender;
        dryResult.Site = packet.Site;
        dryResult.InspectionType = packet.TestType;
        dryResult.Result = EVisionResultType.OK;
        SequenceBase seq = Sequences[packet.Identifier];
        if (seq != null) {
            seq.ResponseQueue.Enqueue(dryResult);  //260413 hbk — MainRun 상단 PopResponse 경로로 송신
        }
        Logging.PrintLog((int)ELogType.Trace, "[DRYRUN] TEST skipped — immediate OK. Site:{0} Type:{1}", packet.Site, packet.TestType);
        return true;
    }
    return Sequences.Start(packet);
}
```

4. ProcessDryRun() 메서드 추가 (SendTestError() 아래):
```csharp
private DryRunResultPacket ProcessDryRun(DryRunPacket packet) {  //260413 hbk
    _dryRunMode = packet.Enable;
    DryRunResultPacket result = new DryRunResultPacket();
    result.Target = packet.Sender;
    result.Site = packet.Site;
    Logging.PrintLog((int)ELogType.Trace, "[DRYRUN] Mode={0}", _dryRunMode ? "ON" : "OFF");
    return result;
}
```

5. ProcessTime() 메서드 추가:
```csharp
private TimeResultPacket ProcessTime(TimePacket packet) {  //260413 hbk
    _syncedTime = packet.SyncedTime;
    TimeResultPacket result = new TimeResultPacket();
    result.Target = packet.Sender;
    result.Site = packet.Site;
    Logging.PrintLog((int)ELogType.Trace, "[TIME] Synced={0:yyyy-MM-dd HH:mm:ss}", _syncedTime);
    return result;
}
```

6. ProcessTrace() 메서드 추가:
```csharp
private TraceResultPacket ProcessTrace(TracePacket packet) {  //260413 hbk
    _palletId = packet.PalletId;
    _materialId = packet.MaterialId;
    TraceResultPacket result = new TraceResultPacket();
    result.Target = packet.Sender;
    result.Site = packet.Site;
    Logging.PrintLog((int)ELogType.Trace, "[TRACE] PalletId={0} MaterialId={1}", _palletId, _materialId);
    return result;
}
```
  </action>
  <verify>
    <automated>cd "D:/Project/FinalVision" && grep -c "_dryRunMode\|ProcessDryRun\|ProcessTime\|ProcessTrace" WPF_Example/Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - SystemHandler.cs contains `private volatile bool _dryRunMode = false;`
    - SystemHandler.cs contains `private DateTime _syncedTime = DateTime.MinValue;`
    - SystemHandler.cs contains `private string _palletId = "";`
    - SystemHandler.cs contains `private string _materialId = "";`
    - SystemHandler.cs contains `case VisionRequestType.DryRun:`
    - SystemHandler.cs contains `case VisionRequestType.Time:`
    - SystemHandler.cs contains `case VisionRequestType.Trace:`
    - SystemHandler.cs contains `if (_dryRunMode)` inside ProcessTest method
    - SystemHandler.cs contains `seq.ResponseQueue.Enqueue(dryResult);`
    - SystemHandler.cs contains `Sequences[packet.Identifier]` with null check in DryRun path
    - SystemHandler.cs contains `private DryRunResultPacket ProcessDryRun(DryRunPacket packet)`
    - SystemHandler.cs contains `private TimeResultPacket ProcessTime(TimePacket packet)`
    - SystemHandler.cs contains `private TraceResultPacket ProcessTrace(TracePacket packet)`
    - SystemHandler.cs contains `Logging.PrintLog` calls with [DRYRUN], [TIME], [TRACE] prefixes
  </acceptance_criteria>
  <done>DryRun/TIME/TRACE 명령 처리 완료. DryRun ON 시 $TEST가 Sequences.Start() 없이 즉시 OK 응답. _syncedTime, _palletId, _materialId 내부 저장 동작.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TCP Client -> SystemHandler | 외부 Client가 ASCII 명령을 송신하는 경계. 모든 입력은 untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | VisionRequestPacket.Convert — TIME 필드 | mitigate | `dataList.Length < 7` guard + `Int32.TryParse` 각 필드 + `new DateTime()` try-catch (ArgumentOutOfRangeException) → return null |
| T-15-02 | Tampering | VisionRequestPacket.Convert — DRYRUN 필드 | mitigate | `dataList.Length < 2` guard + `Int32.TryParse` (enable 값) |
| T-15-03 | Tampering | VisionRequestPacket.Convert — TRACE 필드 | accept | `dataList.Length < 3` guard. palletId/materialId 콤마 금지는 Client 책임 (프로토콜 규격). Vision은 수신 그대로 저장. |
| T-15-04 | Denial of Service | ProcessDryRun | accept | _dryRunMode는 volatile bool — 한 번만 세팅하므로 리소스 소모 없음. Client가 반복 송신해도 단순 bool 토글. |
| T-15-05 | Information Disclosure | _palletId, _materialId | accept | 로그에만 출력. TCP 응답에 값 포함 안 함. 단일 Client 환경(MAX_CONNECTION_COUNT=1). |
</threat_model>

<verification>
1. 빌드 성공: `msbuild FinalVision.sln /p:Configuration=Debug /p:Platform=x64 /t:Build` 에러 0
2. grep 확인: VisionRequestPacket.cs에 CMD_RECV_DRYRUN, CMD_RECV_TIME, CMD_RECV_TRACE 존재
3. grep 확인: VisionResponsePacket.cs에 CMD_SEND_DRYRUN, CMD_SEND_TIME, CMD_SEND_TRACE 존재
4. grep 확인: SystemHandler.cs에 _dryRunMode, ProcessDryRun, ProcessTime, ProcessTrace 존재
</verification>

<success_criteria>
- DRYRUN/TIME/TRACE 3개 명령의 Request 파싱, Response 직렬화, SystemHandler 처리 메서드가 모두 구현됨
- DryRun ON 상태에서 ProcessTest()가 Sequences.Start() 호출 없이 ResponseQueue에 OK 결과를 직접 Enqueue
- $TIME 파싱 시 유효하지 않은 날짜 값에 대해 null 반환 (방어 코드)
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/15-v8-alive-dryrun-time-trace/15-01-SUMMARY.md`
</output>
