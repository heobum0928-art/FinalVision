---
phase: 14-framewidth-frameheight-lightgroupname
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Sequence/Param/CameraSlaveParam.cs
  - WPF_Example/Sequence/Param/CameraParam.cs
  - WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "INI 파일에 FrameWidth, FrameHeight 항목이 저장되지 않는다"
    - "레시피 로드 후 LightGroupName이 INI에 저장된 값을 유지한다 (DefaultLight로 덮어쓰이지 않음)"
    - "Reset 후 LightGroupName이 백업 시점 값으로 복원된다"
  artifacts:
    - path: "WPF_Example/Sequence/Param/CameraSlaveParam.cs"
      provides: "FrameWidth/FrameHeight 필드 제거, LightGroupName CopyTo 주석 해제"
      contains: "slaveParam.LightGroupName = this.LightGroupName"
    - path: "WPF_Example/Sequence/Param/CameraParam.cs"
      provides: "FrameWidth/FrameHeight 필드 제거"
    - path: "WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs"
      provides: "OnLoad에서 LightGroupName 강제 재설정 제거"
  key_links:
    - from: "Sequence_Inspection.OnLoad()"
      to: "CameraMasterParam.LightGroupName setter"
      via: "OnLoad가 더 이상 DefaultLight를 강제 세팅하지 않으므로 INI 로드 값이 유지됨"
      pattern: "OnLoad.*does NOT contain.*_MyParam.LightGroupName"
    - from: "CameraSlaveParam.CopyTo()"
      to: "InspectionParam.LightGroupName"
      via: "Reset(RestoreShot)이 CopyTo를 호출하면 LightGroupName도 복원됨"
      pattern: "slaveParam\\.LightGroupName = this\\.LightGroupName"
---

<objective>
레시피 INI 파일의 세 가지 설정값 버그를 수정한다: (1) 레거시 FrameWidth/FrameHeight 필드가 0으로 저장되는 오염 제거, (2) 레시피 로드 후 LightGroupName이 DefaultLight로 강제 덮어쓰이는 문제 제거, (3) Reset 시 LightGroupName이 복원되지 않는 CopyTo 버그 수정.

Purpose: INI 파일에 불필요한 값이 저장되지 않고, LightGroupName이 레시피별로 올바르게 저장/로드/복원된다.
Output: 3개 소스 파일 수정, MSBuild 0 errors
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-framewidth-frameheight-lightgroupname/14-RESEARCH.md

<interfaces>
<!-- CameraMasterParam.LightGroupName setter propagates to all ChildList -->
From WPF_Example/Sequence/Param/CameraMasterParam.cs:
```csharp
public string LightGroupName {
    get { return _LightGroupName; }
    set {
        _LightGroupName = value;
        foreach (CameraSlaveParam camParam in ChildList) {
            camParam.LightGroupName = _LightGroupName;
        }
    }
}
```

<!-- ParamBase.Save() uses reflection to serialize all public properties -->
<!-- FrameWidth/FrameHeight are public int → saved as 0 to INI even with [Browsable(false)] -->

<!-- Sequence_Inspection.RestoreShot calls _backup[shotIndex].CopyTo(target) -->
From WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs:
```csharp
public bool RestoreShot(int shotIndex) {
    if (!_backup.ContainsKey(shotIndex)) return false;
    if (shotIndex < 0 || shotIndex >= ActionCount) return false;
    if (!(this[shotIndex].Param is InspectionParam target)) return false;
    _backup[shotIndex].CopyTo(target);
    return true;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FrameWidth/FrameHeight 레거시 필드 완전 제거</name>
  <files>WPF_Example/Sequence/Param/CameraSlaveParam.cs, WPF_Example/Sequence/Param/CameraParam.cs</files>
  <read_first>
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
    WPF_Example/Sequence/Param/CameraParam.cs
  </read_first>
  <action>
CameraSlaveParam.cs 수정:
1. Lines 21-24 삭제 (FrameWidth, FrameHeight 프로퍼티 선언 2개 + Browsable 어트리뷰트 2개). 해당 위치에 주석 추가: `//260407 hbk — FrameWidth/FrameHeight 레거시 필드 제거 (INI 오염 방지)`
2. CopyTo() 메서드 내 CameraSlaveParam 분기 (현재 lines 188-190):
   - `slaveParam.FrameWidth = this.FrameWidth;` 라인 삭제
   - `slaveParam.FrameHeight = this.FrameHeight;` 라인 삭제
   - 해당 위치의 기존 주석 `// 레거시 필드 대입 제거  //260327 hbk`도 삭제
3. CopyTo() 메서드 내 CameraParam 분기 (현재 lines 205-207):
   - `camParam.FrameWidth = this.FrameWidth;` 라인 삭제
   - `camParam.FrameHeight = this.FrameHeight;` 라인 삭제
   - 해당 위치의 기존 주석 `// 레거시 필드 대입 제거  //260327 hbk`도 삭제

CameraParam.cs 수정:
1. Lines 73-76 삭제 (FrameWidth, FrameHeight 프로퍼티 선언 2개 + Browsable 어트리뷰트 2개). 해당 위치에 주석 추가: `//260407 hbk — FrameWidth/FrameHeight 레거시 필드 제거 (INI 오염 방지)`
2. CopyTo() 메서드 내 CameraSlaveParam 분기 (현재 lines 241-243):
   - `slaveParam.FrameWidth = this.FrameWidth;` 라인 삭제
   - `slaveParam.FrameHeight = this.FrameHeight;` 라인 삭제
   - 해당 위치의 기존 주석 `// 레거시 필드 대입 제거  //260327 hbk`도 삭제
3. CopyTo() 메서드 내 CameraParam 분기 (현재 lines 258-260):
   - `camParam.FrameWidth = this.FrameWidth;` 라인 삭제
   - `camParam.FrameHeight = this.FrameHeight;` 라인 삭제
   - 해당 위치의 기존 주석 `// 레거시 필드 대입 제거  //260327 hbk`도 삭제

주석 규칙: `//260407 hbk` 형식으로 날짜 태그를 포함한다.
  </action>
  <verify>
    <automated>MSBuild WPF_Example.sln /p:Configuration=Debug /v:minimal 2>&amp;1 | grep -E "error|Error|Build succeeded" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - CameraSlaveParam.cs에 `FrameWidth` 문자열이 존재하지 않는다 (grep -c "FrameWidth" 결과 0)
    - CameraSlaveParam.cs에 `FrameHeight` 문자열이 존재하지 않는다
    - CameraParam.cs에 `FrameWidth` 문자열이 존재하지 않는다
    - CameraParam.cs에 `FrameHeight` 문자열이 존재하지 않는다
    - CameraSlaveParam.cs에 `260407 hbk` 주석이 존재한다
    - CameraParam.cs에 `260407 hbk` 주석이 존재한다
    - MSBuild 0 errors
  </acceptance_criteria>
  <done>FrameWidth/FrameHeight 프로퍼티가 CameraSlaveParam.cs와 CameraParam.cs에서 완전히 제거되고 CopyTo 대입도 제거됨. 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: LightGroupName OnLoad 덮어쓰기 제거 + CopyTo 복원 활성화</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs, WPF_Example/Sequence/Param/CameraSlaveParam.cs, WPF_Example/Sequence/Param/CameraParam.cs</files>
  <read_first>
    WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
    WPF_Example/Sequence/Param/CameraParam.cs
  </read_first>
  <action>
Sequence_Inspection.cs OnLoad() 수정 (현재 line 128):
1. `_MyParam.LightGroupName = DefaultLight;` 라인을 삭제한다.
2. 기존 주석 `//260407 hbk — INI Load가 ChildList에 레거시 값(Corner_Align 등) 덮어쓰므로 기본값 재세팅`을 수정하여:
   `//260407 hbk — DeviceName만 기본값 재세팅 (LightGroupName은 INI 로드 값 유지)`
3. `_MyParam.DeviceName = DefaultCamera;` 라인은 유지한다 (카메라 디바이스는 항상 DefaultCamera여야 함).

CameraSlaveParam.cs CopyTo() 수정:
1. CameraSlaveParam 분기 내 `//slaveParam.LightGroupName = this.LightGroupName;` (현재 line 179) 주석을 해제하여 활성화:
   `slaveParam.LightGroupName = this.LightGroupName;   //260407 hbk — Reset 시 LightGroupName 복원 활성화`
2. CameraParam 분기 내 `//camParam.LightGroupName = this.LightGroupName;` (현재 line 196) 주석을 해제하여 활성화:
   `camParam.LightGroupName = this.LightGroupName;   //260407 hbk — Reset 시 LightGroupName 복원 활성화`

CameraParam.cs CopyTo() 수정:
1. CameraParam 분기 내 `//camParam.LightGroupName = this.LightGroupName;` (현재 line 249) 주석을 해제하여 활성화:
   `camParam.LightGroupName = this.LightGroupName;   //260407 hbk — Reset 시 LightGroupName 복원 활성화`

주의: CameraSlaveParam.cs와 CameraParam.cs의 CopyTo에서 `//slaveParam.DeviceName = this.DeviceName;` 및 `//camParam.DeviceName = this.DeviceName;` 주석은 그대로 유지한다 (DeviceName은 CameraMasterParam에서만 관리).
  </action>
  <verify>
    <automated>MSBuild WPF_Example.sln /p:Configuration=Debug /v:minimal 2>&amp;1 | grep -E "error|Error|Build succeeded" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Sequence_Inspection.cs에 `_MyParam.LightGroupName = DefaultLight` 문자열이 존재하지 않는다 (grep -c 결과 0)
    - Sequence_Inspection.cs에 `_MyParam.DeviceName = DefaultCamera;` 문자열이 여전히 존재한다 (OnLoad 내)
    - CameraSlaveParam.cs CopyTo 내 `slaveParam.LightGroupName = this.LightGroupName;` 가 주석이 아닌 활성 코드로 존재한다 (grep으로 `//slaveParam.LightGroupName`이 아닌 `slaveParam.LightGroupName = this.LightGroupName` 확인)
    - CameraSlaveParam.cs CopyTo 내 `camParam.LightGroupName = this.LightGroupName;` 가 활성 코드로 존재한다
    - CameraParam.cs CopyTo 내 `camParam.LightGroupName = this.LightGroupName;` 가 활성 코드로 존재한다
    - MSBuild 0 errors
  </acceptance_criteria>
  <done>OnLoad에서 LightGroupName 강제 덮어쓰기가 제거되어 INI 로드 값이 유지되고, CopyTo에서 LightGroupName 복사가 활성화되어 Reset 시 복원됨. 빌드 성공.</done>
</task>

</tasks>

<verification>
1. MSBuild 빌드 성공 (0 errors, 0 warnings on modified files)
2. CameraSlaveParam.cs, CameraParam.cs에 FrameWidth/FrameHeight 프로퍼티 없음
3. Sequence_Inspection.OnLoad()에 LightGroupName 재설정 없음
4. CopyTo()에서 LightGroupName 복사 활성화됨
</verification>

<success_criteria>
- MSBuild 빌드 성공
- grep -r "FrameWidth\|FrameHeight" WPF_Example/Sequence/Param/ 결과 0건
- grep "LightGroupName = DefaultLight" Sequence_Inspection.cs는 OnLoad에 없고 OnCreate에만 존재
- grep "slaveParam.LightGroupName = this.LightGroupName" CameraSlaveParam.cs 결과 활성 코드로 존재
</success_criteria>

<output>
After completion, create `.planning/phases/14-framewidth-frameheight-lightgroupname/14-01-SUMMARY.md`
</output>
