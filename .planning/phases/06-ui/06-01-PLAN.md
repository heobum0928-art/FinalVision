---
phase: 06-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Sequence/Param/CameraSlaveParam.cs
  - WPF_Example/Sequence/Param/CameraParam.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "CameraSlaveParam/CameraParam에서 PixelToUM_Offset, MotorXPos, MotorYPos, PartNo 프로퍼티가 완전히 제거됨"
    - "CopyTo() 메서드에서 제거된 필드 참조가 없어 컴파일 오류 없음"
    - "ConvertPixelToMM() 메서드가 CameraSlaveParam에서 제거됨"
    - "LightGroupName, LightLevel, FrameWidth, FrameHeight는 유지됨"
  artifacts:
    - path: "WPF_Example/Sequence/Param/CameraSlaveParam.cs"
      provides: "레거시 필드 제거된 CameraSlaveParam"
      contains: "FrameWidth"
    - path: "WPF_Example/Sequence/Param/CameraParam.cs"
      provides: "레거시 필드 제거된 CameraParam"
      contains: "FrameWidth"
  key_links:
    - from: "CameraSlaveParam.CopyTo()"
      to: "CameraParam 필드"
      via: "대칭 필드 제거"
      pattern: "FrameWidth.*FrameHeight"
---

<objective>
CameraSlaveParam.cs 및 CameraParam.cs에서 ECi_Dispenser 잔재 레거시 필드를 제거한다 (D-01).

Purpose: RecipeEditorWindow PropertyGrid에 불필요한 필드가 표시되지 않도록 사전 정리. InspectionParam이 CameraSlaveParam을 상속하므로 이 정리가 PropertyGrid 표시에 직접 영향.
Output: 레거시 필드 없이 빌드 성공하는 CameraSlaveParam.cs, CameraParam.cs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraSlaveParam.cs 레거시 필드 및 메서드 제거</name>
  <files>WPF_Example/Sequence/Param/CameraSlaveParam.cs</files>
  <read_first>
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
    WPF_Example/Sequence/Param/CameraParam.cs
  </read_first>
  <action>
CameraSlaveParam.cs에서 다음을 제거한다 (per D-01). 모든 변경에 `//260327 hbk` 주석 추가.

**프로퍼티 제거 (Category "General|AOI" 블록, 약 line 22~29):**
- `public double PixelToUM_Offset { get; set; }` (line 23) — 제거
- `public double MotorXPos { get; set; }` (line 25) — 제거
- `public double MotorYPos { get; set; }` (line 26) — 제거
- `public int PartNo { get; set; }` (line 29) — 제거
- 유지: `public int FrameWidth { get; set; }` (line 27)
- 유지: `public int FrameHeight { get; set; }` (line 28)

**메서드 제거 (약 line 98~102):**
- `public virtual double ConvertPixelToMM(double pixel)` 메서드 전체 제거 — PixelToUM_Offset에 의존

**CopyTo() 메서드 내 제거 (약 line 180~228):**
- `else if (param is CameraSlaveParam)` 분기 (line 185~204):
  - `slaveParam.PartNo = this.PartNo;` (line 197) — 제거
  - `slaveParam.MotorXPos = this.MotorXPos;` (line 198) — 제거
  - `slaveParam.MotorYPos = this.MotorYPos;` (line 199) — 제거
  - `slaveParam.PixelToUM_Offset = this.PixelToUM_Offset;` (line 203) — 제거
  - 유지: `slaveParam.FrameWidth = this.FrameWidth;` (line 200)
  - 유지: `slaveParam.FrameHeight = this.FrameHeight;` (line 201)
- `else if (param is CameraParam)` 분기 (line 206~227):
  - `camParam.PartNo = this.PartNo;` (line 218) — 제거
  - `camParam.MotorXPos = this.MotorXPos;` (line 219) — 제거
  - `camParam.MotorYPos = this.MotorYPos;` (line 220) — 제거
  - `camParam.PixelToUM_Offset = this.PixelToUM_Offset;` (line 224) — 제거
  - 유지: `camParam.FrameWidth = this.FrameWidth;` (line 221)
  - 유지: `camParam.FrameHeight = this.FrameHeight;` (line 222)

주석 처리된 `//slaveParam.PixelToMM_Offset` 같은 구 주석 라인도 함께 정리.
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision &amp;&amp; grep -c "PixelToUM_Offset\|MotorXPos\|MotorYPos\|PartNo\|ConvertPixelToMM" WPF_Example/Sequence/Param/CameraSlaveParam.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PixelToUM_Offset" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns 0 matches
    - `grep "MotorXPos" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns 0 matches
    - `grep "MotorYPos" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns 0 matches
    - `grep "PartNo" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns 0 matches
    - `grep "ConvertPixelToMM" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns 0 matches
    - `grep "FrameWidth" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns matches (유지 확인)
    - `grep "FrameHeight" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns matches (유지 확인)
    - `grep "LightGroupName" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns matches (유지 확인)
    - `grep "260327 hbk" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns matches (주석 규칙)
  </acceptance_criteria>
  <done>CameraSlaveParam.cs에서 PixelToUM_Offset, MotorXPos, MotorYPos, PartNo 프로퍼티 + ConvertPixelToMM 메서드 + CopyTo 내 참조 모두 제거. FrameWidth/FrameHeight/LightGroupName/LightLevel 유지.</done>
</task>

<task type="auto">
  <name>Task 2: CameraParam.cs 레거시 필드 제거 + 빌드 확인</name>
  <files>WPF_Example/Sequence/Param/CameraParam.cs</files>
  <read_first>
    WPF_Example/Sequence/Param/CameraParam.cs
    WPF_Example/Sequence/Param/CameraSlaveParam.cs
  </read_first>
  <action>
CameraParam.cs에서 동일 레거시 필드를 제거한다 (per D-01). 모든 변경에 `//260327 hbk` 주석 추가.

**프로퍼티 제거 (Category "General|AOI" 블록, 약 line 73~81):**
- `public double PixelToUM_Offset { get; set; }` (line 75) — 제거
- `public double MotorXPos { get; set; }` (line 77) — 제거
- `public double MotorYPos { get; set; }` (line 78) — 제거
- `public int PartNo { get; set; }` (line 81) — 제거
- 유지: `public int FrameWidth { get; set; }` (line 79)
- 유지: `public int FrameHeight { get; set; }` (line 80)

**CopyTo() 메서드 내 제거 (약 line 228~278):**
- `else if (param is CameraSlaveParam)` 분기 (line 235~254):
  - `slaveParam.PartNo = this.PartNo;` (line 246) — 제거
  - `slaveParam.MotorXPos = this.MotorXPos;` (line 247) — 제거
  - `slaveParam.MotorYPos = this.MotorYPos;` (line 248) — 제거
  - `slaveParam.PixelToUM_Offset = this.PixelToUM_Offset;` (line 253) — 제거
  - 유지: `slaveParam.FrameWidth = this.FrameWidth;` (line 249)
  - 유지: `slaveParam.FrameHeight = this.FrameHeight;` (line 250)
- `else if (param is CameraParam)` 분기 (line 256~276):
  - `camParam.PartNo = this.PartNo;` (line 267) — 제거
  - `camParam.MotorXPos = this.MotorXPos;` (line 268) — 제거
  - `camParam.MotorYPos = this.MotorYPos;` (line 269) — 제거
  - `camParam.PixelToUM_Offset = this.PixelToUM_Offset;` (line 274) — 제거
  - 유지: `camParam.FrameWidth = this.FrameWidth;` (line 270)
  - 유지: `camParam.FrameHeight = this.FrameHeight;` (line 271)

주석 처리된 구 주석 라인 (`//slaveParam.PixelToMM_Offset` 등)도 함께 정리.

**빌드 확인:**
빌드 명령 실행: `msbuild FinalVision.sln /p:Configuration=Debug /t:Build /v:minimal`
오류 0건 확인. 만약 다른 파일에서 PixelToUM_Offset, MotorXPos, MotorYPos, PartNo, ConvertPixelToMM을 참조하면 해당 참조도 제거.
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision &amp;&amp; grep -c "PixelToUM_Offset\|MotorXPos\|MotorYPos\|PartNo" WPF_Example/Sequence/Param/CameraParam.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "PixelToUM_Offset" WPF_Example/Sequence/Param/CameraParam.cs` returns 0 matches
    - `grep "MotorXPos" WPF_Example/Sequence/Param/CameraParam.cs` returns 0 matches
    - `grep "MotorYPos" WPF_Example/Sequence/Param/CameraParam.cs` returns 0 matches
    - `grep "PartNo" WPF_Example/Sequence/Param/CameraParam.cs` returns 0 matches
    - `grep "FrameWidth" WPF_Example/Sequence/Param/CameraParam.cs` returns matches (유지 확인)
    - `grep "260327 hbk" WPF_Example/Sequence/Param/CameraParam.cs` returns matches (주석 규칙)
    - msbuild 빌드 성공 (오류 0건)
  </acceptance_criteria>
  <done>CameraParam.cs 레거시 필드 제거 완료. CameraSlaveParam/CameraParam 양쪽 CopyTo() 대칭 정리. 프로젝트 빌드 오류 0건.</done>
</task>

</tasks>

<verification>
- `grep -rn "PixelToUM_Offset\|MotorXPos\|MotorYPos\|PartNo\|ConvertPixelToMM" WPF_Example/Sequence/Param/` returns 0 matches
- `grep -rn "FrameWidth\|FrameHeight\|LightGroupName\|LightLevel" WPF_Example/Sequence/Param/CameraSlaveParam.cs` returns matches (유지 필드 존재)
- msbuild 빌드 성공
</verification>

<success_criteria>
CameraSlaveParam.cs와 CameraParam.cs에서 PixelToUM_Offset, MotorXPos, MotorYPos, PartNo 프로퍼티와 ConvertPixelToMM 메서드가 완전 제거되고, CopyTo() 내 해당 필드 대입 라인도 제거되어 빌드 오류 없이 성공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui/06-01-SUMMARY.md`
</output>
