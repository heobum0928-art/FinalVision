---
phase: 06-ui
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs
  - WPF_Example/UI/Recipe/OpenRecipeWindow.xaml
  - WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs
  - WPF_Example/MainWindow.xaml.cs
autonomous: false
requirements: []

must_haves:
  truths:
    - "OpenRecipeWindow에서 레시피 선택 후 편집 버튼 클릭으로 RecipeEditorWindow 팝업이 열림"
    - "RecipeEditorWindow에서 저장 버튼 클릭 시 현재 Site 레시피가 ini 파일에 저장됨"
    - "RecipeEditorWindow에서 초기화 버튼 클릭 시 BlobMinArea=100, BlobMaxArea=50000, DelayMs=0으로 리셋됨"
    - "RecipeEditorWindow에서 복사 버튼 클릭 시 새 레시피 디렉터리 생성됨"
    - "레시피 편집 후 저장 -> 레시피 재로드 시 값이 유지됨"
  artifacts:
    - path: "WPF_Example/UI/Recipe/OpenRecipeWindow.xaml"
      provides: "편집 버튼 추가된 OpenRecipeWindow"
      contains: "btn_Edit"
    - path: "WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs"
      provides: "편집 버튼 이벤트 핸들러"
      contains: "Btn_Edit_Click"
    - path: "WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs"
      provides: "Save/Load/Copy/Reset 완성된 코드비하인드"
      contains: "SaveRecipe"
  key_links:
    - from: "OpenRecipeWindow.Btn_Edit_Click"
      to: "RecipeEditorWindow"
      via: "new RecipeEditorWindow(mainView).ShowDialog()"
      pattern: "RecipeEditorWindow.*ShowDialog"
    - from: "RecipeEditorWindow.Button_save_Click"
      to: "SequenceHandler.SaveRecipe"
      via: "SystemHandler.Handle.Sequences.SaveRecipe(_siteNumber, _recipeName)"
      pattern: "SaveRecipe.*_siteNumber"
    - from: "MainWindow.PopupView(Recipe)"
      to: "OpenRecipeWindow"
      via: "mModalWindow = new OpenRecipeWindow()"
      pattern: "OpenRecipeWindow"
---

<objective>
OpenRecipeWindow에 "편집" 버튼 진입점을 추가하고, RecipeEditorWindow의 Save/Load/Copy/Reset 기능을 완성한다 (D-02, D-06).

Purpose: 레시피 편집 UI의 진입점과 저장/불러오기/복사/초기화 전 구간을 완성하여 실제 운영 가능한 레시피 편집 플로우 구축.
Output: OpenRecipeWindow에 편집 버튼 추가, RecipeEditorWindow 이벤트 핸들러 완성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-RESEARCH.md
@.planning/phases/06-ui/06-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs:
```csharp
public partial class OpenRecipeWindow : Window {
    private RecipeListViewModel Model;
    public string SelectedRecipeName { get; }
    // Btn_Ok_Click, Btn_Cancel_Click, Btn_Copy_Click, Btn_Delete_Click 등 기존 핸들러
}
```

From WPF_Example/MainWindow.xaml.cs (line 296~304):
```csharp
case EPageType.Recipe:
    mModalWindow = new OpenRecipeWindow();
    mModalWindow.Owner = this;
    if (mModalWindow.ShowDialog() == true) {
        string selectedName = (mModalWindow as OpenRecipeWindow).SelectedRecipeName;
        if (!mSystemHandler.LoadRecipe(selectedName)) { ... }
    }
    break;
```

From WPF_Example/MainWindow.xaml (line 70):
```xml
<ui:MainView x:Name="mainView" Grid.Column="0" Grid.Row="0"/>
```

From WPF_Example/Sequence/SequenceHandler.cs:
```csharp
public void SaveRecipe(int siteNumber, string recipeName) // Phase 4 오버로드
public void LoadRecipe(int siteNumber, string recipeName) // Phase 4 오버로드
```

From WPF_Example/Utility/RecipeFileHelper.cs:
```csharp
RecipeFiles.Handle.Copy(string srcName, string dstName)
RecipeFiles.Handle.HasRecipe(string name)
RecipeFiles.Handle.CollectRecipe(int siteNumber) // Phase 4 오버로드
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenRecipeWindow 편집 버튼 추가 + MainWindow mainView 전달</name>
  <files>WPF_Example/UI/Recipe/OpenRecipeWindow.xaml, WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs, WPF_Example/MainWindow.xaml.cs</files>
  <read_first>
    WPF_Example/UI/Recipe/OpenRecipeWindow.xaml
    WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs
    WPF_Example/MainWindow.xaml.cs
    WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs
  </read_first>
  <action>
3개 파일 수정 (per D-02). 모든 변경에 `//260327 hbk` 주석.

**1. OpenRecipeWindow.xaml — "편집" 버튼 추가:**

하단 DockPanel (Grid.Column="2" Grid.Row="2") 내 btn_Ok 왼쪽에 편집 버튼 추가:
```xml
<!--260327 hbk — 레시피 편집 버튼 추가-->
<Button x:Name="btn_Edit" Width="110" FontSize="18" Margin="0,0,2,0"
    IsEnabled="{Binding SelectedItems.Count, ElementName=listView_Recipe}"
    Click="Btn_Edit_Click" Content="Edit"/>
```

위치: btn_Cancel 앞에 삽입 (DockPanel 내 순서: btn_Edit, btn_Cancel, btn_Ok)

**2. OpenRecipeWindow.xaml.cs — 편집 이벤트 + MainView 참조:**

필드 추가:
```csharp
private MainView _mainView; //260327 hbk — RecipeEditorWindow에 전달
```

생성자 변경 — MainView 파라미터 추가:
```csharp
public OpenRecipeWindow(MainView mainView) { //260327 hbk
    // 기존 코드 유지
    SystemHandler.Handle.Recipes.CollectRecipe();
    InitializeComponent();
    Model = new RecipeListViewModel();
    this.DataContext = Model;
    _mainView = mainView; //260327 hbk
}
```

기존 파라미터 없는 생성자도 유지 (하위호환):
```csharp
public OpenRecipeWindow() : this(null) { } //260327 hbk — 하위호환
```

편집 버튼 이벤트 핸들러 추가:
```csharp
private void Btn_Edit_Click(object sender, RoutedEventArgs e) { //260327 hbk
    if (SelectedRecipeName == null) return; //260327 hbk
    if (_mainView == null) return; //260327 hbk

    // 선택된 레시피 먼저 로드 //260327 hbk
    string selectedName = SelectedRecipeName; //260327 hbk
    int siteNumber = SystemHandler.Handle.Setting.CurrentSiteIndex; //260327 hbk
    SystemHandler.Handle.Sequences.LoadRecipe(siteNumber, selectedName); //260327 hbk

    // RecipeEditorWindow 팝업 //260327 hbk
    var editor = new RecipeEditorWindow(_mainView); //260327 hbk
    editor.Owner = this; //260327 hbk
    editor.ShowDialog(); //260327 hbk
}
```

**3. MainWindow.xaml.cs — OpenRecipeWindow 생성 시 mainView 전달:**

`PopupView` 메서드 내 `EPageType.Recipe` case (약 line 296~304) 수정:
```csharp
case EPageType.Recipe:
    mModalWindow = new OpenRecipeWindow(mainView); //260327 hbk — mainView 전달
    mModalWindow.Owner = this;
    if (mModalWindow.ShowDialog() == true) {
        string selectedName = (mModalWindow as OpenRecipeWindow).SelectedRecipeName;
        if (!mSystemHandler.LoadRecipe(selectedName)) {
            CustomMessageBox.Show("Error", SystemHandler.Handle.Localize["fail to load recipe"], MessageBoxImage.Error);
        }
    }
    break;
```

빌드 확인.
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision &amp;&amp; grep -c "btn_Edit\|Btn_Edit_Click\|RecipeEditorWindow" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "btn_Edit" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml` returns match
    - `grep "Btn_Edit_Click" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs` returns match
    - `grep "RecipeEditorWindow" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs` returns match
    - `grep "new OpenRecipeWindow(mainView)" WPF_Example/MainWindow.xaml.cs` returns match
    - `grep "_mainView" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs` returns match
    - `grep "260327 hbk" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs` returns match
    - `grep "260327 hbk" WPF_Example/MainWindow.xaml.cs` returns match
  </acceptance_criteria>
  <done>OpenRecipeWindow에 "Edit" 버튼이 추가되고, 클릭 시 선택 레시피를 로드한 뒤 RecipeEditorWindow(mainView).ShowDialog() 팝업. MainWindow에서 mainView 참조 전달.</done>
</task>

<task type="auto">
  <name>Task 2: RecipeEditorWindow Save/Load/Copy/Reset 이벤트 핸들러 완성</name>
  <files>WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs</files>
  <read_first>
    WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs
    WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs
    WPF_Example/Utility/RecipeFileHelper.cs
    WPF_Example/Custom/Sequence/Inspection/Action_Inspection.cs
  </read_first>
  <action>
RecipeEditorWindow.xaml.cs의 빈 이벤트 핸들러를 구현한다 (per D-06). 모든 변경에 `//260327 hbk` 주석.

**Button_save_Click (저장):**
```csharp
private void Button_save_Click(object sender, RoutedEventArgs e) { //260327 hbk
    try {
        System.Windows.Input.Keyboard.ClearFocus(); //260327 hbk — 편집 중 값 커밋
        SystemHandler.Handle.Sequences.SaveRecipe(_siteNumber, _recipeName); //260327 hbk
        CustomMessageBox.Show("Saved", //260327 hbk
            string.Format("Recipe '{0}' saved for Site {1}.", _recipeName, _siteNumber),
            MessageBoxImage.Information);
    } catch (Exception ex) {
        CustomMessageBox.Show("Error", //260327 hbk
            string.Format("Save failed: {0}", ex.Message),
            MessageBoxImage.Error);
    }
}
```

**Button_load_Click (불러오기):**
```csharp
private void Button_load_Click(object sender, RoutedEventArgs e) { //260327 hbk
    // OpenRecipeWindow를 통해 다른 레시피 선택 //260327 hbk
    var openWin = new OpenRecipeWindow(); //260327 hbk — 목록만 선택용
    openWin.Owner = this; //260327 hbk
    if (openWin.ShowDialog() == true) { //260327 hbk
        string selectedName = openWin.SelectedRecipeName; //260327 hbk
        if (string.IsNullOrEmpty(selectedName)) return; //260327 hbk
        SystemHandler.Handle.Sequences.LoadRecipe(_siteNumber, selectedName); //260327 hbk
        _recipeName = selectedName; //260327 hbk
        textBlock_recipeName.Text = _recipeName; //260327 hbk

        // 파라미터 재참조 //260327 hbk
        SequenceBase seq = SystemHandler.Handle.Sequences[ESequence.Inspection]; //260327 hbk
        for (int i = 0; i < 5; i++) { //260327 hbk
            _params[i] = seq[i].Param as InspectionParam; //260327 hbk
        }
        LoadParamsForCurrentTab(); //260327 hbk
    }
}
```

**Button_copy_Click (복사):**
```csharp
private void Button_copy_Click(object sender, RoutedEventArgs e) { //260327 hbk
    bool dlgResult = TextInputBox.Show( //260327 hbk
        "Enter the name of new recipe to copy.", _recipeName, out string inputText);
    if (dlgResult == false) return; //260327 hbk
    string newName = inputText; //260327 hbk
    if (newName == _recipeName) { //260327 hbk
        CustomMessageBox.Show("Error", "Recipe name to be copied must be different.",
            MessageBoxImage.Error); //260327 hbk
        return;
    }
    if (RecipeFiles.Handle.HasRecipe(newName)) { //260327 hbk
        if (CustomMessageBox.ShowConfirmation(newName + " already exists.", //260327 hbk
            "Are you sure you want to overwrite?", MessageBoxButton.YesNo)
            != MessageBoxResult.Yes) {
            return; //260327 hbk
        }
    }
    try {
        // 현재 변경사항 먼저 저장 //260327 hbk
        System.Windows.Input.Keyboard.ClearFocus(); //260327 hbk
        SystemHandler.Handle.Sequences.SaveRecipe(_siteNumber, _recipeName); //260327 hbk
        if (RecipeFiles.Handle.Copy(_recipeName, newName) == false) { //260327 hbk
            CustomMessageBox.Show("Error", //260327 hbk
                string.Format("Copy failed: {0} to {1}", _recipeName, newName),
                MessageBoxImage.Error);
        } else {
            CustomMessageBox.Show("Copied", //260327 hbk
                string.Format("Recipe copied: {0} -> {1}", _recipeName, newName),
                MessageBoxImage.Information);
        }
    } catch (Exception ex) {
        CustomMessageBox.Show("Error", //260327 hbk
            string.Format("Copy failed: {0}", ex.Message),
            MessageBoxImage.Error);
    }
}
```

**Button_reset_Click (초기화) — per D-06:**
```csharp
private void Button_reset_Click(object sender, RoutedEventArgs e) { //260327 hbk
    if (CustomMessageBox.ShowConfirmation("Reset", //260327 hbk
        "Are you sure you want to reset all parameters to defaults?",
        MessageBoxButton.YesNo) != MessageBoxResult.Yes) {
        return; //260327 hbk
    }
    foreach (var p in _params) { //260327 hbk
        p.ROI = new System.Windows.Rect(0, 0, 0, 0); //260327 hbk — 저장값 0,0,0,0
        p.BlobMinArea = 100; //260327 hbk
        p.BlobMaxArea = 50000; //260327 hbk
        p.DelayMs = 0; //260327 hbk
    }
    SystemHandler.Handle.Sequences.SaveRecipe(_siteNumber, _recipeName); //260327 hbk
    LoadParamsForCurrentTab(); //260327 hbk — PropertyGrid + Canvas 갱신 (ROI 0→전체 이미지 자동 초기화)
    CustomMessageBox.Show("Reset", "All parameters reset to defaults.", //260327 hbk
        MessageBoxImage.Information);
}
```

빌드 확인: `msbuild FinalVision.sln /p:Configuration=Debug /t:Build /v:minimal` 오류 0건.
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision &amp;&amp; grep -c "SaveRecipe\|LoadRecipe\|RecipeFiles.Handle.Copy\|BlobMinArea = 100\|Button_save_Click\|Button_load_Click\|Button_copy_Click\|Button_reset_Click" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SaveRecipe.*_siteNumber.*_recipeName" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "LoadRecipe.*_siteNumber" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "RecipeFiles.Handle.Copy" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "BlobMinArea = 100" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "BlobMaxArea = 50000" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "DelayMs = 0" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match
    - `grep "TextInputBox.Show" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns match (복사 이름 입력)
    - `grep "Keyboard.ClearFocus" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs` returns 2+ matches (save + copy)
    - 모든 이벤트 핸들러가 비어있지 않음 (Button_save/load/copy/reset_Click)
    - msbuild 빌드 성공 (오류 0건)
  </acceptance_criteria>
  <done>RecipeEditorWindow에서 저장(SaveRecipe), 불러오기(OpenRecipeWindow 팝업→LoadRecipe), 복사(Copy), 초기화(기본값 리셋) 전 기능 구현. 빌드 오류 없음.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: RecipeEditorWindow 전체 플로우 검증</name>
  <files>WPF_Example/UI/Recipe/RecipeEditorWindow.xaml, WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs, WPF_Example/UI/Recipe/OpenRecipeWindow.xaml, WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs</files>
  <action>
사용자가 SIMUL_MODE 빌드된 앱을 실행하여 RecipeEditorWindow 전체 플로우를 수동 검증한다.
검증 항목: OpenRecipeWindow Edit 진입점, Shot 탭 전환, PropertyGrid 편집, ROI 드래그, Grab 미리보기, Save/Load/Copy/Reset, 레거시 필드 미표시.
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision &amp;&amp; grep -c "RecipeEditorWindow\|btn_Edit\|SaveRecipe\|LoadRecipe" WPF_Example/UI/Recipe/RecipeEditorWindow.xaml.cs WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs</automated>
  </verify>
  <what-built>
    Phase 6 전체: CameraSlaveParam/CameraParam 레거시 필드 제거 + RecipeEditorWindow 신규 생성 + OpenRecipeWindow 편집 진입점 + Save/Load/Copy/Reset/Grab 기능
  </what-built>
  <how-to-verify>
    1. 앱 실행 (SIMUL_MODE 빌드)
    2. 메뉴 또는 InspectionListView에서 "Recipe" 클릭 → OpenRecipeWindow 열림
    3. 레시피 하나 선택 → "Edit" 버튼 클릭 → RecipeEditorWindow 팝업 확인
    4. Shot1~Shot5 탭 전환 → PropertyGrid에 해당 Shot의 ROI/Blob/Delay/Camera 파라미터 표시 확인
    5. PropertyGrid에서 BlobMinArea 값을 200으로 변경 → "Save" 버튼 클릭 → 성공 메시지
    6. 앱 재실행 → 같은 레시피 편집 → BlobMinArea가 200으로 유지되는지 확인
    7. Canvas 영역에서 ROI 드래그 → PropertyGrid ROI 값 변경 확인
    8. "Grab" 버튼 클릭 → 이미지 표시 + 결과 라벨 갱신 확인
    9. "Reset" 클릭 → BlobMinArea=100, BlobMaxArea=50000으로 복원 확인
    10. "Copy" 클릭 → 새 이름 입력 → 복사 성공 메시지 확인
    11. PropertyGrid에 PixelToUM_Offset, MotorXPos, MotorYPos, PartNo가 표시되지 않음 확인 (D-01 레거시 제거)
  </how-to-verify>
  <done>사용자가 11개 검증 항목을 모두 확인하고 "approved" 응답.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- OpenRecipeWindow.xaml에 btn_Edit 버튼 존재
- OpenRecipeWindow.xaml.cs에 Btn_Edit_Click → RecipeEditorWindow.ShowDialog() 연결
- MainWindow.xaml.cs에서 OpenRecipeWindow(mainView) 생성
- RecipeEditorWindow.xaml.cs에 Save/Load/Copy/Reset 4개 이벤트 핸들러 모두 구현됨
- msbuild 빌드 성공
- 전체 플로우: OpenRecipeWindow → Edit → RecipeEditorWindow → Tab 전환 + PropertyGrid + Canvas + Grab + Save/Load/Copy/Reset
</verification>

<success_criteria>
OpenRecipeWindow "Edit" 버튼으로 RecipeEditorWindow 팝업이 열리고, 5개 Shot 탭에서 파라미터 편집 -> 저장 -> 재로드 시 값 유지, 복사/초기화/Grab 미리보기 전 기능이 동작한다.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui/06-03-SUMMARY.md`
</output>
