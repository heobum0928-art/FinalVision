---
phase: 16-alive-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements:
  - ALIVE-UI-02
  - ALIVE-UI-03
  - ALIVE-UI-06
must_haves:
  truths:
    - "SystemHandler가 ALIVE 응답 수신 시점에 AliveHeartbeatReceived 이벤트를 한 번 발행한다"
    - "SystemHandler가 PerformAliveTimeout 진입 시점에 AliveTimeout 이벤트를 한 번 발행한다"
    - "Phase 15 AliveProcess(315~361줄) 본문은 0줄 수정된다 (L-02)"
    - "_aliveResponseReceived 플래그 읽기/쓰기 로직은 변경되지 않는다"
  artifacts:
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "AliveHeartbeatReceived, AliveTimeout 두 신규 public event Action"
      contains: "public event Action AliveHeartbeatReceived"
  key_links:
    - from: "MainRun() VisionRequestType.Alive case"
      to: "AliveHeartbeatReceived?.Invoke()"
      via: "한 줄 추가"
      pattern: "AliveHeartbeatReceived\\?\\.Invoke\\(\\)"
    - from: "PerformAliveTimeout() 메서드 끝"
      to: "AliveTimeout?.Invoke()"
      via: "한 줄 추가"
      pattern: "AliveTimeout\\?\\.Invoke\\(\\)"
---

<objective>
Phase 16 ALIVE UI 인디케이터의 backend 브리징 — `WPF_Example/Custom/SystemHandler.cs` 에 두 개의 신규 public event(`AliveHeartbeatReceived`, `AliveTimeout`)를 추가하고, Phase 15 본문을 건드리지 않은 채 두 지점(MainRun ALIVE case, PerformAliveTimeout 끝)에서 각각 한 줄로 Invoke 한다. UI 측은 16-02 에서 이 이벤트를 구독한다.

Purpose: Phase 15 하트비트 로직(L-02) 을 보존하면서 UI 가 관찰할 수 있는 이산(discrete) 신호 두 개를 노출.
Output: SystemHandler.cs 수정 (필드 2개 추가 + 라인 2개 추가, 총 4 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-alive-ui/16-CONTEXT.md
@.planning/phases/16-alive-ui/16-RESEARCH.md
@WPF_Example/Custom/SystemHandler.cs

<interfaces>
SystemHandler 에서 16-02 가 구독할 신규 인터페이스:

```csharp
// 신규 추가
public event Action AliveHeartbeatReceived;  // ALIVE 응답 수신 시 1회 발행
public event Action AliveTimeout;            // 3회 재시도 모두 실패 후 1회 발행
```

기존(수정 금지):
- MainRun() 라인 74~77 `case VisionRequestType.Alive` — `_aliveResponseReceived = true` + `ProcessAlive` 호출 본체
- AliveProcess() 라인 315~361 — Phase 15 하트비트 송신 + 재시도 루프 (L-02)
- PerformAliveTimeout() 라인 369~379 — 알람/재연결 로직 본체
- _aliveResponseReceived, ALIVE_TIMEOUT_MS, ALIVE_RETRY_COUNT 상수 필드
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SystemHandler 신규 이벤트 필드 + Invoke 한 줄 2건 추가</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (라인 15~30 필드, 라인 52~91 MainRun switch, 라인 315~379 AliveProcess + PerformAliveTimeout)
    - .planning/phases/16-alive-ui/16-RESEARCH.md (Code Examples 섹션, 라인 270~278 정확한 수정 지점)
  </read_first>
  <action>
    `WPF_Example/Custom/SystemHandler.cs` 에 정확히 4줄 추가:

    1) **필드 선언 추가** — `private const int ALIVE_RETRY_COUNT` 선언 바로 다음 줄에 두 줄 삽입 (semantic anchor — 절대 라인번호 사용 금지):
       ```csharp
       public event Action AliveHeartbeatReceived;  //260413 hbk — Phase 16 UI flash 트리거 (Phase 15 로직 미수정)
       public event Action AliveTimeout;            //260413 hbk — Phase 16 UI 빨강 트리거 (Phase 15 로직 미수정)
       ```
       (`System` 네임스페이스의 `Action` 델리게이트 사용 — 파일 상단 using 확인 후 없으면 `using System;` 추가)

    2) **MainRun ALIVE case** — `case VisionRequestType.Alive:` 블록 안, `_aliveResponseReceived = true;` 라인 바로 다음 줄 (그리고 `responsePacket = ProcessAlive(...)` 이전 줄) 에 한 줄 삽입 (semantic anchor only):
       ```csharp
       AliveHeartbeatReceived?.Invoke();  //260413 hbk — Phase 16 ALIVE flash event
       ```
       기존 `_aliveResponseReceived = true;` 와 `responsePacket = ProcessAlive(packet.AsAlive());` 두 줄은 절대 건드리지 말 것 (L-02).

    3) **PerformAliveTimeout 메서드 끝** — `PerformAliveTimeout` 메서드 본문의 마지막 줄, 메서드 닫는 `}` 직전에 한 줄 삽입 (semantic anchor only — 절대 라인번호 사용 금지):
       ```csharp
       AliveTimeout?.Invoke();  //260413 hbk — Phase 16 UI 빨강 latch 트리거
       ```
       메서드 내부 기존 로직(`Logging.PrintLog`, `Server.GetConnectedClientCount()`, 알람/재연결 호출 등) 은 일체 변경 금지.

    **금지사항 (L-02):**
    - `AliveProcess()` 메서드(라인 315~361) 본문 어떤 줄도 추가/수정/삭제 금지
    - `_aliveResponseReceived = false` 재설정 라인(약 326줄) 절대 수정 금지
    - `ALIVE_TIMEOUT_MS`, `ALIVE_RETRY_COUNT`, `ALIVE_SEND_INTERVAL_MS` 상수값 변경 금지
    - 신규 NuGet 참조 추가 금지 (L-03)

    **주석 규약:** 모든 신규 라인 끝에 `//260413 hbk` 주석 필수.
  </action>
  <verify>
    <automated>cd /d/Project/FinalVision &amp;&amp; grep -n "AliveHeartbeatReceived" WPF_Example/Custom/SystemHandler.cs &amp;&amp; grep -n "AliveTimeout" WPF_Example/Custom/SystemHandler.cs &amp;&amp; grep -c "AliveHeartbeatReceived?.Invoke" WPF_Example/Custom/SystemHandler.cs &amp;&amp; grep -c "AliveTimeout?.Invoke" WPF_Example/Custom/SystemHandler.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep "public event Action AliveHeartbeatReceived" WPF_Example/Custom/SystemHandler.cs` → 1건 매치
    - `grep "public event Action AliveTimeout" WPF_Example/Custom/SystemHandler.cs` → 1건 매치
    - `grep "AliveHeartbeatReceived?.Invoke()" WPF_Example/Custom/SystemHandler.cs` → 정확히 1건
    - `grep "AliveTimeout?.Invoke()" WPF_Example/Custom/SystemHandler.cs` → 정확히 1건
    - 모든 신규 라인 4건 끝에 `//260413 hbk` 주석 존재 (`grep -c "//260413 hbk" WPF_Example/Custom/SystemHandler.cs` 가 기존 대비 +4)
    - `git diff WPF_Example/Custom/SystemHandler.cs` 가 +4 / -0 (정확히 4줄 추가, 0줄 삭제) — 추가 라인 외 변경 없음
    - `AliveProcess` 메서드 본문(315~361줄) 의 diff 0줄: `git diff -U0 WPF_Example/Custom/SystemHandler.cs | grep -E "^\\+" | grep -v "AliveHeartbeatReceived\\|AliveTimeout\\|public event"` → 빈 출력
    - C# 컴파일 에러 0건 (`dotnet build WPF_Example/FinalVision.csproj` 또는 VS 빌드 그린)
  </acceptance_criteria>
  <done>
    SystemHandler.cs 가 두 개의 신규 public event 를 노출하고, Phase 15 로직은 0줄 수정된 채로 ALIVE 수신/타임아웃 시점에 각각 한 번씩 Invoke 한다. 빌드 그린.
  </done>
</task>

</tasks>

<verification>
- `git diff --stat WPF_Example/Custom/SystemHandler.cs` 가 4 insertions / 0 deletions
- `grep -n "AliveHeartbeatReceived" WPF_Example/Custom/SystemHandler.cs` 가 2건 (선언 + Invoke)
- `grep -n "AliveTimeout" WPF_Example/Custom/SystemHandler.cs` 가 2건 (선언 + Invoke)
- L-02 회귀 방지: `git diff WPF_Example/Custom/SystemHandler.cs` 의 hunk 가 라인 24 근처(필드), 75 근처(case Alive), 379 근처(PerformAliveTimeout) 3개만 존재 — `AliveProcess`(315~361) 영역 hunk 0개
- L-03 회귀 방지: `git diff WPF_Example/FinalVision.csproj` → 변경 없음 (NuGet 0건 추가)
- 빌드: `dotnet build WPF_Example/FinalVision.csproj` 그린 (또는 VS 빌드 성공)
</verification>

<success_criteria>
- SystemHandler.cs 에 `AliveHeartbeatReceived`, `AliveTimeout` 두 이벤트가 정의되어 있음
- 두 이벤트가 정확한 위치(MainRun Alive case, PerformAliveTimeout 끝)에서 1회씩 Invoke 됨
- Phase 15 본문 0줄 수정 (L-02)
- 신규 NuGet 0건 (L-03)
- 빌드 그린
</success_criteria>

<output>
After completion, create `.planning/phases/16-alive-ui/16-01-SUMMARY.md`
</output>
