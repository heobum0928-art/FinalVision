---
phase: 16-alive-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - WPF_Example/UI/MenuBar.xaml
  - WPF_Example/UI/MenuBar.xaml.cs
autonomous: false
requirements:
  - ALIVE-UI-01
  - ALIVE-UI-02
  - ALIVE-UI-03
  - ALIVE-UI-04
  - ALIVE-UI-05
must_haves:
  truths:
    - "MenuBar 우측 Data & Version Grid 에 원형 LED + 'ALIVE' 텍스트가 보인다"
    - "Client 미연결 상태에서 LED 가 Gray (#FF9E9E9E) 로 표시된다"
    - "Client 연결 상태에서 LED Base 가 녹색 (#FF7EE08B) 로 표시된다"
    - "ALIVE 응답 수신 시마다 LED 가 진한녹색(#FF00B050)→Base녹색 으로 150ms fade flash 1회 수행한다"
    - "PerformAliveTimeout 진입 시 LED 가 빨강 (#FFE53935) 로 래치되어 flash 에 덮이지 않는다"
    - "Client 재접속(OnConnected AlarmEventType) 수신 시 즉시 녹색 Base 로 복귀한다"
    - "모든 UI 업데이트는 Application.Current.Dispatcher/UserControl.Dispatcher.BeginInvoke 로 수행된다 (L-01)"
  artifacts:
    - path: "WPF_Example/UI/MenuBar.xaml"
      provides: "x:Name='alive_Ellipse' + 'ALIVE' TextBlock + AliveFlashStoryboard 리소스"
      contains: "alive_Ellipse"
    - path: "WPF_Example/UI/MenuBar.xaml.cs"
      provides: "OnAliveHeartbeat/OnAliveTimeoutEvent/OnServerAlarm 핸들러 + UpdateState() 3-state 폴링 확장"
      contains: "AliveHeartbeatReceived"
  key_links:
    - from: "MenuBar.xaml UserControl.Resources"
      to: "AliveFlashStoryboard (ColorAnimation #FF00B050→#FF7EE08B 0.15s)"
      via: "x:Key 참조"
      pattern: "AliveFlashStoryboard"
    - from: "MenuBar_Loaded"
      to: "SystemHandler.Handle.AliveHeartbeatReceived/AliveTimeout/Server.OnAlarm"
      via: "이벤트 구독"
      pattern: "AliveHeartbeatReceived \\+="
    - from: "OnAliveHeartbeat / OnAliveTimeoutEvent / OnServerAlarm"
      to: "Dispatcher.BeginInvoke"
      via: "UI 스레드 포워딩 (L-01)"
      pattern: "Dispatcher\\.BeginInvoke"
    - from: "UpdateState() (MainWindow 100ms DispatcherTimer 폴링)"
      to: "_aliveBrush.Color (Gray/BaseGreen) / _aliveTimeoutLatched 분기"
      via: "3-state 폴링"
      pattern: "_aliveTimeoutLatched"
---

<objective>
Phase 16 ALIVE 인디케이터의 frontend — `MenuBar.xaml` Data & Version Grid 에 Row 4 신규 추가 후 Ellipse(14px) + "ALIVE" TextBlock 배치, `UserControl.Resources` 에 `AliveFlashStoryboard` (ColorAnimation 150ms) 정의. `MenuBar.xaml.cs` 에서 16-01 이 발행한 두 이벤트와 `Server.OnAlarm` 을 구독, `UpdateState()` 폴링에 3-state(회색/녹색Base/빨강) 로직을 얹어 완성한다. 모든 UI 터치는 `Dispatcher.BeginInvoke` 경유 (L-01).

Purpose: Phase 15 ALIVE 하트비트의 연결 상태를 운영자가 한눈에 파악할 수 있는 시각 피드백 제공.
Output: MenuBar.xaml (XAML Row 4 + Storyboard 리소스), MenuBar.xaml.cs (필드 3 + 메서드 3 + Loaded/UpdateState/Unloaded 확장).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-alive-ui/16-CONTEXT.md
@.planning/phases/16-alive-ui/16-RESEARCH.md
@WPF_Example/UI/MenuBar.xaml
@WPF_Example/UI/MenuBar.xaml.cs
@WPF_Example/Custom/SystemHandler.cs
@WPF_Example/TcpServer/VisionServer.cs

<interfaces>
16-01 이 이미 노출 (이 plan 실행 시점에 존재):
```csharp
// WPF_Example/Custom/SystemHandler.cs
public event Action AliveHeartbeatReceived;  // ALIVE 응답 수신
public event Action AliveTimeout;            // 3회 재시도 실패 타임아웃
```

기존 인터페이스 (수정 금지):
```csharp
// SystemHandler (singleton)
public static SystemHandler Handle { get; }
public VisionServer Server { get; }  // nullable until Initialize()
public SequenceHandler Sequences { get; }

// VisionServer / TcpServer
public bool IsConnected();  // 연결 client 1개 이상이면 true
public event EventHandler<AlarmEventArgs> OnAlarm;
// AlarmEventArgs.AlarmType: enum AlarmEventType { OnConnected, OnDisconnected, OnAcceptFail, OnSendFail, OnRecvMessageParsingFail, ... }

// MenuBar 기존
public void UpdateState();  // MainWindow DispatcherTimer 100ms 에서 호출
```

색상 (확정):
- Gray (미연결): `#FF9E9E9E`
- BaseGreen (연결 유지/flash 끝 상태): `#FF7EE08B`
- FlashGreen (수신 순간): `#FF00B050`
- Red (타임아웃 래치): `#FFE53935`
- Flash duration: `0:0:0.15` (150ms)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MenuBar.xaml — Row 4 ALIVE 인디케이터 + AliveFlashStoryboard 리소스 추가</name>
  <files>WPF_Example/UI/MenuBar.xaml</files>
  <read_first>
    - WPF_Example/UI/MenuBar.xaml 전체 (특히 UserControl.Resources 섹션과 `Data & Version` Grid.Column="4" 블록, 대략 라인 229~245)
    - .planning/phases/16-alive-ui/16-RESEARCH.md (라인 200~329 XAML 조각 섹션)
  </read_first>
  <action>
    `WPF_Example/UI/MenuBar.xaml` 에 다음 두 가지 수정:

    **1) UserControl.Resources 에 Storyboard 리소스 추가** (기존 Resources 블록 안에 삽입, 없으면 `<UserControl.Resources>` 블록 신규 생성):
    ```xml
    <Storyboard x:Key="AliveFlashStoryboard"><!--260413 hbk-->
        <ColorAnimation Storyboard.TargetName="alive_Ellipse"
                        Storyboard.TargetProperty="(Shape.Fill).(SolidColorBrush.Color)"
                        From="#FF00B050" To="#FF7EE08B" Duration="0:0:0.15"/>
    </Storyboard>
    ```
    - `From` = FlashGreen `#FF00B050`, `To` = BaseGreen `#FF7EE08B`, `Duration` = 150ms
    - `FillBehavior` 기본(HoldEnd) 사용 — 폴링이 다음 Tick 에 덮어쓰므로 OK
    - `AutoReverse` 지정 금지 (기본 false)

    **2) `Data & Version` Grid (Grid.Column="4") 에 Row 4 신규 추가** — 라인 234~239 근처 `Grid.RowDefinitions` 에 한 줄 `<RowDefinition Height="2*"/><!--260413 hbk-->` 를 기존 네 개 Row 뒤에 추가. 그리고 기존 4개 자식 요소(label_DateTime, 구분선 Label, label_Version, label_DLLVersion) 뒤에 신규 StackPanel 추가:
    ```xml
    <!--260413 hbk — Phase 16 ALIVE 인디케이터-->
    <StackPanel Grid.Row="4" Orientation="Horizontal"
                HorizontalAlignment="Right" VerticalAlignment="Center"
                Margin="0,0,6,2"><!--260413 hbk-->
        <Ellipse x:Name="alive_Ellipse" Width="14" Height="14"
                 Stroke="Gray" StrokeThickness="1"><!--260413 hbk-->
            <Ellipse.Fill>
                <SolidColorBrush Color="#FF9E9E9E"/><!--260413 hbk Gray Base-->
            </Ellipse.Fill>
        </Ellipse>
        <TextBlock Text="ALIVE" FontSize="12" Margin="4,0,0,0"
                   VerticalAlignment="Center"/><!--260413 hbk-->
    </StackPanel>
    ```

    **금지사항:**
    - 기존 4개 Row 의 Height / 기존 자식 4개(label_DateTime, 구분선, label_Version, label_DLLVersion) 어떤 속성도 변경 금지
    - 다른 Grid.Column (0~3) 은 건드리지 않음
    - XAML 주석에 `--` 연속 금지 (MC3000) — 위 예제의 `//` 는 모두 `<!-- -->` 형식 유지, 내부에 `--` 포함하지 않도록 유의 (`260413 hbk` 만 사용)
    - 신규 NuGet / 리소스 파일(이미지 등) 참조 금지 (L-03)
  </action>
  <verify>
    <automated>cd /d/Project/FinalVision &amp;&amp; grep -n "alive_Ellipse" WPF_Example/UI/MenuBar.xaml &amp;&amp; grep -n "AliveFlashStoryboard" WPF_Example/UI/MenuBar.xaml &amp;&amp; grep -n "#FF9E9E9E" WPF_Example/UI/MenuBar.xaml &amp;&amp; grep -n "Text=\"ALIVE\"" WPF_Example/UI/MenuBar.xaml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'x:Name="alive_Ellipse"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c 'x:Key="AliveFlashStoryboard"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c 'From="#FF00B050"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c 'To="#FF7EE08B"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c 'Duration="0:0:0.15"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c '#FF9E9E9E' WPF_Example/UI/MenuBar.xaml` → 1 (Ellipse 초기 Gray Fill)
    - `grep -c 'Text="ALIVE"' WPF_Example/UI/MenuBar.xaml` → 1
    - `grep -c 'Grid.Row="4"' WPF_Example/UI/MenuBar.xaml` → 최소 1 (신규 StackPanel)
    - 모든 신규 라인(추가된 StackPanel/Ellipse/TextBlock/Storyboard 블록) 에 `260413 hbk` 주석 최소 1개 이상 존재
    - XAMLParseException 없이 빌드 그린 (`dotnet build WPF_Example/FinalVision.csproj`)
    - 기존 `label_DateTime`, `label_Version`, `label_DLLVersion` x:Name 3개는 grep 결과 그대로 유지 (삭제/개명 없음)
  </acceptance_criteria>
  <done>
    MenuBar 우측 Data & Version Grid 에 원형 LED(14px, Gray) + "ALIVE" TextBlock 이 Row 4 에 배치되고, UserControl.Resources 에 AliveFlashStoryboard 가 정의되어 XAML 빌드가 통과한다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MenuBar.xaml.cs — 이벤트 구독 + 3-state 폴링 + Dispatcher 기반 flash/래치 로직</name>
  <files>WPF_Example/UI/MenuBar.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/MenuBar.xaml.cs (라인 1~80 현재 구조 — using, 필드, MenuBar_Loaded, UpdateState)
    - WPF_Example/Custom/SystemHandler.cs (16-01 에서 추가된 AliveHeartbeatReceived/AliveTimeout 이벤트 확인)
    - WPF_Example/TcpServer/VisionServer.cs (OnAlarm 이벤트 시그니처 + AlarmEventArgs.AlarmType enum 이름 확인)
    - .planning/phases/16-alive-ui/16-RESEARCH.md (라인 143~196 Implementation Sketch, 라인 246~267 Pitfalls)
  </read_first>
  <action>
    `WPF_Example/UI/MenuBar.xaml.cs` 에 다음 확장:

    **1) 필드 추가** — 기존 `MainWindow mParentWindow;` (라인 23) 근처에 세 필드 추가:
    ```csharp
    private SolidColorBrush _aliveBrush;              //260413 hbk — alive_Ellipse Fill SolidColorBrush 캐시
    private Storyboard _flashStoryboard;              //260413 hbk — AliveFlashStoryboard 리소스 캐시
    private volatile bool _aliveTimeoutLatched;       //260413 hbk — 빨강 래치 (OnConnected 수신 시 clear)
    private static readonly Color AliveGray       = Color.FromArgb(0xFF, 0x9E, 0x9E, 0x9E);  //260413 hbk
    private static readonly Color AliveBaseGreen  = Color.FromArgb(0xFF, 0x7E, 0xE0, 0x8B);  //260413 hbk
    private static readonly Color AliveRed        = Color.FromArgb(0xFF, 0xE5, 0x39, 0x35);  //260413 hbk
    ```

    **2) `MenuBar_Loaded` 확장** — 라인 48~51 기존 본문 아래에 추가:
    ```csharp
    //260413 hbk — Phase 16 ALIVE 인디케이터 초기화
    _aliveBrush = (SolidColorBrush)alive_Ellipse.Fill;
    _flashStoryboard = (Storyboard)this.Resources["AliveFlashStoryboard"];

    SystemHandler.Handle.AliveHeartbeatReceived += OnAliveHeartbeat;  //260413 hbk
    SystemHandler.Handle.AliveTimeout += OnAliveTimeoutEvent;          //260413 hbk
    if (SystemHandler.Handle.Server != null) {                          //260413 hbk — NRE 방어 (Pitfall #4)
        SystemHandler.Handle.Server.OnAlarm += OnServerAlarm;           //260413 hbk
    }
    this.Unloaded += MenuBar_Unloaded;                                  //260413 hbk — 구독 해제 훅
    ```

    **3) `UpdateState()` 확장** — 기존 3줄(DateTime/status/seqName) 아래에 3-state 폴링 추가:
    ```csharp
    //260413 hbk — Phase 16 ALIVE 3-state 폴링
    if (_aliveBrush == null) return;           //260413 hbk — Loaded 이전 가드
    if (_aliveTimeoutLatched) {                //260413 hbk — 빨강 래치 유지
        _aliveBrush.Color = AliveRed;          //260413 hbk
        return;                                //260413 hbk
    }
    bool connected = SystemHandler.Handle.Server?.IsConnected() ?? false;  //260413 hbk
    _aliveBrush.Color = connected ? AliveBaseGreen : AliveGray;            //260413 hbk
    ```

    **4) 신규 메서드 3개 + Unloaded 핸들러** (클래스 내 임의 위치):
    ```csharp
    //260413 hbk — Phase 16 ALIVE 응답 수신 → flash 1회
    private void OnAliveHeartbeat() {
        Dispatcher.BeginInvoke(new Action(() => {
            if (_aliveTimeoutLatched) return;  //260413 hbk — 빨강 중에는 flash 금지 (Pitfall #2)
            if (_flashStoryboard == null) return;
            _flashStoryboard.Begin(this, true);  //260413 hbk — isControllable=true, 재진입 안전
        }));
    }

    //260413 hbk — Phase 16 ALIVE 타임아웃 → 빨강 래치
    private void OnAliveTimeoutEvent() {
        Dispatcher.BeginInvoke(new Action(() => {
            _aliveTimeoutLatched = true;                     //260413 hbk
            if (_flashStoryboard != null) _flashStoryboard.Stop(this);  //260413 hbk
            if (_aliveBrush != null) _aliveBrush.Color = AliveRed;      //260413 hbk
        }));
    }

    //260413 hbk — Phase 16 Client 재접속 감지 → 빨강 래치 해제
    private void OnServerAlarm(object sender, AlarmEventArgs e) {
        if (e.AlarmType != AlarmEventArgs.AlarmEventType.OnConnected) return;  //260413 hbk
        Dispatcher.BeginInvoke(new Action(() => {
            _aliveTimeoutLatched = false;  //260413 hbk — 다음 UpdateState() 폴링이 녹색 Base 복귀
        }));
    }

    //260413 hbk — Phase 16 Unload 시 이벤트 구독 해제 (메모리 누수 방지)
    private void MenuBar_Unloaded(object sender, RoutedEventArgs e) {
        SystemHandler.Handle.AliveHeartbeatReceived -= OnAliveHeartbeat;  //260413 hbk
        SystemHandler.Handle.AliveTimeout -= OnAliveTimeoutEvent;          //260413 hbk
        if (SystemHandler.Handle.Server != null) {
            SystemHandler.Handle.Server.OnAlarm -= OnServerAlarm;          //260413 hbk
        }
    }
    ```

    **5) using 보강** — 파일 상단에 필요 시 추가 (기존 미존재 시에만):
    - `using System;` (Action 델리게이트 — 대부분 이미 있음)
    - `using System.Windows.Media;` (SolidColorBrush, Color — 이미 있음)
    - `using System.Windows.Media.Animation;` (Storyboard — 없으면 추가)
    - `AlarmEventArgs` 네임스페이스는 기존 `MenuBar.xaml.cs` 가 `SystemHandler.Handle.Server` 를 접근할 때 사용하는 패턴과 동일하게 사용 (신규 using 불필요 — SystemHandler 경유 접근).

    **AlarmEventArgs 타입 (codebase 에서 확정 — 재조사 금지):**
    - 이벤트 시그니처: `public event AlarmEventHandler OnAlarm;` where `public delegate void AlarmEventHandler(object sender, AlarmEventArgs e);` (TcpServer.cs:57, 333)
    - 핸들러 시그니처: `private void OnServerAlarm(object sender, AlarmEventArgs e)`
    - 프로퍼티명: `e.AlarmType` (TcpServer.cs:43, `public AlarmEventArgs.AlarmEventType AlarmType { get; }`)
    - enum 은 `AlarmEventArgs` 중첩 타입: `AlarmEventArgs.AlarmEventType.OnConnected` / `.OnDisconnected` (TcpServer.cs:31)
    - 구독: `SystemHandler.Handle.Server.OnAlarm += OnServerAlarm;`
    - 해제 (MenuBar_Unloaded): `SystemHandler.Handle.Server.OnAlarm -= OnServerAlarm;`
    - 분기 조건: `if (e.AlarmType == AlarmEventArgs.AlarmEventType.OnConnected) { ... }` — `_aliveTimeoutLatched = false` 로 clear, Dispatcher.BeginInvoke 로 폴링이 다음 Tick 에 Ellipse.Fill 을 BaseGreen 으로 되돌리도록 함.

    **금지사항 (잠금):**
    - L-01: 어떤 UI 프로퍼티 쓰기도 `Dispatcher.BeginInvoke` 밖에서 수행 금지. `Dispatcher.Invoke`(동기) 사용 금지 (Phase 15 1초 주기 지연 위험).
    - L-02: `SystemHandler.Handle` 의 `_aliveResponseReceived`, `ALIVE_TIMEOUT_MS`, `AliveProcess`, `PerformAliveTimeout` 등 Phase 15 내부를 읽거나 쓰는 코드 금지. 오직 `AliveHeartbeatReceived`, `AliveTimeout`, `Server.OnAlarm`, `Server.IsConnected()` 4개 public 멤버만 사용.
    - L-03: 신규 NuGet / 외부 리소스 추가 금지.
    - 기존 `UpdateState()` 의 3줄(DateTime/status/seqName) 및 `UpdateLoginID`, `IsEditable`, `ButtonSetting_Click` 등 기존 멤버 로직은 일체 수정 금지.
  </action>
  <verify>
    <automated>cd /d/Project/FinalVision &amp;&amp; grep -n "AliveHeartbeatReceived" WPF_Example/UI/MenuBar.xaml.cs &amp;&amp; grep -n "_aliveTimeoutLatched" WPF_Example/UI/MenuBar.xaml.cs &amp;&amp; grep -c "Dispatcher.BeginInvoke" WPF_Example/UI/MenuBar.xaml.cs &amp;&amp; grep -n "OnServerAlarm" WPF_Example/UI/MenuBar.xaml.cs &amp;&amp; grep -n "_flashStoryboard.Begin" WPF_Example/UI/MenuBar.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AliveHeartbeatReceived += OnAliveHeartbeat" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "AliveHeartbeatReceived -= OnAliveHeartbeat" WPF_Example/UI/MenuBar.xaml.cs` → 1 (Unloaded 해제)
    - `grep -c "AliveTimeout += OnAliveTimeoutEvent" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "AliveTimeout -= OnAliveTimeoutEvent" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "Server.OnAlarm += OnServerAlarm" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "_flashStoryboard.Begin(this, true)" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "_flashStoryboard.Stop(this)" WPF_Example/UI/MenuBar.xaml.cs` → 1
    - `grep -c "Dispatcher.BeginInvoke" WPF_Example/UI/MenuBar.xaml.cs` → 최소 3 (heartbeat, timeout, server alarm)
    - `grep -c "Dispatcher.Invoke(" WPF_Example/UI/MenuBar.xaml.cs` 는 기존값 초과하지 않음 (신규 동기 Invoke 도입 금지, L-01)
    - `grep -c "_aliveTimeoutLatched = true" WPF_Example/UI/MenuBar.xaml.cs` → 1 (OnAliveTimeoutEvent 내부)
    - `grep -c "_aliveTimeoutLatched = false" WPF_Example/UI/MenuBar.xaml.cs` → 1 (OnServerAlarm OnConnected 분기 내부)
    - `grep -c "IsConnected()" WPF_Example/UI/MenuBar.xaml.cs` → 1 (UpdateState 폴링)
    - `grep -c "0x9E, 0x9E, 0x9E" WPF_Example/UI/MenuBar.xaml.cs` → 1 (AliveGray)
    - `grep -c "0x7E, 0xE0, 0x8B" WPF_Example/UI/MenuBar.xaml.cs` → 1 (AliveBaseGreen)
    - `grep -c "0xE5, 0x39, 0x35" WPF_Example/UI/MenuBar.xaml.cs` → 1 (AliveRed)
    - L-02 회귀 방지: `grep "_aliveResponseReceived\\|ALIVE_TIMEOUT_MS\\|ALIVE_RETRY_COUNT\\|AliveProcess\\|PerformAliveTimeout" WPF_Example/UI/MenuBar.xaml.cs` → 빈 출력
    - L-03 회귀 방지: `git diff WPF_Example/FinalVision.csproj` → 변경 없음
    - 모든 신규 라인 끝에 `//260413 hbk` 주석 (최소 1회 이상 해당 메서드/블록에 존재)
    - `dotnet build WPF_Example/FinalVision.csproj` 그린 (0 errors, 0 new warnings)
  </acceptance_criteria>
  <done>
    MenuBar.xaml.cs 가 16-01 의 두 이벤트와 VisionServer.OnAlarm 을 구독하여 3-state 인디케이터를 구동한다. 모든 UI 터치는 Dispatcher.BeginInvoke 경유이며, Phase 15 내부 상태를 직접 참조하지 않는다. 빌드 그린.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실기 UAT — 3-state 전환 + 재접속 복귀 육안 검증</name>
  <what-built>
    Phase 16 ALIVE UI 인디케이터 전체: SystemHandler 이벤트 2건(16-01) + MenuBar XAML Row 4 Ellipse/TextBlock + code-behind 3-state 폴링 + 이벤트 기반 flash/래치(16-02).
  </what-built>
  <how-to-verify>
    1. Visual Studio 로 FinalVision 솔루션 빌드 및 실행 (Start Debugging F5).
    2. **[ALIVE-UI-01 미연결 = 회색]** Client 미접속 상태에서 MenuBar 우측 상단 Data & Version 영역 아래의 원형 LED 가 **Gray (#FF9E9E9E)** 로 표시되는지 확인. 옆에 "ALIVE" 텍스트 라벨이 보이는지 확인.
    3. **[ALIVE-UI-02 연결 + flash]** Client 시뮬레이터 또는 터미널 테스트 도구로 포트 7701 접속. 접속 즉시 LED 가 **녹색 Base (#FF7EE08B)** 로 변해야 함. 이후 Phase 15 의 1초 주기 `$ALIVE:1@` 왕복에 맞춰 **약 1초마다 진한녹색(#FF00B050)→Base녹색 으로 150ms 짧은 fade flash** 가 반복되는지 육안 확인. (15초 관찰 권장)
    4. **[ALIVE-UI-03 타임아웃 = 빨강]** Client 를 강제 종료(TCP 연결 비정상 단절). 약 9초(3회×3초) 후 LED 가 **Red (#FFE53935)** 로 고정되는지 확인. 이후 flash 가 더 이상 뜨지 않아야 함(빨강 래치 유지).
    5. **[ALIVE-UI-04 재접속 복귀]** Client 를 다시 포트 7701 에 접속. LED 가 **즉시(100ms 폴링 Tick 내) 녹색 Base 로 복귀**하고 flash 가 다시 시작되는지 확인.
    6. **[ALIVE-UI-05 스레드 안전성]** 위 4가지 시나리오 실행 중 VS Output 창에 `InvalidOperationException` / `The calling thread cannot access this object` 에러 **0건** 확인.
    7. **[ALIVE-UI-06 / L-02 회귀]** `git diff --stat master...HEAD WPF_Example/Custom/SystemHandler.cs` 가 `4 insertions, 0 deletions` 이고, `git diff master...HEAD WPF_Example/Custom/SystemHandler.cs` 출력의 모든 `+` 라인이 (a) 필드 선언 2줄 또는 (b) `AliveHeartbeatReceived?.Invoke()` 또는 (c) `AliveTimeout?.Invoke()` 뿐인지 확인 (AliveProcess 본체 수정 0줄).
    8. **[L-03 회귀]** `git diff master...HEAD WPF_Example/FinalVision.csproj` 및 `packages.config` 변경 0건 확인 (신규 NuGet 0건).
    9. **[L-01 회귀]** `grep -n "Dispatcher.Invoke(" WPF_Example/UI/MenuBar.xaml.cs` 결과에 신규 추가된 동기 Invoke 호출 없음 확인 (기존 `UpdateLoginID` 의 Invoke 는 유지 OK).

    예상 결과 표:
    | 단계 | 기대 LED 상태 |
    |------|---------------|
    | 2 | Gray |
    | 3 | BaseGreen + 1초 주기 FlashGreen flash |
    | 4 | Red (9초 후) |
    | 5 | BaseGreen (즉시) + flash 재개 |
  </how-to-verify>
  <acceptance_criteria>
    - 단계 2~5 LED 상태 전환 육안 확인 통과
    - VS Output 창에 Dispatcher/스레드 관련 예외 0건
    - `git diff --stat WPF_Example/Custom/SystemHandler.cs` = 4 insertions / 0 deletions
    - `git diff WPF_Example/FinalVision.csproj` 변경 없음
    - Phase 15 회귀 없음: 실기 구동 후 Client 연결 유지 1분+ 관측 시 기존 TCP 통신/$TEST 응답/택타임 로그 정상
    - WORKLOG.md 에 오늘(260413) Phase 16 작업 기록 추가됨
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- MenuBar.xaml: `alive_Ellipse`, `AliveFlashStoryboard`, `#FF9E9E9E`, `Text="ALIVE"` 4건 모두 grep 매치
- MenuBar.xaml.cs: 이벤트 구독/해제 대칭 + `Dispatcher.BeginInvoke` 3건 이상 + `_aliveTimeoutLatched` 플립 2건(true/false)
- L-01 grep-verifiable: `grep "Dispatcher.Invoke(" WPF_Example/UI/MenuBar.xaml.cs` 결과가 기존 `UpdateLoginID` 1건만 유지, 신규 동기 Invoke 0건
- L-02 grep-verifiable: `grep "_aliveResponseReceived\\|ALIVE_TIMEOUT_MS\\|AliveProcess\\|PerformAliveTimeout" WPF_Example/UI/MenuBar.xaml.cs` → 빈 출력
- L-03 grep-verifiable: `git diff WPF_Example/FinalVision.csproj` + `packages.config` 변경 0건
- `dotnet build WPF_Example/FinalVision.csproj` 그린
- 실기 UAT 4 시나리오(단계 2~5) 육안 통과
</verification>

<success_criteria>
- 3-state(회색/녹색Base/빨강) 전환 동작 확인
- 녹색 flash (150ms fade) 가 ALIVE 응답 수신 시마다 1회 수행됨
- 9초 타임아웃 후 빨강 래치, 재접속 시 즉시 녹색 Base 복귀
- L-01/L-02/L-03 3개 잠금 모두 grep/diff 로 회귀 없음 확인
- Phase 15 ALIVE 하트비트 로직 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/16-alive-ui/16-02-SUMMARY.md`
</output>
