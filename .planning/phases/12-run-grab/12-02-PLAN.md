---
phase: 12-run-grab
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/ShotTabView.xaml
  - WPF_Example/UI/ContentItem/ShotTabView.xaml.cs
  - WPF_Example/UI/ContentItem/MainView.xaml.cs
autonomous: true
requirements: [IMG-03]

must_haves:
  truths:
    - "시간 폴더를 선택하면 VirtualCamera.BackgroundImagePath가 해당 폴더 경로로 설정된다 (per D-05)"
    - "BackgroundImagePath 설정 후 GrabImage()가 카메라 대신 폴더 내 파일에서 순차적으로 이미지를 반환한다"
    - "폴더 로드 후 RUN(시퀀스 Start)을 실행하면 5-Shot이 BackgroundImagePath 파일 기반으로 순차 Grab된다 (per D-07)"
    - "폴더 로드 후 5개 ShotTabView 탭 모두 갱신되어 로드된 이미지가 표시된다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/ShotTabView.xaml"
      provides: "LoadFolder 버튼 XAML 정의"
      contains: "btn_loadFolder"
    - path: "WPF_Example/UI/ContentItem/ShotTabView.xaml.cs"
      provides: "Btn_LoadFolder_Click 핸들러 — Ookii FolderBrowserDialog + BackgroundImagePath 설정"
      contains: "Btn_LoadFolder_Click"
    - path: "WPF_Example/UI/ContentItem/MainView.xaml.cs"
      provides: "RefreshAllShotImages() — 5개 ShotTabView 일괄 갱신 메서드"
      contains: "RefreshAllShotImages"
  key_links:
    - from: "ShotTabView.xaml.cs Btn_LoadFolder_Click"
      to: "VirtualCamera.BackgroundImagePath"
      via: "Ookii FolderBrowserDialog 선택 폴더 경로 설정 (per D-05, D-06)"
      pattern: "BackgroundImagePath"
    - from: "VirtualCamera.BackgroundImagePath setter"
      to: "VirtualCamera.BackgroundImageFileList"
      via: "setter 내부에서 폴더 내 이미지 파일 자동 수집"
      pattern: "BackgroundImageFileList.AddRange"
    - from: "ShotTabView.xaml.cs Btn_LoadFolder_Click"
      to: "MainView.RefreshAllShotImages()"
      via: "폴더 로드 후 5개 ShotTabView 일괄 갱신"
      pattern: "RefreshAllShotImages"
---

<objective>
Ookii VistaFolderBrowserDialog로 시간폴더를 선택하면 VirtualCamera.BackgroundImagePath를 설정하여, 이후 GrabImage() 호출 시 카메라 대신 폴더 내 파일에서 이미지를 순차 반환하게 한다.

Purpose: IMG-03 — 시간 폴더 선택 시 VirtualCamera.BackgroundImagePath 재활용으로 Shot1~5 이미지를 일괄 로드 (per D-05, D-06, D-07)
Output: ShotTabView에 LoadFolder 버튼 추가, BackgroundImagePath 설정으로 파일 기반 Grab 활성화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-run-grab/12-CONTEXT.md
@.planning/phases/12-run-grab/12-RESEARCH.md

<interfaces>
<!-- VirtualCamera.BackgroundImagePath — 핵심 재활용 대상 (D-05) -->
<!-- setter에서 BackgroundImageFileList.Clear() 후 폴더 내 이미지 파일(.bmp, .jpg, .jpeg, .png, .tiff) 자동 수집 -->
<!-- GrabImage() 호출 시 BackgroundImagePath != null이면 BackgroundImageFileList에서 순차 반환 -->
private string _BackgroundImagePath;
public string BackgroundImagePath { get; set; }  // setter 내부에서 파일 목록 자동 수집
public List<string> BackgroundImageFileList { get; }
public int BackgroundImageIndex { get; private set; }

<!-- DeviceSelector.MenuItem_LoadImage_Click — 재사용할 패턴 (D-06) -->
Ookii.Dialogs.Wpf.VistaFolderBrowserDialog dlg = new ...();
dlg.SelectedPath = SystemHandler.Handle.Setting.ImageSavePath;
if ((bool)dlg.ShowDialog()) {
    pSelectedDevice.BackgroundImagePath = dlg.SelectedPath;
}

<!-- DeviceHandler 카메라 접근 -->
// SystemHandler.Handle.Devices[DeviceHandler.INSPECTION_CAMERA] → VirtualCamera
// DeviceHandler.INSPECTION_CAMERA = "Final_Inspection"

<!-- ShotTabView.xaml.cs 기존 필드 -->
private int _shotIndex = -1;
private SequenceHandler _pSeq;
private DeviceHandler _pDev;  // = SystemHandler.Handle.Devices
// GetParam(): _pSeq[ESequence.Inspection][_shotIndex].Param as InspectionParam

<!-- MainView.xaml.cs — ShotTabView 배열 패턴 (line 236, 499) -->
ShotTabView[] views = { shotView_1, shotView_2, shotView_3, shotView_4, shotView_5 };

<!-- ShotTabView.xaml.cs 기존 RefreshImage() -->
public void RefreshImage() // 원본/측정 선택에 따라 이미지 갱신, tb_imagePath 경로 표시
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShotTabView 폴더 로드 버튼 + BackgroundImagePath 설정 + 전체 탭 갱신 (per D-05, D-06, D-07, D-08)</name>
  <files>WPF_Example/UI/ContentItem/ShotTabView.xaml, WPF_Example/UI/ContentItem/ShotTabView.xaml.cs, WPF_Example/UI/ContentItem/MainView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/ShotTabView.xaml (현재 툴바 레이아웃, Grid.Column 구조)
    - WPF_Example/UI/ContentItem/ShotTabView.xaml.cs (전체 — 기존 필드, _pSeq, _pDev, GetParam, RefreshImage)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (ShotTabView[] views 배열 패턴 line 236, 499 — RefreshAllShotImages 추가 위치)
    - WPF_Example/Device/Camera/VirtualCamera.cs (BackgroundImagePath setter line 142~164, BackgroundImageFileList)
    - WPF_Example/UI/Device/DeviceSelector.xaml.cs (MenuItem_LoadImage_Click line 269~295 — Ookii FolderBrowserDialog + BackgroundImagePath 패턴)
    - WPF_Example/Custom/Device/DeviceHandler.cs (INSPECTION_CAMERA 상수 line 14)
  </read_first>
  <action>
**ShotTabView.xaml (per D-08: Claude 재량으로 ShotTabView 내 배치):**
1. Grid.Column="3" StackPanel 내에 btn_openImage 앞에 "폴더" 버튼 추가:
```xml
<Button x:Name="btn_loadFolder" Content="폴더" Width="52" FontSize="12"
        Click="Btn_LoadFolder_Click" Margin="0,0,4,0"
        ToolTip="시간 폴더 선택 — BackgroundImagePath로 파일 기반 Grab 활성화"/>
```

**ShotTabView.xaml.cs — Btn_LoadFolder_Click 신규 메서드 (per D-05, D-06):**

기존 DeviceSelector.MenuItem_LoadImage_Click 패턴을 재사용한다. 폴더를 선택하면 VirtualCamera.BackgroundImagePath를 설정한다. BackgroundImagePath setter가 내부에서 폴더 내 이미지 파일(.bmp, .jpg 등)을 자동으로 BackgroundImageFileList에 수집한다. 이후 RUN(시퀀스 Start) 실행 시 GrabImage()가 BackgroundImageFileList에서 순차적으로 이미지를 반환한다.

```csharp
//260406 hbk -- IMG-03: 시간폴더 선택 → BackgroundImagePath 설정 (D-05, D-06)
private void Btn_LoadFolder_Click(object sender, RoutedEventArgs e)
{
    //260406 hbk -- D-06: 기존 DeviceSelector.MenuItem_LoadImage_Click 패턴 재사용
    var dlg = new Ookii.Dialogs.Wpf.VistaFolderBrowserDialog {
        Description = "시간 폴더 선택 (이미지 로드용)",
        UseDescriptionForTitle = true,
        SelectedPath = SystemSetting.Handle.ImageSavePath,
    };
    if (dlg.ShowDialog() != true) return;

    //260406 hbk -- D-05: VirtualCamera.BackgroundImagePath 설정
    // setter 내부에서 BackgroundImageFileList 자동 수집됨
    var camera = _pDev[DeviceHandler.INSPECTION_CAMERA];
    if (camera == null) return;
    camera.BackgroundImagePath = dlg.SelectedPath;

    //260406 hbk -- 로드된 파일 수 확인
    if (camera.BackgroundImageFileList.Count == 0) {
        CustomMessageBox.Show("알림", "폴더에서 이미지 파일을 찾지 못했습니다.", MessageBoxImage.Information);
        camera.BackgroundImagePath = null;  //260406 hbk -- 리셋
        return;
    }

    Logging.PrintLog((int)ELogType.Trace, "[IMG] BackgroundImagePath 설정: {0} ({1}개 파일)",
        dlg.SelectedPath, camera.BackgroundImageFileList.Count);

    //260406 hbk -- 5개 ShotTabView 일괄 갱신 (WARNING 1 대응)
    // 부모 MainView의 RefreshAllShotImages 호출
    var mainView = Window.GetWindow(this)?.FindName("mainView") as MainView;
    // 위 방식이 안 되면 직접 접근: mParentWindow 패턴이 ShotTabView에는 없으므로
    // MainView에 public 메서드 추가 후 호출
    // 실제 구현 시 아래 패턴 사용:
    RefreshImage();  //260406 hbk -- 현재 탭은 즉시 갱신
    // 나머지 탭은 MainView.RefreshAllShotImages()에서 처리
}
```

**중요:** `Btn_LoadFolder_Click`에서 SimulImagePath를 설정하지 않는다. SimulImagePath는 개별 Shot 이미지 로드(Btn_OpenImage_Click)에서만 사용한다. 폴더 로드는 BackgroundImagePath만 설정하고, 실제 이미지 로드는 시퀀스 Start → GrabImage()에서 자동 처리된다 (per D-07).

**MainView.xaml.cs — RefreshAllShotImages 메서드 추가:**
기존 line 236~238의 인라인 패턴을 public 메서드로 추출한다:

```csharp
//260406 hbk -- IMG-03: 5개 ShotTabView 일괄 갱신 (폴더 로드 후 사용)
public void RefreshAllShotImages() {
    ShotTabView[] views = { shotView_1, shotView_2, shotView_3, shotView_4, shotView_5 };
    for (int i = 0; i < views.Length; i++)
        views[i].RefreshImage();
}
```

**ShotTabView.xaml.cs에서 MainView 접근 방법:**
ShotTabView에서 부모 MainView에 접근하는 기존 패턴을 확인할 것. `_pDev`처럼 SetParam 시 전달받는 참조가 있는지 확인. 없다면 VisualTreeHelper 또는 Window.GetWindow(this) 패턴으로 접근하되, 더 간단한 방법이 있다면 그것을 사용.

**using 추가 (필요시):**
- `using FinalVisionProject.Setting;` (SystemSetting 참조)
- `using FinalVisionProject.Custom.Device;` (DeviceHandler.INSPECTION_CAMERA 참조)
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && dotnet build WPF_Example.sln --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ShotTabView.xaml에 `btn_loadFolder` 요소가 존재함
    - ShotTabView.xaml.cs에 `Btn_LoadFolder_Click` 메서드가 존재함
    - ShotTabView.xaml.cs의 Btn_LoadFolder_Click에 `BackgroundImagePath` 설정이 존재함 (SimulImagePath가 아님)
    - ShotTabView.xaml.cs의 Btn_LoadFolder_Click에 `VistaFolderBrowserDialog` 사용이 존재함
    - MainView.xaml.cs에 `RefreshAllShotImages` 메서드가 존재함
    - `//260406 hbk` 주석이 신규 코드에 존재함
    - 빌드 성공 (error 0)
  </acceptance_criteria>
  <done>시간 폴더 선택 시 VirtualCamera.BackgroundImagePath가 설정되고, 이후 GrabImage()가 파일에서 이미지를 반환하며, 5개 ShotTabView 탭이 모두 갱신된다</done>
</task>

</tasks>

<verification>
1. 빌드 성공: `dotnet build WPF_Example.sln` error 0
2. ShotTabView.xaml에 btn_loadFolder 버튼 존재
3. ShotTabView.xaml.cs에 Btn_LoadFolder_Click 핸들러 존재 (BackgroundImagePath 설정, SimulImagePath 미사용)
4. MainView.xaml.cs에 RefreshAllShotImages 메서드 존재
</verification>

<success_criteria>
- ShotTabView에 "폴더" 버튼이 추가되어 Ookii VistaFolderBrowserDialog로 시간폴더를 선택할 수 있음
- 폴더 선택 시 VirtualCamera.BackgroundImagePath가 설정되어 GrabImage()가 파일 기반으로 전환됨
- BackgroundImagePath setter가 내부에서 폴더 내 이미지 파일을 자동 수집함
- 폴더에 이미지가 없으면 경고 후 BackgroundImagePath를 null로 리셋함
- 5개 ShotTabView 탭이 모두 갱신됨
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/12-run-grab/12-02-SUMMARY.md`
</output>
