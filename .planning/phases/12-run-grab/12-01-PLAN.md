---
phase: 12-run-grab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/UI/ContentItem/ShotTabView.xaml
  - WPF_Example/UI/ContentItem/ShotTabView.xaml.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements: [OPS-02]

must_haves:
  truths:
    - "ShotTabView의 Grab 버튼이 제거되어 Shot 탭에서 카메라 촬상이 불가능하다"
    - "InspectionListView의 RUN(btn_start) 버튼 클릭 시 BackgroundImagePath가 설정된 카메라면 시퀀스 Start로 5-Shot 순차 Grab 실행된다 (per D-07)"
    - "InspectionListView의 RUN(btn_start) 버튼 클릭 시 개별 Shot에 SimulImagePath가 설정되어 있으면 해당 Shot만 RunBlobOnLastGrab 실행된다 (per D-04)"
    - "BackgroundImagePath도 SimulImagePath도 없는 상태에서 RUN 클릭 시 기존 시퀀스 Start 동작이 유지된다"
    - "InspectionListView의 Grab(button_grab)은 카메라 촬상 전용으로 기존 동작을 유지한다"
  artifacts:
    - path: "WPF_Example/UI/ContentItem/ShotTabView.xaml"
      provides: "btn_grab 버튼 제거된 XAML"
      contains: "btn_editRoi"
    - path: "WPF_Example/UI/ContentItem/ShotTabView.xaml.cs"
      provides: "Btn_Grab_Click 핸들러 제거된 코드비하인드"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "Btn_start_Click 분기 로직 (BackgroundImagePath → 시퀀스 Start, SimulImagePath → RunBlobOnLastGrab)"
  key_links:
    - from: "InspectionListView.xaml.cs Btn_start_Click"
      to: "mParentWindow.StartSequence()"
      via: "BackgroundImagePath가 설정된 카메라에서 시퀀스 Start (D-07)"
      pattern: "BackgroundImagePath.*StartSequence"
    - from: "InspectionListView.xaml.cs Btn_start_Click"
      to: "Action_Inspection.RunBlobOnLastGrab()"
      via: "SimulImagePath 존재 시 개별 Shot 직접 호출 (D-04)"
      pattern: "RunBlobOnLastGrab"
    - from: "VirtualCamera.BackgroundImagePath"
      to: "VirtualCamera.GrabImage()"
      via: "BackgroundImagePath 설정 시 GrabImage()가 파일에서 이미지 반환"
      pattern: "BackgroundImagePath"
---

<objective>
ShotTabView의 Shot별 Grab 버튼을 제거하고, InspectionListView의 RUN(btn_start) 버튼 동작을 변경하여 Grab/Run 역할을 명확히 분리한다.

Purpose: OPS-02 — Grab은 카메라 촬상 전용(InspectionListView button_grab), Run은 로드된 이미지 검사 전용(InspectionListView btn_start)으로 역할 분리
Output: ShotTabView에서 Grab 버튼 제거, InspectionListView RUN 동작 변경 (BackgroundImagePath/SimulImagePath 분기)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-run-grab/12-CONTEXT.md
@.planning/phases/12-run-grab/12-RESEARCH.md

<interfaces>
<!-- ShotTabView.xaml — btn_grab 제거 대상 (line 21~22) -->
<Button x:Name="btn_grab" Content="Grab" Width="80" FontSize="14"
        Click="Btn_Grab_Click" Margin="0,0,8,0"/>

<!-- ShotTabView.xaml.cs — Btn_Grab_Click 제거 대상 (line 138~178) -->
private async void Btn_Grab_Click(object sender, RoutedEventArgs e)

<!-- InspectionListView.xaml.cs — Btn_start_Click 변경 대상 (line 103~131) -->
private void Btn_start_Click(object sender, RoutedEventArgs e) {
    // 현재: 선택된 노드의 시퀀스/액션을 mParentWindow.StartSequence로 실행
}

<!-- VirtualCamera.BackgroundImagePath — 폴더 설정 시 GrabImage()가 파일에서 읽음 -->
public string BackgroundImagePath { get; set; }
public List<string> BackgroundImageFileList { get; }

<!-- Action_Inspection.RunBlobOnLastGrab (line 292~303) — 개별 Shot 검사 -->
public void RunBlobOnLastGrab()

<!-- DeviceHandler — 카메라 접근 -->
// SystemHandler.Handle.Devices[DeviceHandler.INSPECTION_CAMERA] → VirtualCamera
// DeviceHandler.INSPECTION_CAMERA = "Final_Inspection"

<!-- InspectionListView.xaml.cs — SelectedParam (기존 프로퍼티) -->
// SelectedParam: 현재 트리에서 선택된 Action의 ParamBase
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShotTabView Grab 버튼 제거 (per D-01)</name>
  <files>WPF_Example/UI/ContentItem/ShotTabView.xaml, WPF_Example/UI/ContentItem/ShotTabView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ContentItem/ShotTabView.xaml (현재 btn_grab XAML 정의 확인)
    - WPF_Example/UI/ContentItem/ShotTabView.xaml.cs (Btn_Grab_Click 핸들러 + _grabTask 필드 확인)
  </read_first>
  <action>
**ShotTabView.xaml (per D-01):**
1. Line 21~22의 `<Button x:Name="btn_grab" Content="Grab" .../>` 전체 삭제
2. 삭제 후 StackPanel Grid.Column="0" 내 첫 요소가 `btn_editRoi` ToggleButton이 되도록 정리
3. btn_editRoi의 Margin을 `0,0,12,0`으로 유지 (기존과 동일)

**ShotTabView.xaml.cs (per D-01):**
1. `Btn_Grab_Click` 메서드 전체 삭제 (line 138~178, `private async void Btn_Grab_Click` ~ `_grabTask = null;` 까지)
2. `_grabTask` 필드 삭제 (line 27: `private Task _grabTask;`)
3. `btn_grab.IsEnabled = true;` 등 btn_grab 참조하는 코드가 다른 메서드에 있다면 함께 제거
4. 주석 추가: `//260406 hbk -- D-01: ShotTabView Grab 버튼 제거 (InspectionListView button_grab으로 통합)`
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && dotnet build WPF_Example.sln --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ShotTabView.xaml에 `btn_grab` 문자열이 존재하지 않음
    - ShotTabView.xaml.cs에 `Btn_Grab_Click` 문자열이 존재하지 않음
    - ShotTabView.xaml.cs에 `_grabTask` 문자열이 존재하지 않음
    - 빌드 성공 (error 0)
  </acceptance_criteria>
  <done>ShotTabView에서 Grab 버튼과 관련 코드가 완전히 제거되고 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: InspectionListView RUN 버튼 동작 변경 — BackgroundImagePath/SimulImagePath 통합 분기 (per D-03, D-04, D-07)</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (Btn_start_Click 현재 구현 line 103~131, SelectedParam 프로퍼티, button_grab_Click line 218~235)
    - WPF_Example/Custom/Sequence/Inspection/Action_Inspection.cs (RunBlobOnLastGrab line 292~303, Run() EStep.Grab line 239~261)
    - WPF_Example/Device/Camera/VirtualCamera.cs (BackgroundImagePath line 142~164, GrabImage 시 파일 로드 분기 line 222~233)
    - WPF_Example/Custom/Device/DeviceHandler.cs (INSPECTION_CAMERA 상수 line 14)
    - WPF_Example/UI/ContentItem/MainView.xaml.cs (ShotTabView[] views 배열 패턴 line 236, line 499)
  </read_first>
  <action>
**InspectionListView.xaml.cs Btn_start_Click 메서드 변경 (per D-03, D-04, D-07):**

현재 `Btn_start_Click`은 선택된 노드의 시퀀스/액션을 `mParentWindow.StartSequence(seqID, actID)`로 실행한다. 이 로직 앞에 두 단계의 분기를 추가한다:

**분기 1 — BackgroundImagePath 경로 (per D-05, D-07):**
카메라에 BackgroundImagePath가 설정되어 있으면, 시퀀스 Start()를 호출한다. GrabImage()가 자동으로 BackgroundImagePath에서 파일을 읽으므로 5-Shot 순차 Grab이 파일 기반으로 실행된다.

**분기 2 — SimulImagePath 경로 (per D-04):**
개별 Shot에 SimulImagePath가 설정되어 있으면, 해당 Shot만 RunBlobOnLastGrab()으로 직접 검사한다.

**분기 3 — 기본 경로:**
위 조건 모두 해당하지 않으면 기존 시퀀스 Start 동작 유지.

```csharp
private void Btn_start_Click(object sender, RoutedEventArgs e) {
    if (treeListBox_sequence.SelectedIndex < 0) return;

    //260406 hbk -- D-07: BackgroundImagePath가 설정된 카메라면 시퀀스 Start로 5-Shot Grab (파일에서 읽음)
    var camera = SystemHandler.Handle.Devices[DeviceHandler.INSPECTION_CAMERA];
    if (camera != null && !string.IsNullOrEmpty(camera.BackgroundImagePath)) {
        if (!SystemHandler.Handle.Sequences.IsIdle) return;   //260406 hbk -- TCP 충돌 방지
        //260406 hbk -- 기존 RUN과 동일하게 시퀀스 Start 호출 — GrabImage()가 BackgroundImagePath에서 순차 로드
        mParentWindow.StartSequence(ESequence.Inspection);
        return;
    }

    //260406 hbk -- D-04: 개별 Shot에 SimulImagePath 설정된 경우 선택된 Shot만 RunBlobOnLastGrab
    if (treeListBox_sequence.SelectedItem is NodeViewModel node
        && node.NodeType == ENodeType.Action) {

        ESequence seqID = node.SequenceID;
        if (seqID == ESequence.Inspection) {
            var seq = SystemHandler.Handle.Sequences[seqID];
            int actIndex = -1;
            for (int i = 0; i < seq.ActionCount; i++) {
                if (seq[i].ID == node.ActionID) { actIndex = i; break; }
            }
            if (actIndex >= 0 && seq[actIndex] is Action_Inspection act) {
                var p = act.Param as InspectionParam;
                if (p != null && !string.IsNullOrEmpty(p.SimulImagePath)
                    && File.Exists(p.SimulImagePath)) {
                    if (!SystemHandler.Handle.Sequences.IsIdle) return;   //260406 hbk
                    act.RunBlobOnLastGrab();   //260406 hbk -- D-04: 선택된 Shot만 검사
                    //260406 hbk -- UI 갱신: 해당 Shot의 ShotTabView RefreshImage
                    mParentWindow.mainView.RefreshShotImage(actIndex);
                    return;
                }
            }
        }
    }

    //260406 hbk -- 기본: 기존 시퀀스 실행 로직 유지 (카메라 촬상 + 검사)
    if (treeListBox_sequence.SelectedItem is NodeViewModel) {
        // ... 기존 로직 그대로 유지 ...
    }
}
```

**핵심 우선순위 (3단계 분기):**
1. BackgroundImagePath 설정됨 → `mParentWindow.StartSequence(ESequence.Inspection)` (per D-07: 5-Shot 순차 Grab, GrabImage()가 파일에서 읽음)
2. 개별 SimulImagePath 설정됨 → `RunBlobOnLastGrab()` 직접 호출 (per D-04: 선택된 Shot만)
3. 둘 다 없음 → 기존 `mParentWindow.StartSequence(seqID, actID)` 동작 유지

**RefreshShotImage 호출:**
`mParentWindow.mainView`에 `RefreshShotImage(int shotIndex)` 메서드가 없다면, MainView.xaml.cs에 추가가 필요하다. 기존 패턴 (line 236):
```csharp
// MainView.xaml.cs에 추가
public void RefreshShotImage(int shotIndex) {
    ShotTabView[] views = { shotView_1, shotView_2, shotView_3, shotView_4, shotView_5 };
    if (shotIndex >= 0 && shotIndex < views.Length)
        views[shotIndex].RefreshImage();
}
```
이미 동일 패턴이 line 236~238에 인라인으로 있으므로 메서드로 추출한다. 없다면 새로 추가.

**using 추가 (필요시):**
- `using FinalVisionProject.Custom.Sequence.Inspection;` (Action_Inspection 참조용) — 이미 있다면 생략
- `using FinalVisionProject.Custom.Device;` (DeviceHandler.INSPECTION_CAMERA 참조)
- `using System.IO;` (File.Exists 참조용)

**주의사항 (Research Pitfall 1, 4 반영):**
- `SystemHandler.Handle.Sequences.IsIdle` 체크 필수 (TCP 충돌 방지)
- BackgroundImagePath 경로는 시퀀스 Start()를 사용하므로 BeginInspection()이 호출되어 시간폴더가 생성된다 — 이는 D-07의 의도된 동작
- SimulImagePath 경로는 RunBlobOnLastGrab() 직접 호출이므로 시간폴더 생성 안 됨
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && dotnet build WPF_Example.sln --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - InspectionListView.xaml.cs의 Btn_start_Click 메서드에 `BackgroundImagePath` 분기가 존재함
    - InspectionListView.xaml.cs의 Btn_start_Click에 `mParentWindow.StartSequence(ESequence.Inspection)` 호출이 존재함 (D-07)
    - InspectionListView.xaml.cs의 Btn_start_Click에 `RunBlobOnLastGrab()` 호출이 존재함 (D-04)
    - InspectionListView.xaml.cs에 `IsIdle` 체크가 존재함
    - `//260406 hbk` 주석이 변경된 라인에 존재함
    - 빌드 성공 (error 0)
  </acceptance_criteria>
  <done>RUN 버튼 클릭 시 BackgroundImagePath 설정된 카메라면 시퀀스 Start로 5-Shot 파일 Grab, 개별 SimulImagePath면 RunBlobOnLastGrab, 없으면 기존 카메라 시퀀스가 실행된다</done>
</task>

</tasks>

<verification>
1. 빌드 성공: `dotnet build WPF_Example.sln` error 0
2. ShotTabView.xaml에 btn_grab 없음
3. ShotTabView.xaml.cs에 Btn_Grab_Click, _grabTask 없음
4. InspectionListView.xaml.cs Btn_start_Click에 BackgroundImagePath + StartSequence 분기 존재 (D-07)
5. InspectionListView.xaml.cs Btn_start_Click에 SimulImagePath + RunBlobOnLastGrab 분기 존재 (D-04)
</verification>

<success_criteria>
- ShotTabView에서 Grab 버튼이 완전히 제거됨 (XAML + 코드비하인드)
- InspectionListView RUN 버튼이 BackgroundImagePath → SimulImagePath → 기본 순서로 3단계 분기 실행됨
- InspectionListView Grab 버튼(button_grab)은 기존 카메라 촬상 동작 유지
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/12-run-grab/12-01-SUMMARY.md`
</output>
