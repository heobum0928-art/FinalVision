---
phase: 12-run-grab
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - WPF_Example/Setting/SystemSetting.cs
  - WPF_Example/UI/Setting/SystemSettingWindow.xaml
  - WPF_Example/UI/Setting/SystemSettingWindow.xaml.cs
autonomous: false
requirements: [IMG-04]

must_haves:
  truths:
    - "SystemSetting 창에 이미지 관리 탭이 존재하고 날짜 폴더 목록이 표시된다"
    - "체크박스로 날짜 폴더를 선택하고 삭제 버튼을 클릭하면 확인 다이얼로그가 표시된다"
    - "확인 후 선택된 날짜 폴더가 recursive 삭제되고 목록에서 제거된다"
    - "취소 시 삭제가 실행되지 않는다"
  artifacts:
    - path: "WPF_Example/Setting/SystemSetting.cs"
      provides: "GetImageDateFolders() 메서드 — 날짜 폴더 목록 반환"
      contains: "GetImageDateFolders"
    - path: "WPF_Example/UI/Setting/SystemSettingWindow.xaml"
      provides: "이미지 관리 탭 UI (TabItem + ListBox + 삭제 버튼)"
      contains: "tab_imageManage"
  key_links:
    - from: "SystemSettingWindow 삭제 버튼"
      to: "Directory.Delete(path, recursive: true)"
      via: "선택된 날짜 폴더 경로"
      pattern: "Directory.Delete"
    - from: "SystemSettingWindow 이미지 관리 탭"
      to: "SystemSetting.Handle.ImageSavePath"
      via: "기준 경로에서 날짜 폴더 조회"
      pattern: "ImageSavePath"
---

<objective>
SystemSetting 창에 이미지 관리 탭을 추가하여 날짜 폴더(yyyyMMdd) 단위로 저장된 검사 이미지를 체크박스 선택 후 삭제할 수 있게 한다.

Purpose: IMG-04 — 날짜/시간 폴더 단위로 저장된 검사 이미지 삭제 가능
Output: SystemSetting 창에 이미지 관리 탭 추가, 날짜 폴더 목록 + 체크 삭제 기능
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-run-grab/12-CONTEXT.md
@.planning/phases/12-run-grab/12-RESEARCH.md

<interfaces>
<!-- SystemSetting.cs 주요 멤버 -->
public static SystemSetting Handle { get; } = new SystemSetting();
public string ImageSavePath { get; set; } = @"D:\Data\Image";

<!-- CustomMessageBox 패턴 (InspectionListView.xaml.cs에서 확인) -->
var result = CustomMessageBox.ShowConfirmation("제목", "메시지", MessageBoxButton.YesNo);
if (result != MessageBoxResult.Yes) return;

<!-- Directory.Delete 패턴 -->
Directory.Delete(folder, recursive: true);

<!-- ImageFolderManager.BeginInspection() 폴더 구조 -->
// basePath / yyyyMMdd / HHmm / {shotName}_{OK|NG}_{ss_fff}.bmp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemSetting에 날짜 폴더 조회 메서드 추가 + SystemSettingWindow 이미지 관리 탭 (per D-09, D-10, D-11, D-12)</name>
  <files>WPF_Example/Setting/SystemSetting.cs, WPF_Example/UI/Setting/SystemSettingWindow.xaml, WPF_Example/UI/Setting/SystemSettingWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/Setting/SystemSetting.cs (전체 — ImageSavePath 위치, 기존 메서드 패턴)
    - WPF_Example/UI/Setting/SystemSettingWindow.xaml (전체 — 기존 구조가 PropertyGrid 기반인지 TabControl 기반인지 확인)
    - WPF_Example/UI/Setting/SystemSettingWindow.xaml.cs (전체 — 기존 코드비하인드 구조)
    - WPF_Example/Utility/ImageFolderManager.cs (폴더 구조: basePath/yyyyMMdd/HHmm)
  </read_first>
  <action>
**사전 구조 판단이 필수이다.** SystemSettingWindow.xaml을 읽고 다음을 먼저 확인:
- TabControl 기반이면 → 대안 B: 기존 TabControl에 TabItem 추가
- PropertyGrid(PropertyTools.Wpf) 기반이면 → 대안 A: 별도 Window 클래스 `ImageManageWindow` 신규 생성. SystemSettingWindow의 메뉴나 버튼에서 접근 가능하게 한다. 또는 기존 PropertyGrid 아래에 버튼 하나를 추가하여 별도 창을 여는 방식.

**대안 A (PropertyGrid 기반인 경우):**
1. `ImageManageWindow.xaml` + `ImageManageWindow.xaml.cs` 신규 생성
2. SystemSettingWindow에 "이미지 관리" 버튼 추가하여 `new ImageManageWindow().ShowDialog()` 호출
3. FinalVision.csproj에 `<Compile Include="..."/>` + `<Page Include="..."/>` 추가 필수

**대안 B (TabControl 기반인 경우):**
1. 기존 TabControl에 `tab_imageManage` TabItem 추가

**어느 대안이든 공통 구현:**

**SystemSetting.cs — GetImageDateFolders 메서드 추가:**
```csharp
//260406 hbk -- IMG-04: 이미지 저장 경로의 날짜 폴더 목록 반환 (D-10)
public string[] GetImageDateFolders() {
    if (!Directory.Exists(ImageSavePath)) return new string[0];
    return Directory.GetDirectories(ImageSavePath)
        .Where(d => {
            string name = Path.GetFileName(d);
            return name.Length == 8 && name.All(char.IsDigit);
        })
        .OrderByDescending(d => d)
        .ToArray();
}
```
- using 추가: `using System.Linq;` (이미 존재하면 생략)

**이미지 관리 UI (per D-09: SystemSetting 창에 탭 추가):**

XAML (TabItem 또는 별도 Window 내):
```xml
<!-- 이미지 관리 탭   //260406 hbk D-09 -->
<TabItem x:Name="tab_imageManage" Header="이미지 관리">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="저장된 이미지 날짜 폴더" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- D-10: 날짜 폴더 체크박스 목록 -->
        <ListBox x:Name="lb_dateFolders" Grid.Row="1" SelectionMode="Extended">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"
                              FontSize="13" Margin="4,2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="새로고침" Width="80" FontSize="12" Click="Btn_RefreshFolders_Click" Margin="0,0,8,0"/>
            <Button x:Name="btn_deleteFolders" Content="선택 삭제" Width="100" FontSize="12"
                    Click="Btn_DeleteFolders_Click" Foreground="Red"/>
        </StackPanel>
    </Grid>
</TabItem>
```

코드비하인드:
```csharp
//260406 hbk -- IMG-04: 날짜 폴더 목록용 뷰모델
public class DateFolderItem : System.ComponentModel.INotifyPropertyChanged {
    public string FullPath { get; set; }
    public string Name { get; set; }
    private bool _isChecked;
    public bool IsChecked {
        get => _isChecked;
        set { _isChecked = value; PropertyChanged?.Invoke(this, new System.ComponentModel.PropertyChangedEventArgs(nameof(IsChecked))); }
    }
    public event System.ComponentModel.PropertyChangedEventHandler PropertyChanged;
}

//260406 hbk -- 폴더 목록 로드
private void LoadDateFolders() {
    var folders = SystemSetting.Handle.GetImageDateFolders();
    var items = new System.Collections.ObjectModel.ObservableCollection<DateFolderItem>();
    foreach (var f in folders) {
        items.Add(new DateFolderItem { FullPath = f, Name = Path.GetFileName(f), IsChecked = false });
    }
    lb_dateFolders.ItemsSource = items;
}

private void Btn_RefreshFolders_Click(object sender, RoutedEventArgs e) {
    LoadDateFolders();
}

//260406 hbk -- D-11, D-12: 선택된 폴더 삭제
private void Btn_DeleteFolders_Click(object sender, RoutedEventArgs e) {
    var items = lb_dateFolders.ItemsSource as System.Collections.ObjectModel.ObservableCollection<DateFolderItem>;
    if (items == null) return;
    var selected = items.Where(x => x.IsChecked).ToList();
    if (selected.Count == 0) {
        CustomMessageBox.Show("알림", "삭제할 폴더를 선택하세요.", MessageBoxImage.Information);
        return;
    }

    //260406 hbk -- D-11: 삭제 전 확인 다이얼로그
    string names = string.Join(", ", selected.Select(x => x.Name));
    var result = CustomMessageBox.ShowConfirmation(
        "폴더 삭제 확인",
        $"선택된 {selected.Count}개 폴더를 삭제합니까?\n({names})\n\n하위 파일 모두 삭제됩니다.",
        MessageBoxButton.YesNo);
    if (result != MessageBoxResult.Yes) return;

    int deleted = 0;
    foreach (var item in selected) {
        try {
            Directory.Delete(item.FullPath, true);   //260406 hbk -- D-12
            deleted++;
        } catch (Exception ex) {
            CustomMessageBox.Show("삭제 실패", $"{item.Name}: {ex.Message}", MessageBoxImage.Error);
        }
    }

    if (deleted > 0) {
        Logging.PrintLog((int)ELogType.Trace, "[IMG] 폴더 {0}개 삭제 완료", deleted);
        LoadDateFolders();   //260406 hbk -- 목록 새로고침
    }
}
```

**탭 로드 시점:** SystemSettingWindow 생성자 또는 Loaded 이벤트에서 `LoadDateFolders()` 호출.

**FinalVision.csproj:** 신규 .cs/.xaml 파일을 생성한 경우 `<Compile Include="..."/>` 항목을 .csproj에 추가해야 한다 (Phase 11 결정사항: explicit Compile includes 필요).
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && dotnet build WPF_Example.sln --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SystemSetting.cs에 `GetImageDateFolders` 메서드가 존재함
    - 이미지 관리 UI(탭 또는 별도 창)에 `lb_dateFolders` ListBox가 존재함
    - 코드비하인드에 `Btn_DeleteFolders_Click` 메서드가 존재함
    - `Btn_DeleteFolders_Click`에 `CustomMessageBox.ShowConfirmation` 호출이 존재함
    - `Btn_DeleteFolders_Click`에 `Directory.Delete` 호출이 존재함 (recursive: true)
    - `//260406 hbk` 주석이 신규 코드에 존재함
    - 빌드 성공 (error 0)
  </acceptance_criteria>
  <done>SystemSetting 창에서 날짜 폴더 목록이 표시되고 체크 선택 후 확인 다이얼로그를 거쳐 삭제가 실행된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 기능 수동 검증</name>
  <files>none</files>
  <action>사용자가 앱을 실행하여 Phase 12 전체 기능(Plan 01 + Plan 02 + Plan 03)을 수동 검증한다.</action>
  <what-built>
    Phase 12 전체 기능:
    1. ShotTabView Grab 버튼 제거 (Plan 01)
    2. InspectionListView RUN 버튼 3단계 분기: BackgroundImagePath -> SimulImagePath -> 기본 (Plan 01)
    3. ShotTabView 폴더 로드 버튼 -- BackgroundImagePath 설정으로 파일 기반 Grab 활성화 (Plan 02)
    4. SystemSetting 이미지 관리 탭 -- 날짜 폴더 선택 삭제 (Plan 03)
  </what-built>
  <how-to-verify>
    1. 앱 실행 후 ShotTabView에 Grab 버튼이 없는지 확인
    2. ShotTabView의 "폴더" 버튼 클릭 -> 시간폴더 선택 -> BackgroundImagePath 설정 확인 (로그에 "[IMG] BackgroundImagePath 설정" 출력)
    3. InspectionListView에서 RUN 클릭 -> 시퀀스 Start로 5-Shot 순차 파일 Grab 실행 확인 (BackgroundImagePath 경로)
    4. 폴더 로드 없이 개별 Shot에 SimulImagePath 설정 후 RUN 클릭 -> 해당 Shot만 RunBlobOnLastGrab 실행 확인
    5. InspectionListView Grab(camera) 버튼은 기존 카메라 촬상 동작 유지 확인
    6. SystemSetting -> 이미지 관리 탭 -> 날짜 폴더 목록 표시 확인
    7. 체크박스 선택 -> 삭제 -> 확인 다이얼로그 -> 삭제 완료 확인
    8. 삭제 확인 다이얼로그에서 취소 -> 삭제 안 됨 확인
  </how-to-verify>
  <verify>사용자가 8가지 검증 항목을 수동으로 확인하고 approved 입력</verify>
  <done>사용자가 Phase 12 전체 기능을 검증하고 approved를 입력함</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. 빌드 성공: `dotnet build WPF_Example.sln` error 0
2. SystemSetting.cs에 GetImageDateFolders 메서드 존재
3. 이미지 관리 UI에 날짜 폴더 목록 + 삭제 버튼 존재
4. 삭제 시 CustomMessageBox.ShowConfirmation 확인 다이얼로그 호출
</verification>

<success_criteria>
- SystemSetting 창에 이미지 관리 탭(또는 별도 창)이 추가됨
- 날짜 폴더(yyyyMMdd) 목록이 체크박스와 함께 표시됨
- 선택된 폴더를 확인 다이얼로그 후 recursive 삭제 가능
- 삭제 후 목록이 자동 새로고침됨
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/12-run-grab/12-03-SUMMARY.md`
</output>
