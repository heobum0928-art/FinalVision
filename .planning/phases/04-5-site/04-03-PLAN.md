---
phase: 04-5-site
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - WPF_Example/Sequence/SequenceHandler.cs
  - WPF_Example/Custom/SystemHandler.cs
autonomous: true
requirements:
  - REQ-005
  - REQ-008
must_haves:
  truths:
    - "SequenceHandler.LoadRecipe(int siteNumber, string name)이 Recipe/SiteN/name/main.ini 경로에서 로드하고 SiteContext.CurrentRecipeName을 갱신한다"
    - "SequenceHandler.SaveRecipe(int siteNumber, string name)이 Recipe/SiteN/name/main.ini 경로에 저장한다"
    - "ProcessRecipeChange가 packet.Site를 사용하여 SiteManager.Handle[siteNumber-1].CurrentRecipeName을 갱신한다"
    - "기존 LoadRecipe(string name) 메서드가 그대로 유지된다 (기존 코드 호환)"
    - "빌드가 에러 0건으로 성공한다"
  artifacts:
    - path: "WPF_Example/Sequence/SequenceHandler.cs"
      provides: "LoadRecipe(int, string) + SaveRecipe(int, string) 오버로드, LoadFromIni(int, string) + SaveToIni(int, string) private 메서드"
      exports: ["LoadRecipe(int, string)", "SaveRecipe(int, string)"]
    - path: "WPF_Example/Custom/SystemHandler.cs"
      provides: "ProcessRecipeChange — Site 인자 반영, SiteContext.CurrentRecipeName 갱신"
  key_links:
    - from: "SequenceHandler.LoadFromIni(int siteNumber, string name)"
      to: "RecipeFiles.GetRecipeFilePath(int, string)"
      via: "SystemHandler.Handle.Recipes.GetRecipeFilePath(siteNumber, name)"
      pattern: "GetRecipeFilePath.*siteNumber.*name"
    - from: "SequenceHandler.LoadFromIni(int siteNumber, string name)"
      to: "SiteManager.Handle[siteNumber-1].CurrentRecipeName"
      via: "SiteManager.Handle[siteNumber - 1].CurrentRecipeName = name"
      pattern: "SiteManager.Handle\\[siteNumber - 1\\]"
    - from: "ProcessRecipeChange"
      to: "SequenceHandler.LoadRecipe(int, string)"
      via: "Sequences.LoadRecipe(siteNumber, recipeName)"
      pattern: "LoadRecipe.*siteNumber.*recipeName"
---

<objective>
SequenceHandler에 Site 인자를 받는 LoadRecipe/SaveRecipe 오버로드를 추가하고, ProcessRecipeChange를 Site 인자를 전달하도록 수정한다.

Purpose: 이 Plan이 완료되면 TCP 명령으로 Site 2에 레시피 변경 요청이 오면 `Recipe/Site2/레시피명/main.ini`에서 로드하고 `SiteManager.Handle[1].CurrentRecipeName`이 갱신되는 전체 흐름이 완성된다.
Output: SequenceHandler Site 오버로드 4개(public 2 + private 2), ProcessRecipeChange 수정, 빌드 성공
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-5-site/04-RESEARCH.md
@.planning/phases/04-5-site/04-01-SUMMARY.md
@.planning/phases/04-5-site/04-02-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01에서 생성한 타입 — executor가 직접 참조 -->
```csharp
// WPF_Example/Custom/Site/SiteManager.cs
namespace FinalVisionProject.Site {
    public class SiteManager {
        public static SiteManager Handle { get; }
        public SiteContext this[int siteIndex] { get; }  // 0-based
    }
}

// WPF_Example/Custom/Site/SiteContext.cs
namespace FinalVisionProject.Site {
    public class SiteContext {
        public string CurrentRecipeName { get; set; }
        public void AddResult(bool isOk);
    }
}
```

<!-- Plan 02에서 추가한 메서드 — executor가 직접 참조 -->
```csharp
// WPF_Example/Utility/RecipeFileHelper.cs
public string GetRecipeFilePath(int siteNumber, string name);
// 반환: Path.Combine(RecipeSavePath, "Site" + siteNumber, name, "main.ini")
```

<!-- 기존 SequenceHandler.cs의 LoadFromIni/SaveToIni 현재 구조 (수정 전 참조용) -->
```csharp
// WPF_Example/Sequence/SequenceHandler.cs — 현재 존재하는 private 메서드
private bool LoadFromIni(string name) {
    if (string.IsNullOrEmpty(name)) return false;
    string recipeFile = SystemHandler.Handle.Recipes.GetRecipeFilePath(name);  // ← Site 없음
    if (File.Exists(recipeFile) == false) return false;
    IniFile loadFile = new IniFile(recipeFile);
    ModelName = loadFile["Info"]["ModelName"].ToString();
    // ... Param 로드 ...
    pSetting.CurrentRecipeName = name;  // ← 전역 설정만 갱신
    return true;
}

private bool SaveToIni(string name) {
    if (name != null) ModelName = name;
    string recipeFile = SystemHandler.Handle.Recipes.GetRecipeFilePath(ModelName);  // ← Site 없음
    // ... Param 저장 ...
    saveFile.Save(recipeFile);
    return true;
}

public bool LoadRecipe(string name, ERecipeFileType fileType = ERecipeFileType.Ini) {
    // result = LoadFromIni(name) 또는 LoadFromJson(name)
    // OnRecipeChanged?.Invoke(...)
    // if (result) ExecOnLoad(name);
}

public bool SaveRecipe(string name, ERecipeFileType fileType = ERecipeFileType.Ini) {
    // result = SaveToIni(name) 또는 SaveToJson(name)
}
```

<!-- 기존 ProcessRecipeChange 현재 구조 (수정 전 참조용) -->
```csharp
// WPF_Example/Custom/SystemHandler.cs
private RecipeChangeResultPacket ProcessRecipeChange(RecipeChangePacket packet) {
    RecipeChangeResultPacket resultPacket = new RecipeChangeResultPacket();
    resultPacket.Target = packet.Sender;
    resultPacket.Site = packet.Site;
    string recipeName = packet.RecipeName;
    // 현재: Site 무시, HasRecipe(recipeName) 전역 체크, LoadRecipe(recipeName) 전역 로드
    if (Recipes.HasRecipe(recipeName) == false) { resultPacket.Result = EVisionResultType.NG; }
    else if ((Setting.CurrentRecipeName != recipeName) && LoadRecipe(recipeName)) { resultPacket.Result = EVisionResultType.OK; }
    else if ((Setting.CurrentRecipeName == recipeName) && LoadRecipe(recipeName)) { resultPacket.Result = EVisionResultType.OK; }
    else { resultPacket.Result = EVisionResultType.NG; }
    return resultPacket;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SequenceHandler.cs에 Site 인자 LoadRecipe/SaveRecipe 오버로드 추가</name>
  <files>WPF_Example/Sequence/SequenceHandler.cs</files>
  <read_first>
    - WPF_Example/Sequence/SequenceHandler.cs (파일 전체 — 기존 LoadRecipe, LoadFromIni, SaveToIni, SaveRecipe 메서드 위치와 내용 파악)
    - WPF_Example/Custom/Site/SiteManager.cs (Plan 01 생성 파일 — using 구문에 추가할 namespace 확인)
  </read_first>
  <action>
**1단계: using 추가**

SequenceHandler.cs 상단 using 블록에 다음을 추가한다 (아직 없는 경우에만):
```csharp
using FinalVisionProject.Site;
```

**2단계: LoadFromIni(int siteNumber, string name) 추가**

기존 `private bool LoadFromIni(string name)` 메서드 바로 아래에 다음 오버로드를 추가한다:

```csharp
/// <summary>
/// Site 지정 INI 로드. Recipe/SiteN/name/main.ini 경로를 사용한다.
/// 로드 완료 후 SiteContext.CurrentRecipeName 갱신.
/// </summary>
private bool LoadFromIni(int siteNumber, string name) {
    if (string.IsNullOrEmpty(name)) return false;
    string recipeFile = SystemHandler.Handle.Recipes.GetRecipeFilePath(siteNumber, name);
    if (File.Exists(recipeFile) == false) return false;

    IniFile loadFile = new IniFile(recipeFile);
    ModelName = loadFile["Info"]["ModelName"].ToString();
    string version = loadFile["Info"]["Version"].ToString();

    if (version != SystemHandler.Handle.Recipes.GetVersion()) {
        // 버전 불일치 (현재 미구현 — 기존 LoadFromIni(string)와 동일 처리)
    }

    int m = 0;
    for (int i = 0; i < Sequences.Count; i++) {
        for (int j = 0; j < this[i].ActionCount; j++) {
            ParamBase param = this[i][j].Param;
            param.Load(loadFile, "Param" + m.ToString());
            m++;
        }
    }

    // 전역 CurrentRecipeName 갱신 (기존 코드 호환)
    pSetting.CurrentRecipeName = name;
    // Site별 CurrentRecipeName 갱신
    SiteManager.Handle[siteNumber - 1].CurrentRecipeName = name;

    return true;
}
```

**3단계: SaveToIni(int siteNumber, string name) 추가**

기존 `private bool SaveToIni(string name)` 메서드 바로 아래에 다음 오버로드를 추가한다:

```csharp
/// <summary>
/// Site 지정 INI 저장. Recipe/SiteN/name/main.ini 경로를 사용한다.
/// </summary>
private bool SaveToIni(int siteNumber, string name) {
    if (name != null) ModelName = name;
    string recipeFile = SystemHandler.Handle.Recipes.GetRecipeFilePath(siteNumber, ModelName);

    string recipeDir = Path.GetDirectoryName(recipeFile);
    if (Directory.Exists(recipeDir) == false) {
        Directory.CreateDirectory(recipeDir);
    }

    IniFile saveFile = new IniFile();
    saveFile["Info"]["ModelName"] = ModelName;
    saveFile["Info"]["Version"] = Version;

    int m = 0;
    for (int i = 0; i < Sequences.Count; i++) {
        for (int j = 0; j < this[i].ActionCount; j++) {
            ParamBase param = this[i][j].Param;
            param.Save(saveFile, "Param" + m.ToString());
            m++;
        }
    }

    saveFile.Save(recipeFile);
    return true;
}
```

**4단계: public LoadRecipe(int siteNumber, string name) 추가**

기존 `public bool LoadRecipe(string name, ERecipeFileType fileType = ERecipeFileType.Ini)` 메서드 바로 아래에 다음 오버로드를 추가한다:

```csharp
/// <summary>
/// Site 지정 레시피 로드. INI 전용 (Phase 4 범위).
/// </summary>
public bool LoadRecipe(int siteNumber, string name) {
    bool result = LoadFromIni(siteNumber, name);
    OnRecipeChanged?.Invoke(this, new RecipeChangedEventArgs(name));
    if (result) ExecOnLoad(name);
    return result;
}
```

**5단계: public SaveRecipe(int siteNumber, string name) 추가**

기존 `public bool SaveRecipe(string name, ERecipeFileType fileType = ERecipeFileType.Ini)` 메서드 바로 아래에 다음 오버로드를 추가한다:

```csharp
/// <summary>
/// Site 지정 레시피 저장. INI 전용 (Phase 4 범위).
/// </summary>
public bool SaveRecipe(int siteNumber, string name) {
    return SaveToIni(siteNumber, name);
}
```

**절대 금지 사항:**
- 기존 `LoadFromIni(string name)`, `SaveToIni(string name)`, `LoadRecipe(string name, ...)`, `SaveRecipe(string name, ...)` 메서드 수정 또는 삭제 금지
- `LoadFromJson`, `SaveToJson` 등 JSON 관련 코드 수정 금지
  </action>
  <verify>
    <automated>msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `using FinalVisionProject.Site` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `private bool LoadFromIni(int siteNumber, string name)` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `private bool SaveToIni(int siteNumber, string name)` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `public bool LoadRecipe(int siteNumber, string name)` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `public bool SaveRecipe(int siteNumber, string name)` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 `SiteManager.Handle[siteNumber - 1].CurrentRecipeName = name` 가 포함된다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 기존 `private bool LoadFromIni(string name)` 가 여전히 존재한다
    - `WPF_Example/Sequence/SequenceHandler.cs`에 기존 `public bool LoadRecipe(string name` 시그니처가 여전히 존재한다
    - msbuild 빌드가 exit code 0으로 완료된다 (에러 0건)
  </acceptance_criteria>
  <done>SequenceHandler에 Site 오버로드 4개 추가, 기존 메서드 유지, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: ProcessRecipeChange를 Site 인자 전달로 수정</name>
  <files>WPF_Example/Custom/SystemHandler.cs</files>
  <read_first>
    - WPF_Example/Custom/SystemHandler.cs (파일 전체 — ProcessRecipeChange 메서드 현재 내용과 Sequences/Recipes/Setting 참조 패턴 확인)
    - WPF_Example/Sequence/SequenceHandler.cs (Task 1에서 추가한 LoadRecipe(int, string) 시그니처 확인)
    - WPF_Example/Custom/Site/SiteManager.cs (Plan 01 생성 파일 — using 추가 필요 여부 확인)
  </read_first>
  <action>
**1단계: using 추가 (필요시)**

`WPF_Example/Custom/SystemHandler.cs` 상단 using 블록에 다음을 추가한다 (아직 없는 경우에만):
```csharp
using FinalVisionProject.Site;
```

**2단계: ProcessRecipeChange 메서드 교체**

기존 `ProcessRecipeChange` 메서드 전체를 다음으로 교체한다:

```csharp
private RecipeChangeResultPacket ProcessRecipeChange(RecipeChangePacket packet) {
    RecipeChangeResultPacket resultPacket = new RecipeChangeResultPacket();

    resultPacket.Target = packet.Sender;
    resultPacket.Site = packet.Site;

    int siteNumber = packet.Site;       // 1~5 (패킷에서 직접 Site 번호 사용)
    string recipeName = packet.RecipeName;

    // Site별 레시피 목록을 수집하여 HasRecipe 체크
    Recipes.CollectRecipe(siteNumber);

    if (Recipes.HasRecipe(recipeName) == false) {
        resultPacket.Result = EVisionResultType.NG;
    }
    else if (Sequences.LoadRecipe(siteNumber, recipeName)) {
        resultPacket.Result = EVisionResultType.OK;
    }
    else {
        resultPacket.Result = EVisionResultType.NG;
    }

    return resultPacket;
}
```

**변경 내용 설명:**
- `HasRecipe(recipeName)` 전에 `CollectRecipe(siteNumber)` 호출: Site별 레시피 목록을 최신화하여 다른 Site의 레시피와 혼합되지 않도록 한다
- `LoadRecipe(recipeName)` (전역) → `Sequences.LoadRecipe(siteNumber, recipeName)` (Site별): Plan 03 Task 1에서 추가한 오버로드 호출
- `SiteContext.CurrentRecipeName` 갱신은 `LoadFromIni(int siteNumber, string name)` 내부에서 처리되므로 여기서 별도 처리 불필요
- 기존 `Setting.CurrentRecipeName == recipeName` 중복 체크 제거 (로직 단순화 — LoadRecipe 자체가 항상 로드 시도)

**절대 금지 사항:**
- ProcessLightSet, ProcessRecipeGet, ProcessSiteStatus, ProcessTest 등 다른 메서드 수정 금지
- `Sequences` 필드(SequenceHandler)와 `Recipes` 필드(RecipeFiles) 혼동 주의: `Sequences.LoadRecipe`가 SequenceHandler.Handle.LoadRecipe와 동일함을 확인
  </action>
  <verify>
    <automated>msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Custom/SystemHandler.cs`에 `Recipes.CollectRecipe(siteNumber)` 가 포함된다
    - `WPF_Example/Custom/SystemHandler.cs`에 `Sequences.LoadRecipe(siteNumber, recipeName)` 가 포함된다
    - `WPF_Example/Custom/SystemHandler.cs`에 `int siteNumber = packet.Site` 가 포함된다
    - `WPF_Example/Custom/SystemHandler.cs`에 더 이상 `LoadRecipe(recipeName)` 단독 호출이 ProcessRecipeChange 내에 존재하지 않는다 (siteNumber 없는 버전 제거됨)
    - msbuild 빌드가 exit code 0으로 완료된다 (에러 0건)
  </acceptance_criteria>
  <done>ProcessRecipeChange가 Site 인자 기반으로 동작, SiteContext.CurrentRecipeName 자동 갱신, 빌드 성공</done>
</task>

</tasks>

<verification>
빌드 최종 확인:
```
msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet
```
에러 0건.

코드 검증:
```bash
grep -n "LoadRecipe.*siteNumber" "D:/Project/FinalVision/WPF_Example/Sequence/SequenceHandler.cs"
grep -n "SiteManager.Handle\[siteNumber" "D:/Project/FinalVision/WPF_Example/Sequence/SequenceHandler.cs"
grep -n "CollectRecipe(siteNumber)" "D:/Project/FinalVision/WPF_Example/Custom/SystemHandler.cs"
grep -n "Sequences.LoadRecipe(siteNumber" "D:/Project/FinalVision/WPF_Example/Custom/SystemHandler.cs"
```
모두 1건 이상 매칭되어야 한다.

Phase 4 완료 기준 동작 흐름:
1. TCP 클라이언트가 `$RECIPE_CHANGE:Site=2,RecipeName=Default@` 전송
2. `ProcessRecipeChange` → `siteNumber=2`, `CollectRecipe(2)` → `Sequences.LoadRecipe(2, "Default")`
3. `LoadFromIni(2, "Default")` → `Recipe/Site2/Default/main.ini` 로드
4. `SiteManager.Handle[1].CurrentRecipeName = "Default"` 갱신
5. 응답 패킷 OK 반환
</verification>

<success_criteria>
- SequenceHandler에 Site 오버로드 4개 존재 (LoadFromIni, SaveToIni, LoadRecipe, SaveRecipe)
- ProcessRecipeChange가 packet.Site를 siteNumber로 사용하여 Site별 레시피 로드
- 기존 LoadRecipe(string name) 오버로드 유지 (기존 UI 코드 호환)
- 빌드 에러 0건
- Phase 4 완료: 5개 Site 독립 레시피 로드/저장 + 통계 집계 백엔드 구현 완료
</success_criteria>

<output>
완료 후 `.planning/phases/04-5-site/04-03-SUMMARY.md` 를 생성한다.
</output>
