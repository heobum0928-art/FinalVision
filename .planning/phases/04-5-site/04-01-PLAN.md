---
phase: 04-5-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Site/SiteManager.cs
  - WPF_Example/Custom/Site/SiteContext.cs
  - WPF_Example/Custom/Site/SiteStatistics.cs
autonomous: true
requirements:
  - REQ-005
must_haves:
  truths:
    - "SiteManager.Handle 싱글톤으로 5개 SiteContext 배열을 소유한다"
    - "SiteManager.SwitchSite(int siteNumber)가 1~5 범위 외 값에 false를 반환한다"
    - "SiteContext.AddResult(bool isOk)가 SiteStatistics 카운터와 Queue<bool> 이력을 동시에 갱신한다"
    - "SiteStatistics.Yield가 TotalCount=0일 때 0.0을 반환한다"
    - "SiteStatistics.Add/Reset이 lock(_lock)으로 스레드 안전하게 구현된다"
    - "모든 3개 파일이 namespace FinalVisionProject.Site 아래에 위치한다"
  artifacts:
    - path: "WPF_Example/Custom/Site/SiteManager.cs"
      provides: "SiteManager 싱글톤 — 5개 SiteContext 배열, SwitchSite"
      exports: ["SiteManager"]
    - path: "WPF_Example/Custom/Site/SiteContext.cs"
      provides: "Site별 레시피명, 결과 이력(Queue<bool>), INotifyPropertyChanged"
      exports: ["SiteContext"]
    - path: "WPF_Example/Custom/Site/SiteStatistics.cs"
      provides: "검사수/OK수/NG수/수율 카운터, lock 스레드 안전"
      exports: ["SiteStatistics"]
  key_links:
    - from: "SiteContext"
      to: "SiteStatistics"
      via: "SiteContext.Statistics 프로퍼티 (new SiteStatistics())"
      pattern: "public SiteStatistics Statistics"
    - from: "SiteManager"
      to: "SiteContext"
      via: "_sites 배열 (SiteContext[SITE_COUNT])"
      pattern: "private readonly SiteContext\\[\\] _sites"
---

<objective>
SiteManager / SiteContext / SiteStatistics 3개 파일을 신규 생성한다.

Purpose: Phase 4의 핵심 백엔드 — 5개 Site의 레시피명·결과 이력·통계를 메모리에서 독립 관리하는 데이터 계층을 구축한다. Plan 03의 SequenceHandler Site 확장이 SiteManager.Handle을 참조하므로 이 파일들이 먼저 존재해야 한다.
Output: Custom/Site/ 하위 3개 .cs 파일, 빌드 성공
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-5-site/04-RESEARCH.md
</context>

<interfaces>
<!-- 이 Plan이 생성하는 타입 — Plan 03 executor가 직접 참조한다 -->

```csharp
// Custom/Site/SiteManager.cs
namespace FinalVisionProject.Site {
    public class SiteManager {
        public const int SITE_COUNT = 5;
        public static SiteManager Handle { get; }
        public int CurrentSiteIndex { get; }        // 0-based
        public SiteContext CurrentSite { get; }
        public SiteContext this[int siteIndex] { get; }
        public bool SwitchSite(int siteNumber);     // 1~5, false if out of range
    }
}

// Custom/Site/SiteContext.cs
namespace FinalVisionProject.Site {
    public class SiteContext : INotifyPropertyChanged {
        public int SiteNumber { get; }              // 1~5
        public string SiteName { get; }             // "Site1"~"Site5"
        public string CurrentRecipeName { get; set; }
        public SiteStatistics Statistics { get; }
        public const int MAX_HISTORY = 100;
        public void AddResult(bool isOk);
        public IEnumerable<bool> GetRecentResults();
    }
}

// Custom/Site/SiteStatistics.cs
namespace FinalVisionProject.Site {
    public class SiteStatistics : INotifyPropertyChanged {
        public int TotalCount { get; }
        public int OkCount { get; }
        public int NgCount { get; }
        public double Yield { get; }    // 0.0 when TotalCount==0
        public void Add(bool isOk);
        public void Reset();
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SiteStatistics.cs 생성</name>
  <files>WPF_Example/Custom/Site/SiteStatistics.cs</files>
  <read_first>
    - WPF_Example/Custom/Site/ (디렉터리 존재 여부 확인 — 없으면 생성 후 파일 작성)
    - WPF_Example/Custom/SystemSetting.cs (namespace 패턴 참조: partial class 선언 방식)
  </read_first>
  <action>
`WPF_Example/Custom/Site/` 디렉터리를 생성하고 `SiteStatistics.cs`를 아래 사양으로 작성한다.

파일 내용:
```csharp
using System.ComponentModel;

namespace FinalVisionProject.Site {
    public class SiteStatistics : INotifyPropertyChanged {
        private readonly object _lock = new object();
        private int _totalCount;
        private int _okCount;
        private int _ngCount;

        public int TotalCount { get { lock (_lock) { return _totalCount; } } }
        public int OkCount   { get { lock (_lock) { return _okCount; } } }
        public int NgCount   { get { lock (_lock) { return _ngCount; } } }
        public double Yield  {
            get {
                lock (_lock) {
                    return _totalCount == 0 ? 0.0 : (double)_okCount / _totalCount * 100.0;
                }
            }
        }

        public void Add(bool isOk) {
            lock (_lock) {
                _totalCount++;
                if (isOk) _okCount++;
                else _ngCount++;
            }
            RaisePropertyChanged("TotalCount");
            RaisePropertyChanged("OkCount");
            RaisePropertyChanged("NgCount");
            RaisePropertyChanged("Yield");
        }

        public void Reset() {
            lock (_lock) {
                _totalCount = 0;
                _okCount = 0;
                _ngCount = 0;
            }
            RaisePropertyChanged("TotalCount");
            RaisePropertyChanged("OkCount");
            RaisePropertyChanged("NgCount");
            RaisePropertyChanged("Yield");
        }

        public event PropertyChangedEventHandler PropertyChanged;
        private void RaisePropertyChanged(string name) {
            PropertyChangedEventHandler handler = PropertyChanged;
            if (handler != null) handler(this, new PropertyChangedEventArgs(name));
        }
    }
}
```

주의:
- `nameof()` 대신 문자열 리터럴 사용 (C# 5 이하 호환성 보장, 기존 코드베이스 패턴)
- `_lock` 객체로 Add/Reset의 카운터 변경을 원자적으로 보호
- RaisePropertyChanged는 lock 밖에서 호출 (lock 내부 이벤트 발생은 데드락 위험)
  </action>
  <verify>
    <automated>msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Custom/Site/SiteStatistics.cs` 파일이 존재한다
    - 파일에 `namespace FinalVisionProject.Site` 가 포함된다
    - 파일에 `public class SiteStatistics : INotifyPropertyChanged` 가 포함된다
    - 파일에 `private readonly object _lock` 이 포함된다
    - 파일에 `public double Yield` 가 포함된다
    - 파일에 `public void Reset()` 이 포함된다
    - msbuild 빌드가 exit code 0으로 완료된다
  </acceptance_criteria>
  <done>SiteStatistics.cs 존재, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: SiteContext.cs 생성</name>
  <files>WPF_Example/Custom/Site/SiteContext.cs</files>
  <read_first>
    - WPF_Example/Custom/Site/SiteStatistics.cs (Task 1에서 생성한 파일 — 타입 의존성 확인)
    - WPF_Example/Custom/SystemSetting.cs (INotifyPropertyChanged 패턴 참조)
  </read_first>
  <action>
`WPF_Example/Custom/Site/SiteContext.cs`를 아래 사양으로 작성한다.

파일 내용:
```csharp
using System.Collections.Generic;
using System.ComponentModel;

namespace FinalVisionProject.Site {
    public class SiteContext : INotifyPropertyChanged {
        public int SiteNumber { get; private set; }
        public string SiteName { get { return "Site" + SiteNumber; } }

        private string _currentRecipeName = "Default";
        public string CurrentRecipeName {
            get { return _currentRecipeName; }
            set {
                _currentRecipeName = value;
                RaisePropertyChanged("CurrentRecipeName");
            }
        }

        public SiteStatistics Statistics { get; private set; }

        private readonly Queue<bool> _resultHistory = new Queue<bool>();
        public const int MAX_HISTORY = 100;

        public SiteContext(int siteNumber) {
            SiteNumber = siteNumber;
            Statistics = new SiteStatistics();
        }

        public void AddResult(bool isOk) {
            Statistics.Add(isOk);
            if (_resultHistory.Count >= MAX_HISTORY)
                _resultHistory.Dequeue();
            _resultHistory.Enqueue(isOk);
        }

        public IEnumerable<bool> GetRecentResults() {
            return _resultHistory;
        }

        public event PropertyChangedEventHandler PropertyChanged;
        private void RaisePropertyChanged(string name) {
            PropertyChangedEventHandler handler = PropertyChanged;
            if (handler != null) handler(this, new PropertyChangedEventArgs(name));
        }
    }
}
```

주의:
- `Queue<bool>` 사용으로 FIFO 이력 관리 — 직접 배열 인덱스 관리 금지
- `_resultHistory`는 UI 스레드에서만 접근 (AddResult는 SystemProcess 스레드에서 호출되지 않음 — Phase 4 범위에서는 직접 호출 없음). 스레드 안전은 SiteStatistics에서만 필요
- `IEnumerable<bool>` 반환으로 외부 변경 차단
  </action>
  <verify>
    <automated>msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Custom/Site/SiteContext.cs` 파일이 존재한다
    - 파일에 `namespace FinalVisionProject.Site` 가 포함된다
    - 파일에 `public class SiteContext : INotifyPropertyChanged` 가 포함된다
    - 파일에 `public SiteStatistics Statistics` 가 포함된다
    - 파일에 `private readonly Queue<bool> _resultHistory` 가 포함된다
    - 파일에 `public const int MAX_HISTORY = 100` 가 포함된다
    - 파일에 `public void AddResult(bool isOk)` 가 포함된다
    - msbuild 빌드가 exit code 0으로 완료된다
  </acceptance_criteria>
  <done>SiteContext.cs 존재, SiteStatistics 참조 정상, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 3: SiteManager.cs 생성</name>
  <files>WPF_Example/Custom/Site/SiteManager.cs</files>
  <read_first>
    - WPF_Example/Custom/Site/SiteContext.cs (Task 2에서 생성한 파일)
    - WPF_Example/Custom/Site/SiteStatistics.cs (Task 1에서 생성한 파일)
    - WPF_Example/SystemHandler.cs (싱글톤 패턴: `public static SiteManager Handle { get; } = new SiteManager();` 참조)
  </read_first>
  <action>
`WPF_Example/Custom/Site/SiteManager.cs`를 아래 사양으로 작성한다.

파일 내용:
```csharp
namespace FinalVisionProject.Site {
    public class SiteManager {
        public const int SITE_COUNT = 5;

        public static SiteManager Handle { get; } = new SiteManager();

        private readonly SiteContext[] _sites = new SiteContext[SITE_COUNT];
        private int _currentSiteIndex = 0;  // 0-based (Site1=0 ... Site5=4)

        public int CurrentSiteIndex { get { return _currentSiteIndex; } }
        public SiteContext CurrentSite { get { return _sites[_currentSiteIndex]; } }
        public SiteContext this[int siteIndex] { get { return _sites[siteIndex]; } }

        private SiteManager() {
            for (int i = 0; i < SITE_COUNT; i++) {
                _sites[i] = new SiteContext(i + 1);  // siteNumber = 1~5
            }
        }

        /// <summary>
        /// 현재 Site를 전환한다. siteNumber는 1~5.
        /// 범위 밖이면 false 반환.
        /// </summary>
        public bool SwitchSite(int siteNumber) {
            int idx = siteNumber - 1;
            if (idx < 0 || idx >= SITE_COUNT) return false;
            _currentSiteIndex = idx;
            return true;
        }
    }
}
```

주의:
- `SiteManager.Handle[siteIndex]`의 siteIndex는 **0-based** (Site1=0, Site5=4)
- `SwitchSite(siteNumber)`의 siteNumber는 **1-based** (Site1=1, Site5=5)
- 외부 API(Plan 03 SequenceHandler)에서는 항상 1-based siteNumber 사용, 내부 배열 접근만 0-based 변환
- Plan 03에서 `SiteManager.Handle[siteNumber - 1].CurrentRecipeName = name` 패턴으로 접근함
  </action>
  <verify>
    <automated>msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WPF_Example/Custom/Site/SiteManager.cs` 파일이 존재한다
    - 파일에 `namespace FinalVisionProject.Site` 가 포함된다
    - 파일에 `public class SiteManager` 가 포함된다
    - 파일에 `public static SiteManager Handle { get; } = new SiteManager()` 가 포함된다
    - 파일에 `public const int SITE_COUNT = 5` 가 포함된다
    - 파일에 `private readonly SiteContext[] _sites` 가 포함된다
    - 파일에 `public bool SwitchSite(int siteNumber)` 가 포함된다
    - msbuild 빌드가 exit code 0으로 완료된다 (에러 0건)
  </acceptance_criteria>
  <done>SiteManager.cs 존재, Custom/Site/ 3개 파일 모두 빌드 성공</done>
</task>

</tasks>

<verification>
빌드 확인:
```
msbuild "D:/Project/FinalVision/FinalVision.sln" /p:Configuration=Debug /t:Build /v:quiet
```
에러 0건, 경고만 허용.

파일 존재 확인:
- `WPF_Example/Custom/Site/SiteStatistics.cs`
- `WPF_Example/Custom/Site/SiteContext.cs`
- `WPF_Example/Custom/Site/SiteManager.cs`

코드 확인:
- 3개 파일 모두 `namespace FinalVisionProject.Site` 포함
- `SiteManager.SITE_COUNT == 5`
- `SiteContext.MAX_HISTORY == 100`
- `SiteStatistics`에 `_lock` 객체 포함
</verification>

<success_criteria>
- Custom/Site/ 디렉터리에 3개 파일 생성 완료
- 빌드 성공 (에러 0건)
- SiteManager.Handle 싱글톤 접근 가능
- SwitchSite(0) → false, SwitchSite(6) → false (범위 외 거부)
- SiteStatistics.Yield: TotalCount=0 → 0.0, OK 3건 NG 1건 → 75.0
</success_criteria>

<output>
완료 후 `.planning/phases/04-5-site/04-01-SUMMARY.md` 를 생성한다.
</output>
