---
phase: 10-recipe-copy-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Utility/RecipeFileHelper.cs
  - WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs
autonomous: true
requirements: [RCP-01]

must_haves:
  truths:
    - "RecipeFiles.Copy()가 siteNumber 파라미터를 받아 Site 하위 경로 기준으로 복사한다"
    - "대상 Site 디렉터리가 없어도 Directory.CreateDirectory로 자동 생성되어 복사가 성공한다"
    - "이미 존재하는 레시피 이름으로 복사 시 덮어쓰기 확인 후 forceCopy=true로 복사 성공한다"
    - "OpenRecipeWindow에서 복사 후 CollectRecipe(1)로 목록이 갱신된다"
  artifacts:
    - path: "WPF_Example/Utility/RecipeFileHelper.cs"
      provides: "Copy(prevName, newName, siteNumber, forceCopy) 오버로드 + CopyFilesRecursively 선행 CreateDirectory"
      contains: "Directory.CreateDirectory(targetPath)"
    - path: "WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs"
      provides: "Btn_Copy_Click에서 siteNumber 전달 + forceCopy 분기 수정"
      contains: "Copy(SelectedRecipeName, newName, 1"
  key_links:
    - from: "WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs"
      to: "WPF_Example/Utility/RecipeFileHelper.cs"
      via: "RecipeFiles.Handle.Copy() 호출"
      pattern: "Copy\\(SelectedRecipeName.*1"
---

<objective>
레시피 복사 기능을 Site 경로 기반으로 수정하여, 대상 Site 디렉터리가 없어도 자동 생성 후 복사가 성공하도록 한다. 덮어쓰기 확인 후 forceCopy=true 전달 누락 버그도 함께 수정한다.

Purpose: Copy()가 Site 경로 개념 없이 동작하여 CollectRecipe(int siteNumber)와 경로 불일치가 발생하는 버그를 해소한다 (RCP-01).
Output: RecipeFileHelper.cs의 Copy() 오버로드 + CopyFilesRecursively 선행 Directory.CreateDirectory + OpenRecipeWindow 호출부 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-recipe-copy-infra/10-CONTEXT.md
@.planning/phases/10-recipe-copy-infra/10-RESEARCH.md

@WPF_Example/Utility/RecipeFileHelper.cs
@WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecipeFiles.Copy() siteNumber 오버로드 + CopyFilesRecursively 대상 디렉터리 자동 생성</name>
  <files>WPF_Example/Utility/RecipeFileHelper.cs</files>
  <read_first>
    - WPF_Example/Utility/RecipeFileHelper.cs (현재 Copy(), CopyFilesRecursively(), GetRecipeFilePath(int, string) 구현 확인)
  </read_first>
  <action>
RecipeFileHelper.cs에서 두 군데를 수정한다.

**1) Copy() 메서드 (라인 125~140) — siteNumber 파라미터 추가 (per D-06, D-07)**

기존 시그니처:
```csharp
public bool Copy(string prevName, string newName, bool forceCopy = false)
```

새 시그니처로 교체:
```csharp
//260402 hbk — D-06: siteNumber 파라미터 추가, Site 하위 경로 기준으로 복사
public bool Copy(string prevName, string newName, int siteNumber, bool forceCopy = false) {
    string siteDir     = Path.Combine(SystemHandler.Handle.Setting.RecipeSavePath, "Site" + siteNumber);
    string prevDirPath = Path.Combine(siteDir, prevName);
    string newDirPath  = Path.Combine(siteDir, newName);

    //해당 dir이 이미 존재함
    if (Directory.Exists(newDirPath) && (forceCopy == false)) {
        return false;
    }

    //폴더 통째로 복사하여 이름바꾼 후에 저장
    CopyFilesRecursively(prevDirPath, newDirPath);

    return true;
}
```

주의: 기존 `Copy(string, string, bool)` 시그니처를 완전히 교체한다. 기존 시그니처는 남기지 않는다.

**2) CopyFilesRecursively() 메서드 (라인 142~152) — 대상 루트 디렉터리 자동 생성 (per D-07)**

메서드 본문 첫 줄에 `Directory.CreateDirectory(targetPath);`를 추가한다:

```csharp
private static void CopyFilesRecursively(string sourcePath, string targetPath) {
    //260402 hbk — D-07: 대상 루트 디렉터리가 없어도 자동 생성 (이미 존재해도 안전)
    Directory.CreateDirectory(targetPath);

    //Now Create all of the directories
    foreach (string dirPath in Directory.GetDirectories(sourcePath, "*", SearchOption.AllDirectories)) {
        Directory.CreateDirectory(dirPath.Replace(sourcePath, targetPath));
    }

    //Copy all the files & Replaces any files with the same name
    foreach (string newPath in Directory.GetFiles(sourcePath, "*.*", SearchOption.AllDirectories)) {
        File.Copy(newPath, newPath.Replace(sourcePath, targetPath), true);
    }
}
```

`Directory.CreateDirectory()`는 경로가 이미 존재하면 no-op이므로 조건 체크 불필요 (if (!Directory.Exists) 패턴 사용 금지 — TOCTOU 레이스 방지).
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && grep -n "int siteNumber" WPF_Example/Utility/RecipeFileHelper.cs && grep -n "Directory.CreateDirectory(targetPath)" WPF_Example/Utility/RecipeFileHelper.cs</automated>
  </verify>
  <acceptance_criteria>
    - RecipeFileHelper.cs의 Copy() 시그니처가 `Copy(string prevName, string newName, int siteNumber, bool forceCopy = false)` 이다
    - Copy() 본문에 `Path.Combine(SystemHandler.Handle.Setting.RecipeSavePath, "Site" + siteNumber)` 경로 조합이 있다
    - CopyFilesRecursively() 본문 첫 줄에 `Directory.CreateDirectory(targetPath);` 가 있다
    - 기존 `Copy(string prevName, string newName, bool forceCopy = false)` 시그니처가 제거되었다
    - 주석에 `//260402 hbk` 형식이 사용되었다
  </acceptance_criteria>
  <done>Copy()가 siteNumber를 받아 Site 하위 경로 기준으로 동작하고, CopyFilesRecursively가 대상 디렉터리를 자동 생성한다</done>
</task>

<task type="auto">
  <name>Task 2: OpenRecipeWindow.Btn_Copy_Click 호출부 수정 — siteNumber + forceCopy 정합성</name>
  <files>WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs (현재 Btn_Copy_Click 라인 63~96 전체 흐름 확인)
    - WPF_Example/Utility/RecipeFileHelper.cs (Task 1에서 수정된 Copy() 시그니처 확인)
  </read_first>
  <action>
OpenRecipeWindow.xaml.cs의 Btn_Copy_Click 메서드(라인 63~96)에서 Copy() 호출부를 수정한다 (per D-06, D-08).

**현재 코드 (라인 86):**
```csharp
if(RecipeFiles.Handle.Copy(SelectedRecipeName, newName) == false) {
```

**수정 후:**
두 가지 분기를 처리해야 한다:

1. HasRecipe(newName)이 true인 경우 — ShowConfirmation 후 forceCopy=true 전달
2. HasRecipe(newName)이 false인 경우 — 기본 복사

라인 80~88의 try 블록 내부를 다음으로 교체:

```csharp
//260402 hbk — D-06: siteNumber=1 전달, D-08: 덮어쓰기 시 forceCopy=true
if (RecipeFiles.Handle.HasRecipe(newName)) {
    if(CustomMessageBox.ShowConfirmation(newName + SystemHandler.Handle.Localize[" Has Already Exists."], SystemHandler.Handle.Localize["Are you sure you want to overwrite the existing directory?"], MessageBoxButton.YesNo) != MessageBoxResult.Yes) {
        return;
    }
    //덮어쓰기 확인 완료 — forceCopy=true
    if(RecipeFiles.Handle.Copy(SelectedRecipeName, newName, 1, forceCopy: true) == false) {
        CustomMessageBox.Show(SystemHandler.Handle.Localize["Fail to copy recipe"], string.Format(SystemHandler.Handle.Localize["copy fail recipe {0} to {1}."], SelectedRecipeName, newName), MessageBoxImage.Error);
    }
}
else {
    if(RecipeFiles.Handle.Copy(SelectedRecipeName, newName, 1) == false) {
        CustomMessageBox.Show(SystemHandler.Handle.Localize["Fail to copy recipe"], string.Format(SystemHandler.Handle.Localize["copy fail recipe {0} to {1}."], SelectedRecipeName, newName), MessageBoxImage.Error);
    }
}
```

핵심 변경점:
- `Copy(SelectedRecipeName, newName)` → `Copy(SelectedRecipeName, newName, 1)` (siteNumber=1 추가)
- 덮어쓰기 분기에서 `Copy(SelectedRecipeName, newName, 1, forceCopy: true)` 로 forceCopy 명시 전달
- 기존 HasRecipe 체크가 try 블록 밖(라인 80)에 있었으나 try 블록 안으로 이동하여 Copy() 호출과 동일 블록에서 처리
  </action>
  <verify>
    <automated>cd D:/Project/FinalVision && grep -n "Copy(SelectedRecipeName" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs && grep -c "forceCopy: true" WPF_Example/UI/Recipe/OpenRecipeWindow.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - OpenRecipeWindow.xaml.cs에서 `Copy(SelectedRecipeName, newName, 1)` 호출이 존재한다
    - OpenRecipeWindow.xaml.cs에서 `Copy(SelectedRecipeName, newName, 1, forceCopy: true)` 호출이 존재한다
    - 기존 `Copy(SelectedRecipeName, newName)` (siteNumber 없는) 호출이 제거되었다
    - `HasRecipe(newName)` 분기와 `ShowConfirmation` 흐름이 유지된다
    - 주석에 `//260402 hbk` 형식이 사용되었다
  </acceptance_criteria>
  <done>OpenRecipeWindow에서 레시피 복사 시 siteNumber=1이 전달되고, 덮어쓰기 확인 후 forceCopy=true가 정확히 전달된다</done>
</task>

</tasks>

<verification>
1. `msbuild WPF_Example/FinalVision.csproj` 빌드 성공 (CS7036 컴파일 오류 없음)
2. RecipeFileHelper.cs에서 `Copy(string, string, bool)` 구 시그니처 완전 제거 확인
3. grep으로 프로젝트 전체에서 `RecipeFiles.Handle.Copy(` 호출부가 모두 siteNumber를 포함하는지 확인
</verification>

<success_criteria>
- RecipeFiles.Copy()가 siteNumber 파라미터를 받아 RecipeSavePath/Site{N}/ 하위 경로 기준으로 복사한다
- CopyFilesRecursively가 대상 루트 디렉터리를 자동 생성한다 (Directory.CreateDirectory 선행)
- OpenRecipeWindow에서 복사 시 siteNumber=1이 전달된다
- 덮어쓰기 확인 후 forceCopy=true가 정확히 전달된다
- 프로젝트 빌드가 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/10-recipe-copy-infra/10-01-SUMMARY.md`
</output>
