---
phase: 13-recipeeditorwindow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs
  - WPF_Example/UI/ControlItem/InspectionListView.xaml
  - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs
autonomous: true
requirements:
  - RCP-05
deferred_requirements:
  - RCP-02  # RecipeEditorWindow 팝업 — CONTEXT.md에 의해 다음 Phase로 이연
  - RCP-03  # Grab 미리보기 — CONTEXT.md에 의해 다음 Phase로 이연
  - RCP-04  # Save 버튼 별도 구현 — CONTEXT.md에 의해 다음 Phase로 이연
  - RCP-06  # Edit 버튼 진입점 — CONTEXT.md에 의해 다음 Phase로 이연

must_haves:
  truths:
    - "레시피 로드 완료 시 현재 Shot 파라미터가 메모리에 자동 백업된다"
    - "InspectionListView 툴바에 Reset 버튼이 표시된다"
    - "Shot 선택 후 Reset 클릭 시 해당 Shot 파라미터만 로드 시점 값으로 복원된다"
    - "Reset 후 PropertyGrid가 복원된 값으로 갱신된다"
  artifacts:
    - path: "WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs"
      provides: "TakeBackup 메서드 + _backup Dictionary + OnLoad 호출"
      contains: "TakeBackup"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml"
      provides: "Reset 버튼 XAML"
      contains: "button_reset"
    - path: "WPF_Example/UI/ControlItem/InspectionListView.xaml.cs"
      provides: "button_reset_Click 이벤트 핸들러"
      contains: "button_reset_Click"
  key_links:
    - from: "Sequence_Inspection.OnLoad()"
      to: "TakeBackup()"
      via: "OnLoad override 내부 호출"
      pattern: "TakeBackup\\(\\)"
    - from: "button_reset_Click"
      to: "RestoreShot(shotIndex)"
      via: "Sequence_Inspection 캐스팅 후 호출"
      pattern: "RestoreShot\\("
    - from: "button_reset_Click"
      to: "treeListBox_sequence.UnselectAll()"
      via: "PropertyGrid 강제 갱신 (Paste 패턴 재사용)"
      pattern: "UnselectAll"
---

<objective>
레시피 로드 시점에 Shot 파라미터를 메모리에 백업하고, InspectionListView 툴바의 Reset 버튼으로 선택된 Shot만 로드 시점 상태로 복원하는 기능을 구현한다.

Purpose: 작업자가 파라미터를 편집하다가 Save 전에 원래 값으로 되돌릴 수 있게 한다 (RCP-05 partial, per D-01/D-02/D-03).
Output: Sequence_Inspection에 백업/복원 메서드 + InspectionListView에 Reset 버튼 + 클릭 핸들러
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-recipeeditorwindow/13-CONTEXT.md
@.planning/phases/13-recipeeditorwindow/13-RESEARCH.md

<interfaces>
<!-- CRITICAL: InspectionRecipeManager.cs and ShotConfig.cs are NOT in FinalVision.csproj (dead code).
     The actual Shot parameters are InspectionParam objects on Action_Inspection actions.
     Backup/restore MUST use InspectionParam, NOT ShotConfig/InspectionRecipeManager. -->

From WPF_Example/Custom/Sequence/Inspection/Action_Inspection.cs:
```csharp
public class InspectionParam : CameraSlaveParam   //260326 hbk
{
    public readonly int ShotIndex;
    public string ProcessName { get; set; }
    public ERoiShape ROIShape { get; set; }
    public System.Windows.Rect ROI { get; set; }
    public UI.Circle ROICircle { get; set; }
    public double BlobMinArea { get; set; } = 100000;
    public double BlobMaxArea { get; set; } = 9999999;
    public int BlobThreshold { get; set; } = 100;
    public int DelayMs { get; set; } = 0;
    public string SimulImagePath { get; set; } = "";

    public override bool CopyTo(ParamBase param)   // copies ROIShape, ROI, ROICircle, BlobMinArea, BlobMaxArea, BlobThreshold, DelayMs, SimulImagePath + base camera fields
    public InspectionParam(object owner, int shotIndex) : base(owner)
}

public class Action_Inspection : ActionBase
{
    private InspectionParam _MyParam;
    // constructor: Param = new InspectionParam(this, shotIndex);
}
```

From WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs:
```csharp
public class Sequence_Inspection : SequenceBase   //260326 hbk
{
    // Actions accessed via indexer: this[i] returns ActionBase
    // Castable: this[i] is Action_Inspection, this[i].Param as InspectionParam
    public override void OnLoad()   // called by SequenceHandler.ExecOnLoad() after LoadFromIni
    {
        if (!SystemHandler.Handle.Lights.ApplyLight(_MyParam)) { }
        base.OnLoad();   // iterates Actions, calls act.OnLoad()
    }
}
```

From WPF_Example/UI/ControlItem/InspectionListView.xaml.cs:
```csharp
public partial class InspectionListView : UserControl
{
    private MainWindow mParentWindow;
    public ParamBase SelectedParam { get; private set; } = null;
    // Paste pattern for PropertyGrid refresh:
    //   int index = treeListBox_sequence.SelectedIndex;
    //   treeListBox_sequence.UnselectAll();
    //   treeListBox_sequence.SelectedIndex = index;
    //   if (SelectedParam is InspectionParam ip)
    //       mParentWindow.mainView.SetParam(ESequence.Inspection, ip);
}
```

From WPF_Example/Sequence/Sequence/SequenceBase.cs (line 267):
```csharp
public virtual void OnLoad() {
    foreach(ActionBase act in Actions) { act.OnLoad(); }
}
```

From WPF_Example/Sequence/SequenceHandler.cs:
```csharp
// LoadRecipe flow:
//   LoadFromIni(name) → ParamBase.Load per action → ExecOnLoad(name)
// ExecOnLoad calls seq.OnLoad() for each sequence
// Both LoadFromIni overloads (string name) and (int siteNumber, string name) end with ExecOnLoad
```

Existing button XAML pattern (InspectionListView.xaml lines 222-237):
```xml
<ToolBar>
    <Button x:Name="button_copy" Click="button_copy_Click" Style="{StaticResource disalbedStyle}" ToolTip="...">
        <StackPanel Orientation="Vertical">
            <Image Source="/Resource/copy.png" Stretch="Uniform" Width="42" Height="42"/>
            <TextBlock Text="Copy" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
    </Button>
    <Button x:Name="button_paste" Click="button_paste_Click" IsEnabled="False" Style="{StaticResource disalbedStyle}">
        <StackPanel Orientation="Vertical">
            <Image Source="/Resource/paste.png" Stretch="Uniform" Width="42" Height="42"/>
            <TextBlock Text="paste" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
    </Button>
</ToolBar>
```

Available icons in /Resource/: camera.png, copy.png, paste.png, folder.png, repair.png, save.png, saveas.png, dialog.png (NO undo.png or refresh.png)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backup/restore methods to Sequence_Inspection + trigger on OnLoad</name>
  <files>WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs</files>
  <read_first>
    - WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs (current full file — OnLoad override at line 123)
    - WPF_Example/Custom/Sequence/Inspection/Action_Inspection.cs (InspectionParam class lines 29-172, CopyTo at line 149)
    - WPF_Example/Sequence/Sequence/SequenceBase.cs (lines 265-271 — base.OnLoad iterates Actions)
  </read_first>
  <action>
Add a backup dictionary and backup/restore methods to `Sequence_Inspection` class. Per D-01, backup happens at recipe load time (OnLoad). Per D-03, restore is per-shot only.

IMPORTANT: The actual shot parameters are `InspectionParam` objects (NOT `ShotConfig`). `InspectionRecipeManager` is dead code (not in csproj). Do NOT use it.

1. Add field at top of `Sequence_Inspection` fields region:
```csharp
private Dictionary<int, InspectionParam> _backup = new Dictionary<int, InspectionParam>();   //260407 hbk — 로드 시점 Shot 파라미터 백업
```

2. Add `using System.Collections.Generic;` if not already present (it IS present at line 3).

3. Add `TakeBackup()` method in the methods region, after `AddResponse()`:
```csharp
public void TakeBackup()   //260407 hbk — D-01: LoadRecipe 완료 직후 전체 Shot 백업
{
    _backup.Clear();
    for (int i = 0; i < ActionCount; i++)
    {
        if (this[i].Param is InspectionParam src)
        {
            var snap = new InspectionParam(this[i], src.ShotIndex);
            src.CopyTo(snap);
            _backup[i] = snap;
        }
    }
}
```

4. Add `RestoreShot(int)` method right after `TakeBackup()`:
```csharp
public bool RestoreShot(int shotIndex)   //260407 hbk — D-03: 선택된 Shot만 복원
{
    if (!_backup.ContainsKey(shotIndex)) return false;
    if (shotIndex < 0 || shotIndex >= ActionCount) return false;
    if (!(this[shotIndex].Param is InspectionParam target)) return false;
    _backup[shotIndex].CopyTo(target);
    return true;
}
```

5. Add `HasBackup` property right after `RestoreShot`:
```csharp
public bool HasBackup => _backup.Count > 0;   //260407 hbk — Reset 버튼 가드
```

6. Modify `OnLoad()` override (currently at line 123) to call `TakeBackup()` after `base.OnLoad()`:
```csharp
public override void OnLoad()   //260326 hbk
{
    if (!SystemHandler.Handle.Lights.ApplyLight(_MyParam))
    {
    }
    base.OnLoad();
    TakeBackup();   //260407 hbk — D-01: 로드 완료 후 즉시 백업
}
```

This ensures both `LoadFromIni(string)` and `LoadFromIni(int, string)` paths trigger backup via `ExecOnLoad()`.

Comment style: `//260407 hbk` on all new lines.
  </action>
  <verify>
    <automated>cd "D:/Project/FinalVision" && grep -n "TakeBackup\|RestoreShot\|HasBackup\|_backup" WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs</automated>
  </verify>
  <acceptance_criteria>
    - Sequence_Inspection.cs contains `private Dictionary<int, InspectionParam> _backup`
    - Sequence_Inspection.cs contains `public void TakeBackup()`
    - Sequence_Inspection.cs contains `public bool RestoreShot(int shotIndex)`
    - Sequence_Inspection.cs contains `public bool HasBackup =>`
    - OnLoad() method contains `TakeBackup();` AFTER `base.OnLoad();`
    - TakeBackup uses `InspectionParam` (NOT ShotConfig)
    - RestoreShot uses `_backup[shotIndex].CopyTo(target)` pattern (value copy, not reference replace)
    - No FAI/Halcon/edge measurement code added
  </acceptance_criteria>
  <done>Sequence_Inspection has TakeBackup/RestoreShot/HasBackup methods, OnLoad calls TakeBackup after base.OnLoad</done>
</task>

<task type="auto">
  <name>Task 2: Add Reset button to InspectionListView toolbar + click handler</name>
  <files>WPF_Example/UI/ControlItem/InspectionListView.xaml, WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</files>
  <read_first>
    - WPF_Example/UI/ControlItem/InspectionListView.xaml (lines 180-248 — toolbar + PropertyGrid area)
    - WPF_Example/UI/ControlItem/InspectionListView.xaml.cs (full file — especially button_paste_Click at line 275 for PropertyGrid refresh pattern, Btn_start_Click at line 107 for shotIndex pattern)
    - WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs (after Task 1 — TakeBackup/RestoreShot/HasBackup)
  </read_first>
  <action>
Add Reset button to InspectionListView toolbar and implement click handler. Per D-02, button goes in same toolbar area as Copy/Paste. Per D-03, restores only the selected Shot.

**XAML change (InspectionListView.xaml):**

After the Copy/Paste `</ToolBar>` closing tag (line 237), before `</ToolBarTray>` (line 238), add a new ToolBar block:
```xml
                    <ToolBar>
                        <Button x:Name="button_reset" Click="button_reset_Click" Style="{StaticResource disalbedStyle}"
                                ToolTip="선택된 Shot 파라미터를 레시피 로드 시점으로 초기화">
                            <StackPanel Orientation="Vertical">
                                <Image Source="/Resource/repair.png" Stretch="Uniform" Width="42" Height="42"/>
                                <TextBlock Text="Reset" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </ToolBar>
```

CRITICAL: Do NOT use `--` inside any XAML comment (MC3000 build warning).

**Code-behind change (InspectionListView.xaml.cs):**

Add `button_reset_Click` method after `button_paste_Click` (after line 301):
```csharp
private void button_reset_Click(object sender, RoutedEventArgs e)   //260407 hbk — D-02, D-03: 선택 Shot 파라미터 Reset
{
    NodeViewModel node = treeListBox_sequence.SelectedItem as NodeViewModel;
    if (node == null || node.NodeType != ENodeType.Action) return;
    if (node.SequenceID != ESequence.Inspection) return;

    //260407 hbk — shotIndex 결정 (Btn_start_Click 기존 패턴 재사용)
    SequenceBase inspSeq = SystemHandler.Handle.Sequences[ESequence.Inspection];
    if (inspSeq == null) return;
    int shotIndex = -1;
    for (int i = 0; i < inspSeq.ActionCount; i++)
    {
        if (inspSeq[i].ID == node.ActionID) { shotIndex = i; break; }
    }
    if (shotIndex < 0) return;

    //260407 hbk — Sequence_Inspection 캐스팅 + RestoreShot 호출
    Sequence_Inspection inspectionSeq = inspSeq as Sequence_Inspection;
    if (inspectionSeq == null || !inspectionSeq.HasBackup)
    {
        CustomMessageBox.Show("Reset", "백업 데이터가 없습니다. 레시피를 먼저 로드하세요.", MessageBoxImage.Warning);
        return;
    }

    //260407 hbk — 확인 다이얼로그 (Paste 패턴 준수)
    MessageBoxResult res = CustomMessageBox.ShowConfirmation("Reset", 
        string.Format("Shot_{0} 파라미터를 레시피 로드 시점으로 되돌리시겠습니까?", shotIndex + 1), 
        MessageBoxButton.OKCancel);
    if (res != MessageBoxResult.OK) return;

    bool ok = inspectionSeq.RestoreShot(shotIndex);
    if (!ok)
    {
        CustomMessageBox.Show("Reset", "복원에 실패했습니다.", MessageBoxImage.Error);
        return;
    }

    //260407 hbk — PropertyGrid 강제 갱신 (Paste 패턴 재사용)
    int index = treeListBox_sequence.SelectedIndex;
    treeListBox_sequence.UnselectAll();
    treeListBox_sequence.SelectedIndex = index;

    //260407 hbk — ShotTabView 강제 갱신
    if (SelectedParam is InspectionParam ip)
        mParentWindow.mainView.SetParam(ESequence.Inspection, ip);

    mParentWindow.statusBar.Model.SetText(string.Format("Reset Shot_{0} 완료", shotIndex + 1));
}
```

Ensure `using FinalVisionProject.Sequence;` is already present (it IS, line 12).
  </action>
  <verify>
    <automated>cd "D:/Project/FinalVision" && grep -n "button_reset" WPF_Example/UI/ControlItem/InspectionListView.xaml WPF_Example/UI/ControlItem/InspectionListView.xaml.cs && grep -c "Sequence_Inspection" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs</automated>
  </verify>
  <acceptance_criteria>
    - InspectionListView.xaml contains `x:Name="button_reset"` with `Click="button_reset_Click"`
    - InspectionListView.xaml contains `Source="/Resource/repair.png"` for the Reset button icon
    - InspectionListView.xaml contains `Text="Reset"` in the button StackPanel
    - InspectionListView.xaml has NO `--` inside XAML comments (MC3000 rule)
    - InspectionListView.xaml.cs contains `private void button_reset_Click(object sender, RoutedEventArgs e)`
    - InspectionListView.xaml.cs contains `Sequence_Inspection inspectionSeq = inspSeq as Sequence_Inspection`
    - InspectionListView.xaml.cs contains `inspectionSeq.RestoreShot(shotIndex)`
    - InspectionListView.xaml.cs contains `CustomMessageBox.ShowConfirmation("Reset"` (confirmation dialog)
    - InspectionListView.xaml.cs contains `treeListBox_sequence.UnselectAll()` in reset handler (PropertyGrid refresh)
    - InspectionListView.xaml.cs contains `mParentWindow.mainView.SetParam(ESequence.Inspection, ip)` in reset handler
    - All new lines have `//260407 hbk` comment format
  </acceptance_criteria>
  <done>Reset 버튼이 InspectionListView 툴바에 표시되고, 클릭 시 선택된 Shot만 로드 시점 파라미터로 복원 + PropertyGrid 갱신</done>
</task>

</tasks>

<verification>
1. `grep -rn "TakeBackup\|RestoreShot\|HasBackup" WPF_Example/Custom/Sequence/Inspection/Sequence_Inspection.cs` — all 3 methods present
2. `grep -n "button_reset" WPF_Example/UI/ControlItem/InspectionListView.xaml` — Reset button in XAML
3. `grep -n "button_reset_Click" WPF_Example/UI/ControlItem/InspectionListView.xaml.cs` — handler exists
4. `grep -c "\-\-" WPF_Example/UI/ControlItem/InspectionListView.xaml` — verify no MC3000 violations (only valid in non-comment context)
5. MSBuild 빌드 성공 확인 (수동)
</verification>

<success_criteria>
- Sequence_Inspection.OnLoad()가 TakeBackup()을 호출하여 모든 Shot 파라미터를 메모리에 백업
- InspectionListView 툴바에 Reset 버튼이 Copy/Paste 다음에 표시됨
- Reset 클릭 시 확인 다이얼로그 후 선택된 Shot만 로드 시점 값으로 복원됨 (다른 Shot 불변)
- 복원 후 PropertyGrid와 ShotTabView가 갱신된 값을 표시함
- 빌드 성공 (MC3000 경고 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/13-recipeeditorwindow/13-01-SUMMARY.md`
</output>
