<Window x:Class="FinalVisionProject.UI.ImageManageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="이미지 관리" Height="500" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Loaded="Window_Loaded">
    <!-- //260406 hbk D-09: 이미지 관리 창 -->
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="저장된 이미지 날짜 폴더" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- //260406 hbk D-10: 날짜 폴더 체크박스 목록 -->
        <ListBox x:Name="lb_dateFolders" Grid.Row="1" SelectionMode="Extended">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"
                              FontSize="13" Margin="4,2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="새로고침" Width="80" FontSize="12" Click="Btn_RefreshFolders_Click" Margin="0,0,8,0"/>
            <Button x:Name="btn_deleteFolders" Content="선택 삭제" Width="100" FontSize="12"
                    Click="Btn_DeleteFolders_Click" Foreground="Red"/>
        </StackPanel>
    </Grid>
</Window>
