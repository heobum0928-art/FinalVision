<UserControl x:Class="FinalVisionProject.UI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinalVisionProject.UI"
             mc:Ignorable="d" Loaded="MainView_Loaded" d:DesignWidth="600" d:DesignHeight="400">
    <TabControl x:Name="tabControl_view" TabStripPlacement="Top" FontSize="16">
        <TabControl.ItemContainerStyle>
            <Style TargetType="{x:Type TabItem}">
                <Setter Property="Visibility" Value="Visible"/>
                <!--<Setter Property="Background" Value="#ffffffff"/> --><!--TabControl Background Color-->
            </Style>
        </TabControl.ItemContainerStyle>
        
        <TabItem Header="Main View">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"></RowDefinition>
                    <RowDefinition Height="10*"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition> <!--260326 hbk Shot 뷰어 컨트롤-->
                </Grid.RowDefinitions>

                <Grid Grid.Column="0" Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48"></ColumnDefinition>
                        <ColumnDefinition Width="3*"></ColumnDefinition>
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                       
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    
                    <Image Grid.Column="0" Grid.Row="0" Source="/Resource/focus.png"></Image>
                    <ComboBox x:Name="comboBox_sequence" Grid.Column="1" Grid.Row="0" Margin="0,2,2,2" FontSize="24" SelectionChanged="ComboBox_sequence_SelectionChanged" SelectedItem="{Binding SelectedSeqName}" SelectedIndex="{Binding SelectedSeqIndex}"></ComboBox>
                    <ComboBox x:Name="comboBox_viewMode" Grid.Column="2" Grid.Row="0" Margin="2" FontSize="24" SelectionChanged="ComboBox_viewMode_SelectionChanged" SelectedItem="{Binding SelectedViewMode}" SelectedIndex="{Binding SelectedViewIndex}"></ComboBox>
                    
                    <StackPanel Orientation="Vertical" Grid.Column="3" Grid.Row="0">
                        <Slider x:Name="slider_scale" Value="100" Minimum="20" Maximum="200" Thumb.DragStarted="Slider_DragStarted" Thumb.DragCompleted="Slider_DragCompleted" ValueChanged="Slider_scale_ValueChanged" TickFrequency="5">
                        </Slider>
                        <TextBlock x:Name="textBlock_scale" TextAlignment="Center" Text="{Binding Value, ElementName=slider_scale, StringFormat={}{0:0} %}"></TextBlock>
                    </StackPanel>

                    <Label x:Name="label_pos" Grid.Column="4" Grid.Row="0" HorizontalContentAlignment="Right" Content="{Binding CurrentPosDisplay, ElementName=canvas_main}"></Label>
                </Grid>

                <ScrollViewer x:Name="scrollViewer" Grid.Column="0" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="#FF303030" ScrollChanged="ScrollViewer_ScrollChanged">
                    <local:RuntimeResizer x:Name="canvas_main" HorizontalAlignment="Left" VerticalAlignment="Top" MouseWheel="Canvas_main_MouseWheel" ScrollViewer.CanContentScroll="True">
                        <Canvas.Background>
                            <ImageBrush Stretch="None" AlignmentX="Left" AlignmentY="Top"></ImageBrush>
                        </Canvas.Background>
                        <!--<Image Canvas.Left="0" Canvas.Top="0" x:Name="image_foreground" Stretch="None" Width="{Binding Path=Width, ElementName=canvas_main}" Height="{Binding Path=Height, ElementName=canvas_main}" VerticalAlignment="Top" HorizontalAlignment="Left" Opacity="1" OpacityMask="Black"></Image> -->
                    </local:RuntimeResizer>
                </ScrollViewer>
                <Label Grid.Column="0" Grid.Row="1" x:Name="label_message" Margin="0,10,20,0" HorizontalAlignment="Right" VerticalAlignment="Top" FontSize="24" Foreground="Red" HorizontalContentAlignment="Right">NO Image</Label>

                <!--260326 hbk Shot 뷰어 컨트롤 시작-->
                <Grid Grid.Column="0" Grid.Row="2" Margin="0,2,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="36"></RowDefinition>   <!--Shot 선택 + 이미지 종류-->
                        <RowDefinition Height="36"></RowDefinition>   <!--결과 스트립-->
                    </Grid.RowDefinitions>

                    <!-- Row 0: Shot 선택 ComboBox + 원본/측정 RadioButton -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,2">
                        <Label Content="Shot:" FontSize="14" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <ComboBox x:Name="comboBox_shot" Width="200" FontSize="14" Margin="0,0,8,0"
                                  SelectionChanged="ComboBox_shot_SelectionChanged"/>  <!--260326 hbk-->
                        <RadioButton x:Name="radioButton_original" Content="원본" FontSize="14"
                                     GroupName="ImageMode" IsChecked="True"
                                     VerticalAlignment="Center" Margin="0,0,8,0"
                                     Checked="RadioButton_imageMode_Checked"/>         <!--260326 hbk-->
                        <RadioButton x:Name="radioButton_annotated" Content="측정" FontSize="14"
                                     GroupName="ImageMode"
                                     VerticalAlignment="Center"
                                     Checked="RadioButton_imageMode_Checked"/>         <!--260326 hbk-->
                    </StackPanel>

                    <!-- Row 1: 결과 스트립 (Shot 1~5 OK/NG/미실행 표시) -->
                    <UniformGrid Grid.Row="1" Rows="1" Columns="5" Margin="4,2">  <!--260326 hbk-->
                        <Border x:Name="stripBorder_Shot1" Background="Gray" Margin="2,0"
                                MouseLeftButtonDown="StripBorder_MouseLeftButtonDown" Tag="0"
                                CornerRadius="3">
                            <Label Content="Shot 1" FontSize="12" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        <Border x:Name="stripBorder_Shot2" Background="Gray" Margin="2,0"
                                MouseLeftButtonDown="StripBorder_MouseLeftButtonDown" Tag="1"
                                CornerRadius="3">
                            <Label Content="Shot 2" FontSize="12" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        <Border x:Name="stripBorder_Shot3" Background="Gray" Margin="2,0"
                                MouseLeftButtonDown="StripBorder_MouseLeftButtonDown" Tag="2"
                                CornerRadius="3">
                            <Label Content="Shot 3" FontSize="12" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        <Border x:Name="stripBorder_Shot4" Background="Gray" Margin="2,0"
                                MouseLeftButtonDown="StripBorder_MouseLeftButtonDown" Tag="3"
                                CornerRadius="3">
                            <Label Content="Shot 4" FontSize="12" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                        <Border x:Name="stripBorder_Shot5" Background="Gray" Margin="2,0"
                                MouseLeftButtonDown="StripBorder_MouseLeftButtonDown" Tag="4"
                                CornerRadius="3">
                            <Label Content="Shot 5" FontSize="12" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" Foreground="White"/>
                        </Border>
                    </UniformGrid>
                </Grid>
                <!--260326 hbk Shot 뷰어 컨트롤 끝-->
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
