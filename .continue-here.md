---
phase: ad-hoc (Phase 5 논의 세션)
status: paused
last_updated: 2026-03-26T08:10:57Z
---

<current_state>
Phase 5 12개 항목 논의 완료 + 3개 버그픽스/기능추가 코드 수정 완료.
미커밋 상태. 빌드 테스트 필요.
</current_state>

<completed_work>

1. **Phase 5 12개 항목 논의** - 완료
   - 모드 구분(검사/시뮬), ROI 5개 개별, 시퀀스 알고리즘, OK/NG 결과, PC인터페이스, 티칭 방식, 디버깅 위치, 시뮬 오토런 방법

2. **MainView 마우스 드로잉 비활성화** - 완료
   - `MainView.xaml.cs:128`: IsEditable setter → `canvas_main.IsEditable = false` 고정
   - 로그인해도 드로잉 불가

3. **DeviceSelector 티칭창 X,Y 위치 추가** - 완료
   - `DisplayConfig.cs`: `CenterRectOffsetX`, `CenterRectOffsetY` 추가 (Spinnable)
   - `VirtualCamera.cs:285`: Rect 위치에 Offset 반영
   - `DeviceSelector.xaml.cs`: `PropertyChanged → canvas_preview.InvalidateVisual()` 즉시 갱신

4. **런버튼 NullReferenceException 픽스** - 완료
   - 원인: 레시피 파일 없을 때 ExecOnLoad 미호출 → `_Camera = null` → Run에서 크래시
   - `SequenceHandler.cs:170`: `if (result) ExecOnLoad(name)` → `ExecOnLoad(name)` (조건 제거)
</completed_work>

<remaining_work>

- 빌드 및 런타임 테스트
- Phase 5 통신 관련 (12번 항목): 시뮬모드에서 버튼 1개로 오토 검사 구현
  → TestPacket 직접 생성 → `Sequences.Start(fakePacket)` 호출 방식으로 구현 예정
</remaining_work>

<decisions_made>

- 마우스 드로잉: 메인화면에서 완전 비활성화 (원래 없었던 기능)
- ROI 위치 티칭: DisplayConfig에 OffsetX/Y 추가 (X,Y는 이미지 중앙 기준 오프셋)
- 카메라 초기화: 레시피 파일 존재 여부와 무관하게 OnLoad 항상 호출
- 통신 모듈: 다음 세션에서 구현
</decisions_made>

<blockers>
없음
</blockers>

<context>
수정된 파일 5개 모두 미커밋 상태.
`D:/Project/FinalVision/WPF_Example` 기준:
- UI/ContentItem/MainView.xaml.cs
- Device/DisplayConfig.cs
- Device/Camera/VirtualCamera.cs
- UI/Device/DeviceSelector.xaml.cs
- Sequence/SequenceHandler.cs
</context>

<next_action>
1. 빌드 확인 (VS에서 Ctrl+Shift+B)
2. 테스트: 티칭창 열기 → OffsetX/Y 변경 → 즉시 Rect 위치 반영 확인
3. 테스트: 레시피 없는 상태에서 런 버튼 → NullRef 사라졌는지 확인
4. (다음 작업) 시뮬 오토런 버튼 구현
</next_action>
